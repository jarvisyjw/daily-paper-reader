---
title: "CAM3R: Camera-Agnostic Model for 3D Reconstruction"
title_zh: CAM3R：一种相机无关的三维重建模型
authors: "Namitha Guruprasad, Abhay Yadav, Cheng Peng, Rama Chellappa"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22631v1"
tags: ["query:3d"]
score: 10.0
evidence: 跨不同相机模型的无位姿图像稠密3D重建
tldr: 从无位姿图像恢复密集三维几何是计算机视觉的基础挑战。现有方法主要基于针孔相机数据训练，在处理广角图像时性能显著下降。为此，本文提出了CAM3R，一种相机无关的前馈式三维重建模型，能够处理无需先验标定的广角图像。其核心是一个双视图网络，包含估计像素光线方向的射线模块和推断径向距离的跨视图模块，并通过射线感知全局对齐框架整合成一致的三维场景。该方法在多种相机模型数据集上实现了最先进的位姿估计与重建性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22631v1/fig-001.webp\", \"caption\": \"Fig. 2: CAM3R Overview. Given an input image pair (I1, I2), the framework operates through two parallel streams. The Shared Ray Module recovers the internal camera geometry by regressing Spherical Harmonic coefficients to reconstruct continuous ray directional fields di. Simultaneously, the Cross-view Module extracts features and utilizes a dual-block transformer decoder to facilitate information exchange between the two views. Specialized DPT heads then regress radial distances ri with confidence maps σi, while a Relative Pose Network estimates the rigid transformation P2→1. The local pointmaps Xi,i are generated by fusing rays di with radial distances ri. Finally, the second view is transformed into the reference coordinate frame of the first view via P2→1 to produce the globally aligned 3D reconstruction.\", \"page\": 6, \"index\": 1, \"width\": 723, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-002.webp\", \"caption\": \"Fig. 3: Qualitative Two-View Reconstructions. Visualization of 3D point clouds for image pairs across diverse optical manifolds (panorama, fisheye, pinhole). Despite extreme radial distortions and camera geometries, relative poses are accurately recovered and structural consistency is maintained. Note this is the raw output of the network.\", \"page\": 11, \"index\": 2, \"width\": 723, \"height\": 530}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-003.webp\", \"caption\": \"Fig. 5: Qualitative Multi-View Reconstructions. Global camera trajectories and dense point clouds recovered from unstructured image pools across diverse datasets. Despite high radial distortion and lack of scenegraph information, globally consistent poses and structural geometry are maintained through the Ray-Aware Global Alignment, effectively mitigating trajectory drift and scale ambiguity.\", \"page\": 13, \"index\": 3, \"width\": 723, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-004.webp\", \"caption\": \"Table 1: Two-view relative pose estimation results. We report Accuracy at 15◦ (higher is better). Bold indicates best performance.\", \"page\": 12, \"index\": 4, \"width\": 718, \"height\": 181}]"
motivation: 现有基于针孔相机数据训练的先进三维重建模型在处理鱼眼等广角图像时会出现严重的几何退化问题。
method: 该框架包含一个双视图网络，分为用于估计像素级光线方向的射线模块和用于推断径向距离的跨视图模块，并引入射线感知全局对齐框架以整合成一致场景。
result: 在包括全景、鱼眼和针孔图像在内的多种相机模型数据集上的广泛实验验证了其有效性。
conclusion: CAM3R 在多种相机模型上均实现了最先进的位姿估计与三维重建性能。
---

## 摘要
从无位姿图像中恢复稠密三维几何结构，仍然是计算机视觉领域的一项基础性挑战。当前最先进的模型主要在透视数据集上进行训练，这隐式地将其约束于标准针孔相机几何。因此，当这些模型应用于通过非直线光学器件（如鱼眼或全景传感器）捕获的广角图像时，会出现显著的几何退化。为解决这一问题，我们提出了CAM3R，一种相机无关的前馈式三维重建模型，能够处理来自广角相机模型的图像而无需预先校准。我们的框架由一个双视图网络构成，该网络分为两个部分：一个用于估计逐像素光线方向的射线模块（RM），以及一个用于通过置信度图、点图和相对位姿推断径向距离的跨视图模块（CVM）。为了将这些成对预测统一到一个一致的三维场景中，我们引入了一个射线感知全局对齐框架，用于位姿优化和尺度优化，同时严格保持预测的局部几何结构。在包括全景、鱼眼和针孔图像在内的多种相机模型数据集上进行的大量实验表明，CAM3R在位姿估计和重建方面确立了新的最先进水平。

## Abstract
Recovering dense 3D geometry from unposed images remains a foundational challenge in computer vision. Current state-of-the-art models are predominantly trained on perspective datasets, which implicitly constrains them to a standard pinhole camera geometry. As a result, these models suffer from significant geometric degradation when applied to wide-angle imagery captured via non-rectilinear optics, such as fisheye or panoramic sensors. To address this, we present CAM3R, a Camera-Agnostic, feed-forward Model for 3D Reconstruction capable of processing images from wide-angle camera models without prior calibration. Our framework consists of a two-view network which is bifurcated into a Ray Module (RM) to estimate per-pixel ray directions and a Cross-view Module (CVM) to infer radial distance with confidence maps, pointmaps, and relative poses. To unify these pairwise predictions into a consistent 3D scene, we introduce a Ray-Aware Global Alignment framework for pose refinement and scale optimization while strictly preserving the predicted local geometry. Extensive experiments on various camera model datasets, including panorama, fisheye and pinhole imagery, demonstrate that CAM3R establishes a new state-of-the-art in pose estimation and reconstruction.

---

## 论文详细总结（自动生成）

# CAM3R: Camera-Agnostic Model for 3D Reconstruction 论文总结

## 1. 核心问题与整体含义
*   **研究动机**：当前最先进的3D重建基础模型（如DUSt3R、MASt3R等）主要基于针孔相机模型的数据进行训练。当这些模型处理来自非线性光学器件（如鱼眼、全景镜头）的广角图像时，由于固有的几何假设不匹配，会产生严重的几何失真和性能退化。
*   **核心目标**：提出一个**相机无关的、前馈式**的3D重建框架，能够直接从**未标定、无位姿**的图像中恢复稠密三维几何，并适用于多种相机模型（针孔、鱼眼、全景），克服现有方法对特定相机模型的依赖。

## 2. 方法论
*   **核心思想**：将相机内部几何（光线方向）与场景几何（深度/距离）进行显式解耦学习，从而实现对不同相机投影模型的泛化。
*   **关键技术细节**：
    1.  **双视图网络架构**：
        *   **射线模块 (Ray Module, RM)**：使用Transformer编码器和球谐函数展开，从单张图像回归出每个像素对应的单位三维光线方向 `di`。这相当于隐式地学习相机的内部校准。
        *   **跨视图模块 (Cross-view Module, CVM)**：采用孪生编码器-解码器结构，通过交叉注意力机制交换两视图间的信息。它回归出每个视图的径向距离 `ri`、置信度图 `σi`，以及两视图间的相对位姿 `P2→1`。
        *   **点图生成**：局部点图由 `Xi,i(u) = di(u) · ri(u)` 计算得到。
    2.  **训练目标**：
        *   采用多任务损失联合优化：非对称角度损失 (`LA`) 监督光线方向；局部回归损失 (`Lregr`) 监督点图；相对位姿损失 (`Lpose`) 监督旋转和平移。
    3.  **射线感知全局对齐 (Ray-Aware Global Alignment)**：
        *   用于多视图重建。首先对 exhaustive scene graph 进行两阶段剪枝（对称位姿一致性 + 几何重叠验证），过滤不可靠边。
        *   然后在一个纯粹的“射线一致”三维空间中进行优化。固定由RM预测的光线方向，仅优化相机位姿 `{Pi}`、每图像尺度因子 `{si}`和深度值，使不同视角下沿同一光线的三维点对齐。这避免了传统基于针孔假设的捆绑调整在畸变图像上的失败。

## 3. 实验设计
*   **数据集**：使用了经过增强以包含多种相机模态的数据集混合物进行训练和评估。
    *   训练集：增强版的2D3DS（全景）、360Loc（全景）、ADT（鱼眼）、MegaDepth（针孔）。
    *   零样本测试集：增强版的CO3Dv2（混合针孔/鱼眼）。
*   **评测基准与指标**：
    *   **双视角相对位姿估计**：报告旋转精度 (RRA@15°) 和平移精度 (RTA@15°)。
    *   **多视角重建**：报告位姿精度 (RRA/RTA@30°)、平均精度 (mAA@30°) 和绝对轨迹误差 (ATE RMSE)。
*   **对比方法**：与当前最先进的3D基础模型进行全面对比，包括 DUSt3R, MASt3R, Pow3R, VGGT, π³。

## 4. 资源与算力
论文中提及了部分训练配置信息：
*   **硬件**：使用4块 NVIDIA H200 GPU进行训练。
*   **批量大小**：有效批量大小为32（每GPU批大小4，梯度累积步数2）。
*   **训练时长/轮数**：总训练周期为300-500个epoch。
*   （注：未明确提及单次实验的具体训练小时数或总计算成本。）

## 5. 实验数量与充分性
实验设计较为全面和客观：
1.  **主实验 (2组)**：
    *   双视角相对位姿估计实验（表1），在5个数据集上对比5个基线模型。
    *   多视角重建实验（表2&表3），在相同数据集上对比基线并评估全局对齐效果。
2.  **消融研究 (2组)**：
    *   **异构训练的影响**：比较仅用同构数据训练的 `CAM3R-homo`与完整版`CAM3R`，证明引入异构数据对泛化至关重要。
    *   **射线感知全局对齐的有效性**：比较使用DUSt3R的全局对齐方法与本文提出的射线感知方法，证明后者在异构数据集上性能显著提升（mAA下降近35%），并能有效抑制轨迹漂移。
3.  **架构演进分析 (补充材料)** ：通过三个阶段（直接微调、使用真值光线、最终解耦设计）的定性与定量对比，论证了最终架构设计的必要性。
4.  **广泛的定性展示**：提供了大量双视角、多视角重建结果图，以及零样本泛化到未见数据集(Matterport, FIORD)的可视化。

总体而言，实验覆盖了同构/异构相机组合、双视角/多视角任务、域内/零样本评估等多个维度，并与当前主流SOTA方法进行了公平对比，充分性较高。

##6.主要结论与发现
1.CAM³ R在多种相机模型(针孔鱼眼全景)的双视角和多视角³ D重建任务上均达到了最先进的性能尤其在具有挑战性的跨模态场景(如大视场和全景捕获)中取得了显著的性能提升
2显式解耦相机光线估计与场景几何学习是处理广角畸变图像的有效策略避免了传统方法因隐式绑定针孔假设而导致的性能崩溃
³射线感知全局对齐框架能够成功地将成对预测融合成全局一致的三维场景显著优于基于针孔假设的传统优化方法特别是在处理畸变图像时能有效减少轨迹漂移

##7优点
方法论亮点:
创新性地提出了显式解耦的架构设计将相机内部几何(光线)与场景外部几何(深度位姿)分离使模型能够泛化到不同的光学流形
提出了射线感知全局对齐将优化过程提升到³ D射线一致空间而非传统的图像像素空间从而绕过了针孔投影假设的限制适用于各种相机模型
采用了分阶段的课程学习策略先在同构数据上学习基础的多视图几何再引入异构数据以提升跨模态泛化能力

实验设计亮点:
构建了一个包含多种相机模型的综合性增强数据集用于训练和评估模拟了真实世界中的异构性
进行了详尽的消融实验和架构演进分析有力地支撑了方法论设计的合理性与必要性
不仅进行了定量比较还提供了丰富的定性可视化结果直观展示了模型在处理极端畸变时的优势

##8不足与局限
计算开销:当前架构使用了两个独立的Vision Transformer主干(分别用于RM和CVM)导致较高的内存占用和计算成本限制了实时应用的可能性可扩展性:多视图重建需要对所有可能的图像对进行O(N²次前向推理尽管有剪枝策略但对于大规模图像集合仍然代价高昂未来需要探索更高效的端到端多视图架构偏差风险:虽然通过数据增强引入了异构性但模型的泛化能力仍可能受到训练数据分布的制约例如对于超出训练所见范围的极端或新型镜头设计可能表现不佳应用限制:论文主要关注静态场景的重建对于动态场景或存在严重遮挡的情况未做探讨此外目前输出为稠密点云后续的表面网格生成需要额外步骤

完

好的，我们继续从“应用限制”部分之后开始补全。

## 8. 不足与局限
*   **计算开销**：当前架构使用了两个独立的Vision Transformer主干（分别用于RM和CVM），导致较高的内存占用和计算成本，限制了实时应用的可能性。
*   **可扩展性**：多视图重建需要对所有可能的图像对进行O(N²)次前向推理。尽管有剪枝策略，但对于大规模图像集合，其代价仍然高昂。未来需要探索更高效的端到端多视图架构。
*   **偏差风险**：虽然通过数据增强引入了异构性，但模型的泛化能力仍可能受到训练数据分布的制约。例如，对于超出训练所见范围的极端或新型镜头设计，其表现可能不佳。
*   **应用限制**：论文主要关注静态场景的重建，对于动态场景或存在严重遮挡的情况未做探讨。此外，目前输出为稠密点云，后续的表面网格生成需要额外步骤。

## 9. 总结与展望
CAM³R的核心贡献在于通过**显式解耦相机光线与场景几何**这一思想，打破了现有3D重建基础模型对针孔相机模型的隐性依赖。其提出的射线感知全局对齐框架，为解决广角、畸变图像下的多视图一致性问题提供了新思路。实验证明该方法在跨相机模态的任务中具有卓越的鲁棒性和泛化能力。

未来工作可围绕以下几个方向展开：
1.  **效率优化**：设计更轻量化的单主干网络架构或知识蒸馏方案，以降低计算成本。
2.  **扩展模态**：将框架扩展到事件相机、光场相机等更多成像模态。
3.  **动态与语义理解**：结合时序信息处理动态场景，并融入语义分割等任务以实现更高层次的场景理解。
4.  **端到端表面重建**：探索从解耦表示直接生成高质量表面网格的途径。

总而言之，CAM³R为推动构建真正通用、与相机无关的3D视觉基础模型迈出了关键一步。

（完）
