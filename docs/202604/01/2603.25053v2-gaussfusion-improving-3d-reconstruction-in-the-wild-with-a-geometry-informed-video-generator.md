---
title: "GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator"
title_zh: GaussFusion：利用几何信息引导的视频生成器改进野外三维重建
authors: "Liyuan Zhu, Manjunath Narayana, Michal Stary, Will Hutchcroft, Gordon Wetzstein, Iro Armeni"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25053v2"
tags: ["query:3d"]
score: 10.0
evidence: 通过几何感知生成改进3D高斯泼溅重建
tldr: 本文提出了GaussFusion方法，旨在改进野外场景下的3D高斯泼溅重建质量。针对现有方法因相机位姿误差、覆盖不全和噪声初始化导致的浮动物体、闪烁和模糊等常见伪影，本研究引入了一个新颖的几何信息引导的视频到视频生成器。该方法不局限于单一重建流程，能同时优化基于优化的和前馈式方法生成的3DGS渲染结果。核心是渲染一个编码了深度、法线、不透明度和协方差的Gaussian primitive视频缓冲区，并由生成器对其进行精炼以产生时序一致且无伪影的帧。此外还设计了一个模拟多种退化模式的伪影合成流程以确保鲁棒性和泛化性。最终，GaussFusion在新视角合成基准上达到了最先进的性能，其高效版本能以15 FPS实时运行并保持相近性能，为交互式3D应用提供了可能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25053v2/fig-001.webp\", \"caption\": \"Table 1. Rendering Refinement Performance on DL3DV and RE10K Datasets. We compare our method against state-of-the-art 3DGS refinement approaches. The joint training variant achieves the best overall fidelity and perceptual quality (highest PSNR/SSIM, lowest LPIPS/FID), while the distilled model attains comparable performance with significantly improved runtime efficiency, reaching 15 FPS.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2603.25053v2/fig-002.webp\", \"caption\": \"Figure 3. Qualitative Comparison on Novel-View Refinement. We compare GaussFusion with baseline methods on diverse scenes from DL3DV [33] and RE10K [74]. GaussFusion effectively removes rendering artifacts such as blur, floaters, ghosting, and texture distortions, producing sharper geometry, cleaner reconstruction than Splatfacto [63], GenFusion [59], DiFiX3D+ [57], and ExploreGS [27], and achieving visual fidelity closest to the ground truth. From top to bottom, our model demonstrates strong generalization across various refinement scenarios: (1) inpainting missing regions, (2) outpainting beyond the original view, (3–4) correcting blur, ghosting, and geometric errors, and (5–6) handling artifacts from feed-forward 3DGS models, including transparent Gaussian primitives and spiky geometry.\", \"page\": 7, \"index\": 2, \"width\": 1061, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2603.25053v2/fig-003.webp\", \"caption\": \"Figure 4. Qualitative Comparison on 3D Reconstruction. We show the novel-view renderings from the improved 3D reconstruction refined using enhanced views from different methods. GaussFusion consistently outperforms baseline methods.\", \"page\": 8, \"index\": 3, \"width\": 490, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2603.25053v2/fig-004.webp\", \"caption\": \"Figure 2. GaussFusion Video Generator Architecture. Our model refines video latents using geometry-aware conditioning derived from 3D Gaussian splatting (3DGS). A Gaussian primitive buffer—comprising color, depth, normals, and uncertainty—is first encoded via a VAE and projected by a 3D convolution into a compact latent zG . The noised video latents are processed by a flow transformer built upon DiT blocks, interleaved with Geometry Adapter (GA) blocks. Each GA block fuses geometry features through self-attention and integrates textual scene descriptions via cross-attention, producing a geometry-aware feature xg that modulates the video latent x. The model predicts the flow velocity vθ(xt, t), which is integrated to recover refined video frames decoded by the VAE.\", \"page\": 4, \"index\": 4, \"width\": 846, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2603.25053v2/fig-005.webp\", \"caption\": \"Figure I.3. Additional Qualitative Comparison on 3D Reconstruction on DL3DV [33] and RE10K [74]. GaussFusion showcases sharper and cleaner appearance and geometry compared to baseline methods.\", \"page\": 17, \"index\": 5, \"width\": 1061, \"height\": 879}]"
motivation: 旨在解决3D高斯泼溅在野外场景重建中常见的浮动物体、闪烁和模糊等伪影问题，这些问题源于相机位姿误差、覆盖不全和噪声几何初始化。
method: 提出了一种几何信息引导的视频到视频生成器，通过渲染包含深度、法线等信息的Gaussian primitive视频缓冲区，并对其进行优化以生成时序一致、无伪影的帧。
result: GaussFusion在多个新视角合成基准测试中取得了最先进的性能，其高效变体能以15 FPS实时运行且保持相近性能。
conclusion: 该方法通过几何信息引导的视频生成，显著提升了野外场景的3D高斯泼溅重建质量，并提供了实时交互的应用潜力。
---

## 摘要
我们提出了GaussFusion，一种通过几何信息引导的视频生成来改进野外三维高斯溅射（3DGS）重建的新方法。GaussFusion能够缓解常见的3DGS伪影，包括由相机姿态误差、覆盖不完整和噪声几何初始化引起的漂浮物、闪烁和模糊。与先前局限于单一重建流程的基于RGB的方法不同，我们的方法引入了一个几何信息引导的视频到视频生成器，该生成器能够在基于优化的和前馈方法中优化3DGS渲染结果。给定一个现有的重建结果，我们渲染一个编码深度、法线、不透明度和协方差的高斯基元视频缓冲区，生成器对该缓冲区进行细化以产生时间连贯、无伪影的帧。我们进一步引入了一个模拟多种退化模式的伪影合成流程，以确保鲁棒性和泛化能力。GaussFusion在新视角合成基准测试中取得了最先进的性能，其高效变体能够以15 FPS的实时速度运行并保持相似的性能，从而支持交互式三维应用。

## Abstract
We present GaussFusion, a novel approach for improving 3D Gaussian splatting (3DGS) reconstructions in the wild through geometry-informed video generation. GaussFusion mitigates common 3DGS artifacts, including floaters, flickering, and blur caused by camera pose errors, incomplete coverage, and noisy geometry initialization. Unlike prior RGB-based approaches limited to a single reconstruction pipeline, our method introduces a geometry-informed video-to-video generator that refines 3DGS renderings across both optimization-based and feed-forward methods. Given an existing reconstruction, we render a Gaussian primitive video buffer encoding depth, normals, opacity, and covariance, which the generator refines to produce temporally coherent, artifact-free frames. We further introduce an artifact synthesis pipeline that simulates diverse degradation patterns, ensuring robustness and generalization. GaussFusion achieves state-of-the-art performance on novel-view synthesis benchmarks, and an efficient variant runs in real time at 15 FPS while maintaining similar performance, enabling interactive 3D applications.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator》进行结构化、深入且客观的总结。

---

## GaussFusion 论文深度总结

### 1. 核心问题与整体含义
*   **研究背景**：三维高斯溅射（3DGS）已成为高质量三维重建和新视角合成的流行表示方法。然而，无论是基于优化的还是前馈式的3DGS方法，在稀疏视角、覆盖不全或远离训练视角的场景下，都会产生严重的视觉伪影，如**漂浮物、闪烁、模糊和几何错误**。
*   **现有局限**：
    *   现有的基于生成先验的修复方法（如Diffusion模型）大多仅依赖RGB图像作为条件输入，难以解决复杂的几何伪影。
    *   这些方法通常只针对特定重建流程（优化或前馈）进行训练，缺乏对不同来源3D数据的泛化能力。
*   **研究动机**：如何设计一个**单一、高质量的重建修复模型**，能够利用丰富的几何信息来有效去除各类伪影，并**泛化到不同的3DGS重建范式（优化与前馈）中**。

### 2. 方法论
*   **核心思想**：提出一个“视频到视频”生成框架，将低质量的3DGS渲染序列（包含多种几何信息）转换为高质量、时序一致的无伪影视频。其关键在于利用比RGB更丰富的几何线索来引导生成过程。
*   **关键技术细节**：
    1.  **高斯基元缓冲区（GP-Buffer）**：
        *   从初始的3DGS重建中渲染出一个多模态的视频缓冲区 `G = {C, A, D, N, U}`。
        *   包含颜色(C)、不透明度(A)、深度(D)、屏幕空间法线(N)和逆协方差(U)。其中逆协方差图编码了局部几何不确定性。
        *   GP-Buffer将离散的3D高斯属性转化为像素对齐的视频表示，为生成器提供了明确的几何和外观退化线索。
    2.  **几何信息引导的视频生成器**：
        *   **架构基础**：基于流匹配（Flow Matching）的视频生成模型Wan-2.1。
        *   **几何适配器（GA）模块**：这是核心创新。GP-Buffer被编码后通过一个并行的侧网络（GA块）处理。GA块通过自注意力融合几何特征，并通过交叉注意力结合文本描述，最终将生成的“几何感知特征”注入到主DiT骨干网络的每一层中，实现细粒度的条件调制。
        *   **训练目标**：使用流匹配损失函数（公式4），让模型学习从带噪声/退化的视频潜在表示恢复到干净表示的“速度场”。
    3.  **全面的伪影合成策略**：
        *   为了训练出具有强泛化能力的模型，论文构建了一个包含超过75K个配对视频的大规模数据集。
        *   模拟了多种真实退化模式：稀疏视图采样、不同的高斯初始化策略、优化不足的Splat模型以及前馈模型（如DepthSplat, MVSplat）的预测结果。这种混合数据暴露了模型跨范式的各种缺陷。
    4.  **高效微调策略（Few-step变体）**：
        *   采用两阶段蒸馏：首先使用分布匹配蒸馏将基础多步教师模型压缩为4步学生模型；然后冻结学生模型参数，仅微调新插入的GA模块参数。这使得高效变体能达到约15 FPS的实时推理速度。

### 3. 实验设计
*   **数据集与场景**：
    *   **主要评估数据集**: DL3DV (大规模室内外场景) 和 RE10K (360度场景)。
    *   **测试设置**: 从每个数据集的官方测试集中选取场景。仅使用5%的原视频帧作为训练视图来重建有缺陷的3DGS模型，剩余视图用于评估新视角合成质量。
*   **基准与对比方法**: 
    *   **对比对象**: Splatfacto (原始有缺陷的重建), GenFusion, Difix3D+, ExploreGS, MVSplat360 (专用于MVSplat的前馈修复)。
    *   **评估指标**: PSNR, SSIM (图像质量), LPIPS, FID (感知质量)，以及推理速度(FPS)。

### 4. 资源与算力
*   文中明确提到了训练资源：
    *   **硬件**: 8张 NVIDIA H200 GPU。
    *   **训练规模**: Batch size为8 (通过梯度累积实现有效batch size为32)，共训练100K步。
    *   **分辨率**: 训练视频分辨率为480×832像素，共81帧；在VAE潜在空间中为60×104×21帧。

### 5. 实验数量与充分性
*   **实验全面性较高且设计客观公平**:
    1.  **主实验**: 
        - **(表1)**：在DL3DV和RE10K上对比所有基线方法的新视角渲染精修性能。报告了完整版、单数据集版和高效版的全面结果。
        - **(表2)**：专门评估在两种不同前馈重建方法上的泛化性能。
        - **(表3)**：评估精修后的视频用于优化底层3DGS表示后的最终重建质量提升效果。
    2.  **消融实验 (关键且充分)**:
        - **(表4)**：系统性地验证了GP-Buffer中每个模态对性能提升的贡献。结果表明增加更多几何线索能持续改善效果。
        - **(表5)**：分别验证了“全面的伪影合成策略”和“GA模块”的有效性。证明了二者缺一不可且优于简单基线方案(如直接加和条件)。
    3.  **定性分析 (图1/2/4/I.x等)**: 
        提供了大量可视化对比图例展示了对缺失区域修复、模糊校正、漂浮物去除等不同任务的优越性。

### 6.主要结论与发现
- GaussFusion成功地将丰富的几何信息(GP-Buffer)引入到视频生成框架中(通过GA模块)，实现了对复杂三维重建伪影的有效去除和新视角合成质量的显著提升。- 
-该方法在多个基准测试上取得了最先进的性能表现(PSNR/SSIM/LPIPS/FID综合最优)。-
-通过精心设计的混合伪影合成数据进行训练,GaussFusion展现出强大的跨范式泛化能力:同一个模型既能处理优化式也能处理前馈式重建产生的缺陷。- 
-经过蒸馏的高效版本能以接近实时的速度运行(15 FPS),同时保持大部分性能优势为交互式应用铺平道路。

###7 .优点
-创新性地提出了GP-Buffer这一概念充分利用了三维高斯溅射本身可提供的丰富多模态信息超越了仅依赖RGB的传统思路。- 
-设计了轻量级但高效的Geometry Adapter模块实现了对主干生成模型的细粒度条件注入比简单的特征拼接或相加更有效。- 
-构建了一个大规模多样化的伪影合成数据集这是保证模型强大泛化能力的关键因素之一体现了对问题本质的深刻理解。- 
-兼顾了性能与效率不仅追求SOTA还提供了实用的实时变体考虑到了实际部署需求。

###8 .不足与局限
*(基于正文及补充材料中的Limitations部分)*

-对于包含快速运动或严重运动模糊的视频效果会下降因为VAE难以对这些情况编码出干净的潜在表示从而影响后续生成质量。- 
-受限于固定长度的上下文窗口(81帧)处理更长序列时需要采用滑动窗口策略这可能导致长时序一致性无法完全保证存在分段间不连贯的风险。- 
-虽然能泛化到不同范式但本质上仍是一个后处理的精修步骤而非端到端地更新三维高斯参数未来可探索在线动态更新的机制以实现更紧密的闭环优化。- 
-实验主要聚焦于静态场景的新视角合成未涉及动态场景的重建与精修这是未来可以扩展的方向。

---

**(完)**

好的，我将从您上次输出的结尾处继续，对论文的剩余部分进行补充总结。

---

### 9. 潜在影响与未来方向
*   **对领域的影响**：
    *   **方法论层面**：GaussFusion 成功地将“几何信息引导的视频生成”范式引入3D重建后处理领域，为处理复杂、野外的三维重建问题提供了一个通用且强大的新工具。其核心思想——利用多模态几何缓冲区（GP-Buffer）作为生成条件——可能启发后续研究，将更多类型的中间表示（如点云、体素、神经辐射场特征）纳入生成式修复框架。
    *   **应用层面**：该工作显著提升了在稀疏、非理想输入条件下获得高质量3D内容的可能性，这对于机器人导航、AR/VR内容创建、文化遗产数字化等实际应用具有重要价值。其高效版本（15 FPS）更是向实时交互式3D场景编辑和增强迈出了关键一步。
*   **未来研究方向**：
    1.  **动态场景扩展**：当前方法专注于静态场景。一个自然的延伸是将其应用于动态4D场景（如动态高斯溅射）的修复与增强，这需要处理更复杂的时空一致性问题。
    2.  **端到端优化集成**：如局限性所述，目前是分离的后处理步骤。未来的工作可以探索将生成模型的梯度或先验以某种形式反向传播回3DGS参数空间，实现端到端的联合优化或迭代精化。
    3.  **更长序列与全局一致性**：改进模型架构或推理策略（如引入循环机制、全局记忆模块），以超越固定长度窗口的限制，确保超长视频序列的全局时空一致性。
    4.  **更鲁棒的编码器**：针对快速运动和严重模糊等挑战性情况，开发或集成更强大的视频编码器（VAE），以提取更清晰、信息量更大的潜在表示。

### 10. 总结评价
《GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator》是一篇高质量、工程扎实且具有创新性的研究工作。

*   **贡献明确且显著**：论文清晰地定义并解决了现有基于生成的3D修复方法的两大核心局限——缺乏几何引导和泛化能力不足。通过提出GP-Buffer和Geometry Adapter模块，以及构建大规模的混合伪影数据集，有效地应对了这些挑战。
*   **实验验证充分可信**：实验设计全面且具有说服力。不仅在主基准测试上展示了SOTA性能，还通过详尽的消融实验验证了每个核心组件的必要性。跨范式泛化实验和最终重建质量提升实验进一步巩固了其方法的实用价值。
*   **工程实现考虑周全**：从大规模数据合成、模型训练到高效的蒸馏变体开发，整个技术链条完整且考虑到了实际部署的效率需求，体现了很强的工程思维。
*   **写作与呈现清晰**：论文结构清晰，图表丰富直观，能够很好地辅助读者理解复杂的方法和结果。

总体而言，该工作为“在野”三维重建的质量提升提供了一个强大而通用的解决方案，其提出的“几何引导视频生成”框架很可能成为该子领域的一个重要基准和灵感来源。

---

（完）
