---
title: "AdaSFormer: Adaptive Serialized Transformers for Monocular Semantic Scene Completion from Indoor Environments"
title_zh: AdaSFormer：面向室内环境的自适应序列化Transformer用于单目语义场景补全
authors: "Xuzhi Wang, Xinran Wu, Song Wang, Lingdong Kong, Ziping Zhao"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25494v1"
tags: ["query:3d"]
score: 9.0
evidence: 单目语义场景补全与室内空间布局建模
tldr: 针对室内单目语义场景补全任务中空间复杂、遮挡严重以及Transformer模型内存成本高、细节重建难的问题，本文提出了AdaSFormer框架。该框架包含自适应序列化Transformer、中心相对位置编码和卷积调制层归一化三个关键设计，以动态调整感受野、捕获丰富空间信息并桥接异构特征表示。在公开数据集上的实验验证了其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25494v1/fig-001.webp\", \"caption\": \"Table 2. Comparisons among state-of-the-art MSSC methods on the Occ-ScanNet dataset [91]. The symbol † denotes the results\", \"page\": 7, \"index\": 1, \"width\": 1030, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2603.25494v1/fig-002.webp\", \"caption\": \"Figure 5. Qualitative Comparisons of semantic scene completion results on the NYUv2 testset [63] with different methods. Figures from left to right: (a) The single RGB input; (b) The ground truth; (c) ISO [91]; (d) Baseline; and (e) Our proposed method.\", \"page\": 8, \"index\": 2, \"width\": 1036, \"height\": 495}, {\"url\": \"assets/figures/arxiv/2603.25494v1/fig-003.webp\", \"caption\": \"Figure 2. AdaSFormer Framework. (a) Overall network architecture: It mainly consists of a 2D encoder, a depth estimation network, a 2D-to-3D projection module, and a 3D network. (b) Adaptive Serialized Transformer (ASF): This is the core of our method. A set of ASF blocks forms the encoder of the 3D network. The 3D voxels are adaptively converted into 1D patches, which are then augmented with center-relative positional encoding. These patches are processed through standard self-attention and feed-forward layers. Through the proposed Convolution-Modulated Layer Normalization (CMLN), the heterogeneous features from the transformer and convolutional layers are bridged. The resulting features are then fed into a DDR block for scene completion. (c) Convolution-Modulated Layer Normalization (CMLN): Modulates features to integrate transformer and convolutional representations effectively. (d) Center-Relative Positional Encoding: Enhances the richness of input information by modeling the relative spatial relationships of patches.\", \"page\": 4, \"index\": 3, \"width\": 920, \"height\": 372}, {\"url\": \"assets/figures/arxiv/2603.25494v1/fig-004.webp\", \"caption\": \"Figure 3. (a) illustrates the Z-order serialization of a 2D image, as well as that of a sparse 2D image, where the letters “C” and “V” in the figure indicate the locations of objects in the spatial domain. (b)-(e) illustration of Z-order serialization with shifts of 0, 3, 6, and 9 (from left to right). Different shift settings significantly affect the coverage and overlap between adjacent patches, especially for large patch sizes.\", \"page\": 5, \"index\": 4, \"width\": 1034, \"height\": 217}]"
motivation: 室内单目语义场景补全因空间布局复杂和遮挡严重而极具挑战，现有Transformer方法存在内存成本高和细节重建困难的问题。
method: 该方法提出了三个核心设计：自适应序列化Transformer、中心相对位置编码和卷积调制层归一化。
result: 在NYUv2和Occ-ScanNet数据集上的大量实验表明，AdaSFormer实现了最先进的性能。
conclusion: AdaSFormer通过其自适应序列化Transformer等创新设计，有效解决了室内单目语义场景补全的挑战，并取得了最先进的性能。
---

## 摘要
室内单目语义场景补全（MSSC）由于复杂的空间布局和严重的遮挡，比其室外对应任务更具挑战性。尽管Transformer非常适合建模全局依赖关系，但其高内存成本以及在重建细粒度细节方面的困难限制了其在室内MSSC中的应用。为了解决这些限制，我们提出了AdaSFormer，一个专为室内MSSC设计的序列化Transformer框架。我们的模型具有三个关键设计：（1）一个具有可学习偏移的自适应序列化Transformer，能够动态调整感受野；（2）一种捕捉丰富空间信息的中心相对位置编码；（3）一个连接卷积特征与Transformer特征之间异构表示的卷积调制层归一化。在NYUv2和Occ-ScanNet上的大量实验表明，AdaSFormer实现了最先进的性能。代码公开于：https://github.com/alanWXZ/AdaSFormer。

## Abstract
Indoor monocular semantic scene completion (MSSC) is notably more challenging than its outdoor counterpart due to complex spatial layouts and severe occlusions. While transformers are well suited for modeling global dependencies, their high memory cost and difficulty in reconstructing fine-grained details have limited their use in indoor MSSC. To address these limitations, we introduce AdaSFormer, a serialized transformer framework tailored for indoor MSSC. Our model features three key designs: (1) an Adaptive Serialized Transformer with learnable shifts that dynamically adjust receptive fields; (2) a Center-Relative Positional Encoding that captures spatial information richness; and (3) a Convolution-Modulated Layer Normalization that bridges heterogeneous representations between convolutional and transformer features. Extensive experiments on NYUv2 and Occ-ScanNet demonstrate that AdaSFormer achieves state-of-the-art performance. The code is publicly available at: https://github.com/alanWXZ/AdaSFormer.

---

## 论文详细总结（自动生成）

# AdaSFormer 论文总结

## 1. 核心问题与整体含义
*   **研究背景**：单目语义场景补全（MSSC）旨在从单张RGB图像中预测整个3D场景的体素占用和语义标签，对于机器人导航、3D重建等应用至关重要。室内场景因其**复杂的空间布局**（多层级结构、重叠的家具）和**严重的遮挡**，比室外场景更具挑战性。
*   **现有方法局限**：
    *   **基于卷积神经网络（CNN）的方法**：感受野有限，难以建模长距离依赖关系，扩大卷积核会导致立方级计算成本剧增。
    *   **直接应用Transformer到3D体素的方法**：计算和内存成本过高，且难以重建细粒度细节。
*   **研究动机与目标**：设计一个能够有效建模全局上下文、同时保持计算效率的框架，以解决室内MSSC任务中的复杂空间推理问题。

## 2. 方法论
论文提出了 **AdaSFormer (Adaptive Serialized Transformer)** 框架，其核心是一个结合了卷积和Transformer优势的混合架构。

*   **整体流程**：
    1.  **2D特征提取与深度估计**：使用EfficientNet提取2D特征，并利用现成方法估计深度图。
    2.  **2D到3D投影**：结合相机参数和深度图，将2D特征投影到3D空间。
    3.  **3D编码器（AdaSFormer核心）**：由一系列AdaSFormer块组成，交替使用Transformer模块捕获全局上下文和使用卷积模块进行局部特征传播与细化。
    4.  **轻量级解码器**：集成多尺度特征以生成最终的语义场景补全结果。

*   **三个关键技术设计**：
    1.  **自适应序列化注意力 (Adaptive Serialized Attention, ASA)**：
        *   **核心思想**：将稀疏的3D体素通过空间填充曲线（如Z曲线）序列化为1D序列。为了克服固定序列化起点导致感受野受限的问题，引入了可学习的偏移量（shift）。
        *   **实现细节**：使用带温度退火策略的直通式Gumbel-Softmax来端到端地学习离散的偏移量选择。这使得模型能动态调整每个序列化分组的起始点，从而获得更灵活、更有效的空间表示。
    2.  **中心相对位置编码 (Center-Relative Positional Encoding, CRPE)**：
        *   **核心思想**：考虑到CNN已能有效捕获局部位置信息，本文设计了一种编码“信息丰富度”的位置编码。它计算每个体素相对于场景中心点的偏航角和俯仰角差异。
        *   **实现细节**：场景中心定义为所有被占用体素的平均坐标。计算出的角度差经过MLP处理后作为偏置项加到序列化注意力中，帮助模型感知不同区域与场景中心的相对空间关系。
    3.  **卷积调制层归一化 (Convolution-Modulated Layer Normalization, CMLN)**：
        *   **核心思想**：为了解决Transformer和CNN提取的特征异构性带来的融合困难问题，提出了一种自适应的层归一化方式。
        *   **实现细节**：归一化的缩放因子γ和偏置项β不再是固定参数，而是由输入体素特征通过一个小型MLP动态生成。这使得网络能够根据输入上下文自适应地调制特征统计量，更好地桥接两种架构的特征表示。

## 3. 实验设计
*   **数据集与Benchmark**：
    *   在广泛使用的室内数据集上进行评估。
    *   `NYUv2`：包含1449张深度图像（795训练/654测试），体素网格为60x36x60。
    *   `Occ-ScanNet`：大规模室内数据集（45,755训练/19,764验证帧），体素网格为60x60x36。
    *   两个数据集均标注了相同的12个语义类别（包括空类别）。
*   **评估指标**：
    *   语义场景补全 (**SSC-mIoU**) ：所有类别的平均交并比。
    *   场景补全 (**SC-IoU**) ：仅区分占用与非占用的交并比。
*   **对比方法**: 
    *   与当前最先进的室内MSSC方法进行了全面比较，包括 `MonoScene`, `NDC-Scene`, `ISO`, `MonoMRN`, `GenFuSE`, `TPVFormer`, `GaussianFormer`, `SurroundOCC`等。

## 4. 资源与算力
论文中明确提到：“All experiments are conducted on an NVIDIA A100 GPU.” （所有实验均在NVIDIA A100 GPU上进行）。但未具体说明GPU数量、总训练时长或总计算开销。

## 5. 实验数量与充分性
实验设计较为充分和客观：
1.  **主实验 (定量对比)** ：在两个主流数据集 (`NYUv2`、`Occ-ScanNet`)上与多个SOTA方法进行对比。结果以表格形式清晰呈现了各项指标及排名（最佳和第二佳高亮显示）。
2.  **消融实验 (Ablation Study)** ：系统地验证了三个核心组件的有效性（ASA, CRPE, CMLN），展示了每个模块带来的性能增益。此外还针对关键设计选择进行了深入分析：
    *   ASA的不同实现方式 (`VanillaShift`, `GumbelShift`, `AnnealedShift`) 。
    *   CRPE的不同组成部分 (`PVM` vs.物理中心；有无`RYP`) 。
3.  **效率分析**: 
    *   提供了关键组件的延迟分析表 (`Latency study`) ，并与基线方法(`ISO`)对比，证明了所提方法的轻量高效性。
4.  **定性评估**: 
    *   提供了可视化结果图 (`Visual Results`) ，直观展示了模型在复杂遮挡和结构恢复上的优势。

总体而言，实验覆盖了性能验证、组件分析、效率评估和可视化等多个方面，论证链条完整且具有说服力。

## 6.主要结论与发现
1. AdaSFormer在NYUv2和Occ-ScanNet数据集上均取得了最先进的性能。例如在Occ-ScanNet上达到45.33 SSC-mIoU，显著超越之前的最佳方法ISO (31.25 mIoU)。
2. ASA通过可学习的偏移机制实现了自适应感受野调整；CRPE有效增强了模型对空间信息丰富度的感知；CMLN成功桥接了异构特征表示——这三个组件均被证明对性能提升至关重要且互补。
3. AdaSFormer在保持高性能的同时显著降低了推理延迟 (178ms vs ISO的535ms)，证明了其高效性。

##7 .优点
1.**创新性地融合了序列化Transformer的优势来解决室内MSSC的内存瓶颈问题**
提出了自适应序列化的概念使模型能够动态调整感受野范围以适应复杂的室内布局

2.**系统性的混合架构设计**
不仅引入了Transformer来捕获全局依赖还通过精心设计的CMLN模块解决了CNN-Transformer特征异构性问题实现了两者的优势互补

3.**全面的实验验证**
在两个主流数据集上进行了充分的定量比较详细的消融研究和效率分析为方法的有效性提供了强有力的证据

4.**开源代码**
论文公开了代码便于复现和后续研究

##8 .不足与局限
1.**泛化能力未充分验证**
仅在两个室内数据集上进行测试未在更多样化的真实世界场景或跨领域数据上评估其泛化能力

2.**对深度估计质量的依赖**
整个流程依赖于一个离线的深度估计算法如果深度估计不准确可能会将误差传播至后续阶段影响最终补全质量

3.**复杂度分析的局限性**
虽然报告了延迟但缺乏对训练阶段的计算成本内存消耗以及在不同硬件上的可扩展性的详细分析

4.**潜在的应用限制**
该方法主要针对静态室内环境对于动态物体或室外大尺度街景的直接适用性可能需要进一步调整或验证

5.**未来工作方向暗示不足**
论文没有深入探讨如何将该框架扩展到视频输入时序信息或多视图输入等更具挑战性的设定中

(完)

好的，我们继续从上次中断处开始，补全总结。

## 8. 不足与局限
1.  **泛化能力未充分验证**：
    *   仅在两个室内数据集 (`NYUv2`, `Occ-ScanNet`) 上进行测试，未在更多样化的真实世界场景（如不同光照、布局风格）或跨领域数据（如室外街景）上评估其泛化能力。模型对室内场景特定模式的依赖程度尚不明确。

2.  **对深度估计质量的依赖**：
    *   整个流程依赖于一个离线的深度估计算法。如果深度估计不准确（例如在透明物体、镜面反射或弱纹理区域），可能会将误差传播至后续的2D-3D投影和3D编码阶段，从而影响最终补全质量。论文未深入探讨其对深度估计误差的鲁棒性。

3.  **复杂度分析的局限性**：
    *   虽然报告了推理延迟并证明了优于基线方法，但缺乏对训练阶段的计算成本、内存消耗以及在不同硬件（如边缘设备）上的可扩展性的详细分析。这对于实际部署是一个重要考量因素。

4.  **潜在的应用限制**：
    *   该方法主要针对静态室内环境进行设计。对于动态物体（如移动的人或宠物）或室外大尺度街景的直接适用性可能需要进一步的架构调整或验证。

5.  **未来工作方向暗示不足**：
    *   论文没有深入探讨如何将该框架扩展到更具挑战性的设定中，例如处理视频输入以利用时序信息、融合多视图输入以解决单视角固有的遮挡问题，或者探索更高效的序列化策略以处理更高分辨率的体素网格。

## 9. 总结与评价
AdaSFormer是一篇在单目语义场景补全领域贡献显著的论文。它敏锐地指出了室内场景的独特挑战以及现有方法在处理全局上下文与计算效率之间的权衡困境。其核心贡献在于提出了一个精巧的混合架构，通过**自适应序列化注意力 (ASA)**、**中心相对位置编码 (CRPE)** 和**卷积调制层归一化 (CMLN)**三个创新模块，有效地将Transformer的全局建模能力与CNN的局部归纳偏置相结合。

该工作的亮点在于其“自适应”思想贯穿始终：ASA使模型能动态调整序列化感受野；CRPE根据场景内容自适应地编码空间信息丰富度；CMLN则根据输入特征自适应地调制归一化参数。这种设计理念使得模型能够灵活应对室内场景的复杂性和多样性。

实验部分设计严谨、论证充分，不仅在标准Benchmark上取得了新的SOTA性能，还通过详尽的消融实验和效率分析验证了每个组件的必要性及整体框架的高效性。代码开源也极大地促进了研究的可复现性和后续发展。

当然，正如“不足”部分所述，其在泛化性、对上游模块的依赖性以及更广泛的应用场景探索方面仍有提升空间。总体而言，AdaSFormer为MSSC任务提供了一个强大且高效的解决方案，其融合全局与局部、序列与并行的设计思路对相关3D视觉任务也具有重要的启发意义。

（完）
