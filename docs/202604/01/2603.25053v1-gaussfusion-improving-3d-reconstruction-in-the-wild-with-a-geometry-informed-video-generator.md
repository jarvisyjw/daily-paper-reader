---
title: "GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator"
title_zh: GaussFusion：利用几何信息引导的视频生成器改进野外场景的3D重建
authors: "Liyuan Zhu, Manjunath Narayana, Michal Stary, Will Hutchcroft, Gordon Wetzstein, Iro Armeni"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25053v1"
tags: ["query:3d"]
score: 10.0
evidence: 通过视频生成改进3D高斯泼溅重建
tldr: 本文提出了GaussFusion方法，旨在改进野外场景下的3D高斯泼溅重建质量。针对现有方法因相机位姿误差、覆盖不全和噪声初始化导致的漂浮物、闪烁和模糊等伪影问题，该方法创新性地引入了一个几何信息引导的视频到视频生成器。它通过渲染并优化一个编码了深度、法线等几何信息的Gaussian原始视频缓冲区，来生成时序一致且无伪影的帧序列。此外，还设计了模拟多种退化模式的伪影合成流程以增强鲁棒性。该方法在优化式和前馈式两种重建流程上均有效，不仅在新视角合成基准上达到最优性能，其高效版本还能以21 FPS实时运行，支持交互式3D应用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25053v1/fig-001.webp\", \"caption\": \"Table 1. Rendering Refinement Performance on DL3DV and RE10K Datasets. We compare our method against state-of-the-art 3DGS refinement approaches. The joint training variant achieves the best overall fidelity and perceptual quality (highest PSNR/SSIM, lowest LPIPS/FID), while the distilled model attains comparable performance with significantly improved runtime efficiency, reaching 15 FPS.\", \"page\": 6, \"index\": 1, \"width\": 976, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2603.25053v1/fig-002.webp\", \"caption\": \"Figure 3. Qualitative Comparison on Novel-View Refinement. We compare GaussFusion with baseline methods on diverse scenes from DL3DV [33] and RE10K [74]. GaussFusion effectively removes rendering artifacts such as blur, floaters, ghosting, and texture distortions, producing sharper geometry, cleaner reconstruction than Splatfacto [63], GenFusion [59], DiFiX3D+ [57], and ExploreGS [27], and achieving visual fidelity closest to the ground truth. From top to bottom, our model demonstrates strong generalization across various refinement scenarios: (1) inpainting missing regions, (2) outpainting beyond the original view, (3–4) correcting blur, ghosting, and geometric errors, and (5–6) handling artifacts from feed-forward 3DGS models, including transparent Gaussian primitives and spiky geometry.\", \"page\": 7, \"index\": 2, \"width\": 1061, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2603.25053v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative Comparison on 3D Reconstruction. We show the novel-view renderings from the improved 3D reconstruction refined using enhanced views from different methods. GaussFusion consistently outperforms baseline methods.\", \"page\": 8, \"index\": 3, \"width\": 490, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2603.25053v1/fig-004.webp\", \"caption\": \"Figure 2. GaussFusion Video Generator Architecture. Our model refines video latents using geometry-aware conditioning derived from 3D Gaussian splatting (3DGS). A Gaussian primitive buffer—comprising color, depth, normals, and uncertainty—is first encoded via a VAE and projected by a 3D convolution into a compact latent zG . The noised video latents are processed by a flow transformer built upon DiT blocks, interleaved with Geometry Adapter (GA) blocks. Each GA block fuses geometry features through self-attention and integrates textual scene descriptions via cross-attention, producing a geometry-aware feature xg that modulates the video latent x. The model predicts the flow velocity vθ(xt, t), which is integrated to recover refined video frames decoded by the VAE.\", \"page\": 4, \"index\": 4, \"width\": 846, \"height\": 439}, {\"url\": \"assets/figures/arxiv/2603.25053v1/fig-005.webp\", \"caption\": \"Figure I.3. Additional Qualitative Comparison on 3D Reconstruction on DL3DV [33] and RE10K [74]. GaussFusion showcases sharper and cleaner appearance and geometry compared to baseline methods.\", \"page\": 17, \"index\": 5, \"width\": 1061, \"height\": 879}]"
motivation: 旨在解决野外场景中3D高斯泼溅重建常见的伪影问题，如漂浮物、闪烁和模糊，这些问题源于相机位姿误差、覆盖不完整和噪声几何初始化。
method: 该方法引入了一个几何信息引导的视频到视频生成器，利用渲染出的包含深度、法线等信息的Gaussian原始视频缓冲区，来生成时序一致、无伪影的帧。
result: GaussFusion在新视角合成基准测试中取得了最先进的性能，其高效变体能以21 FPS实时运行且保持相近性能。
conclusion: 该方法通过几何信息引导的视频生成，显著提升了野外场景的3D高斯泼溅重建质量，并实现了实时交互应用。
---

## 摘要
我们提出了GaussFusion，一种通过几何信息引导的视频生成来改进野外场景中3D高斯溅射（3DGS）重建的新方法。GaussFusion缓解了常见的3DGS伪影，包括由相机姿态误差、覆盖不完整和噪声几何初始化引起的漂浮物、闪烁和模糊。与先前局限于单一重建流程的基于RGB的方法不同，我们的方法引入了一个几何信息引导的视频到视频生成器，该生成器可同时优化基于优化的和前馈方法的3DGS渲染结果。给定一个现有重建，我们渲染一个编码深度、法线、不透明度和协方差的高斯基元视频缓冲区，生成器对该缓冲区进行细化以产生时间连贯、无伪影的帧。我们进一步引入了一个模拟多样化退化模式的伪影合成流程，以确保鲁棒性和泛化能力。GaussFusion在新视角合成基准测试中取得了最先进的性能，其高效变体能以21 FPS实时运行并保持相似性能，从而支持交互式3D应用。

## Abstract
We present GaussFusion, a novel approach for improving 3D Gaussian splatting (3DGS) reconstructions in the wild through geometry-informed video generation. GaussFusion mitigates common 3DGS artifacts, including floaters, flickering, and blur caused by camera pose errors, incomplete coverage, and noisy geometry initialization. Unlike prior RGB-based approaches limited to a single reconstruction pipeline, our method introduces a geometry-informed video-to-video generator that refines 3DGS renderings across both optimization-based and feed-forward methods. Given an existing reconstruction, we render a Gaussian primitive video buffer encoding depth, normals, opacity, and covariance, which the generator refines to produce temporally coherent, artifact-free frames. We further introduce an artifact synthesis pipeline that simulates diverse degradation patterns, ensuring robustness and generalization. GaussFusion achieves state-of-the-art performance on novel-view synthesis benchmarks, and an efficient variant runs in real time at 21 FPS while maintaining similar performance, enabling interactive 3D applications.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator》进行结构化、深入且客观的总结。

---

### **论文总结：GaussFusion**

#### **1. 核心问题与整体含义**
*   **研究动机**：3D高斯溅射（3DGS）已成为高质量3D重建和渲染的主流表示方法。然而，无论是基于优化的方法还是新兴的前馈式方法，在稀疏视角、覆盖不全或远离训练视角的场景下，其重建结果仍普遍存在**漂浮物、闪烁、模糊和几何错误**等视觉伪影。现有基于生成先验的改进方法大多仅依赖RGB图像作为条件，难以解决复杂的几何伪影，且通常只针对单一重建流程（优化式或前馈式）设计，缺乏通用性。
*   **整体含义**：本文旨在提出一个**通用、鲁棒且高效的后处理框架**，能够利用丰富的几何信息来修复来自不同来源（优化式/前馈式）的3DGS重建中的各类伪影，从而提升新视角合成的质量和三维表示的一致性。

#### **2. 方法论**
*   **核心思想**：将3DGS重建的改进问题转化为一个**几何信息引导的视频到视频生成任务**。核心是利用从原始3DGS中提取的、比RGB更丰富的多模态几何线索来指导生成模型，以修复时序连贯、无伪影的视频帧。
*   **关键技术细节**：
    1.  **高斯基元缓冲区（GP-Buffer）**：这是方法的基石。它不是一个简单的RGB视频，而是一个像素对齐的多模态表示，通过渲染获得包含以下信息的视频序列：
        *   `C` (颜色)、`A` (不透明度)、`D` (深度)
        *   `N` (屏幕空间法线)
        *   `U` (逆协方差图)：编码每个高斯溅射点的局部几何不确定性。
    2.  **几何信息引导的视频生成器架构**：
        *   以预训练的文本到视频模型Wan为骨干。
        *   引入**几何适配器（GA）模块**：将编码后的GP-Buffer特征通过自注意力、交叉注意力等方式注入到DiT骨干网络中，实现空间对齐的几何条件调制。
        *   采用流匹配（Flow Matching）框架进行训练和推理。
    3.  **全面的伪影合成策略**：为了训练一个通用的模型，作者构建了一个包含超过7.5万个配对视频的大规模数据集。退化模式模拟了：
        *   稀疏视图采样（随机保留5%帧）。
        *   多种初始化策略（SfM点云、随机点云、密集点图）。
        *   欠优化的3DGS模型。
        *   前馈式模型（如DepthSplat, MVSplat）的输出结果。
    4.  **高效微调策略**：采用两阶段蒸馏方案将多步生成器压缩为仅需4步的高效模型（Ours-Few-step），实现实时推理。

#### **3. 实验设计**
*   **数据集与场景**：
    *   **主要评估数据集**：DL3DV-10K（大规模室内外场景）、RE10K（真实世界场景）。
    *   **测试设置**：从完整视频中随机下采样5%的帧作为训练视图来重建有缺陷的3DGS模型，剩余视图用于评估新视角合成质量。
*   **基准与对比方法**：
    *   **基准任务1 - 渲染细化性能 (Novel-View Synthesis)**：在DL3DV和RE10K上评估纯渲染质量提升。对比了Splatfacto (基线)、GenFusion、Difix3D+和ExploreGS等最先进的细化方法。
    *   **基准任务2 - 前馈式视图合成改进 (Feed-forward View Synthesis)**：在RE10K上评估对前馈模型DepthSplat和MVSplat输出的改进能力。对比了Difix3D+、ExploreGS以及专门为MVSplat设计的MVSplat360。
    *   **基准任务3 - 三维重建改进 (Improving 3D Reconstruction)**：使用生成的干净帧重新优化底层三维高斯参数后评估整体三维场景质量。

#### **4. 资源与算力**
文中明确提到了训练配置：
*   **硬件**: 8张 NVIDIA H200 GPU。
*   **训练时长**: 100,000步迭代。
*   **批大小**: 每GPU批大小为1，梯度累积4步后有效批大小为32。
*   **分辨率**: 训练视频分辨率为480×832像素。

#### **5. 实验数量与充分性**
实验设计全面且具有说服力：
1.  **主实验充分性高**: 在两个主流大规模数据集上进行了全面的定量比较（PSNR, SSIM, LPIPS, FID），并提供了丰富的定性对比图例展示各种伪影修复效果（补全缺失区域、外推视野范围、去模糊/去鬼影等）。
2.  **消融实验系统性强**: 
    *   **(关键) GP-Buffer模态消融**:验证了逐步加入深度、法线等几何通道能持续提升性能。
    *   **(关键) GA模块消融**:证明了所提GA模块比简单融合或全参数微调更有效且收敛更快。
    *   **(关键)数据策略消融**:证明了混合多种退化模式的数据集对于跨范式泛化至关重要。
3.  **(加分项)跨范式泛化验证**:专门设计了针对前馈模型的测试集并进行了公平比较，有力地支撑了“通用”这一核心主张。

#### **6.主要结论与发现**
1. GaussFusion在多个新视角合成基准测试中取得了最先进的性能指标和视觉质量。
2. GP-Buffer提供的丰富几何信息是有效识别和修复复杂三维伪影的关键因素之一 。
3. GA模块能高效地将这些几何特征注入到视频生成过程中 ，优于简单的条件拼接方式 。
4. “混合多种退化模式”的训练数据策略是实现跨优化式和前馈式两种重建范式泛化的必要条件 。
5. GaussFusion不仅能改善单帧渲染 ，还能通过生成的干净帧反向优化底层三维高斯参数 ，从而提升整个三维场景的重建质量 。
6. GaussFusion的高效变体能在保持高性能的同时达到约21 FPS的速度 ，具备了实时交互应用的潜力 。

#### .优点
1.**创新性地利用多模态GP-Buffer作为条件信号**,超越了传统仅用RGB的方法 ，为解决复杂的几何不一致性问题提供了更明确的线索 。
2.**提出了一个真正通用的框架**,首次在一个模型中同时支持对优化式和前馈式两种主流重建流程的输出进行有效改进 。
3.**精心设计的综合伪影合成流程**,为训练鲁棒的通用模型提供了高质量 、多样化的数据基础 ，是泛化能力的重要保障 。
4.**兼顾了性能与效率**,不仅提出了高性能的全版本模型 ，还通过蒸馏技术获得了高效的实时版本 ，拓宽了应用场景 。

#### .不足与局限
1.**对极端运动的处理能力有限**:论文指出在快速运动或严重运动模糊的视频中 ，VAE编码可能失效导致生成质量下降 。这限制了其在动态剧烈场景中的应用 。
2.**长序列处理的局限性**:受限于固定的上下文窗口(81帧)，处理更长视频时需采用滑动窗口策略 。虽然能保持短期一致性但无法保证超长序列的整体连贯性和同步性 。这可能导致长视频中出现不自然的跳变或累积误差 。
(完)

好的，我将继续从“不足与局限”部分开始补全。

#### **7. 不足与局限**
1.  **对极端运动的处理能力有限**：论文指出在快速运动或严重运动模糊的视频中，VAE编码可能失效导致生成质量下降。这限制了其在动态剧烈场景中的应用。
2.  **长序列处理的局限性**：受限于固定的上下文窗口（81帧），处理更长视频时需采用滑动窗口策略。虽然能保持短期一致性但无法保证超长序列的整体连贯性和同步性。这可能导致长视频中出现不自然的跳变或累积误差。
3.  **对原始重建质量的依赖**：GaussFusion作为一个后处理框架，其修复能力存在上限。如果输入的3DGS重建质量极差（例如几何完全崩溃），GP-Buffer提供的几何线索本身已严重失真，则生成模型难以恢复出正确的场景内容。
4.  **计算开销与部署门槛**：尽管提出了高效的蒸馏版本，但核心模型（尤其是训练阶段）仍依赖于大规模预训练的视频扩散模型和大量GPU资源进行微调。这为复现和研究带来了较高的硬件门槛。
5.  **评估的潜在偏差**：主要定量评估基于新视角合成质量（PSNR, SSIM等），这些指标主要衡量渲染图像的保真度，但对三维几何一致性的评估相对间接（主要通过重新优化后的点云可视化）。缺乏对修复后三维高斯溅射本身几何属性（如位置、尺度、旋转）的定量分析。

#### **8. 未来方向**
基于上述局限，论文也暗示或可引申出以下未来研究方向：
1.  **动态场景建模**：将框架扩展到包含动态物体和复杂相机运动的场景中，可能需要结合光流估计或4D表示来增强时序建模能力。
2.  **端到端联合优化**：探索将生成式修复模块与3DGS重建过程进行更紧密的耦合或端到端训练的可能性，以打破后处理框架的性能上限。
3.  **更高效的架构设计**：研究更轻量级的条件注入机制或骨干网络，以进一步降低推理成本，促进在移动设备或边缘计算平台上的应用。
4.  **几何一致性的直接优化**：开发能够直接作用于三维高斯溅射参数空间（而非仅图像空间）的损失函数或正则化项，以更显式地保证和改进三维几何质量。

#### **9. 总结评价**
GaussFusion是一项扎实且富有洞察力的工作。它敏锐地指出了当前3DGS改进方法中“重外观、轻几何”的普遍问题，并创新性地提出了利用多模态几何缓冲区（GP-Buffer）来引导视频生成这一解决方案。该方法不仅在技术上有效——显著提升了多种来源3DGS重建的新视角合成质量——更重要的是其设计理念具有通用性，首次统一了对优化式和前馈式两种范式输出的改进流程。大规模合成数据集的构建和系统的消融实验为结论提供了强有力的支撑。

尽管在处理极端运动、长序列以及完全依赖后处理等方面存在固有局限，但这项工作为利用生成先验提升三维重建的鲁棒性开辟了一条清晰且富有前景的新路径。其提出的“几何信息引导”核心思想、通用化框架设计以及兼顾性能与效率的工程实践，对未来相关领域的研究具有重要的参考价值。

（完）
