---
title: "Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds"
title_zh: 迈向三维场景理解的基础模型：面向点云的实例感知自监督学习
authors: "Bin Yang, Mohamed Abdelsamad, Miao Zhang, Alexandru Paul Condurache"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25165v2"
tags: ["query:3d"]
score: 9.0
evidence: 用于3D场景理解和点云感知的自监督学习
tldr: 针对现有自监督学习方法在点云上获得的表示对实例定位任务迁移效果不佳的问题，本研究提出了一个面向实例的自监督框架PointINS。该框架通过几何感知学习来丰富点云表示，采用正交偏移分支联合学习高级语义理解和几何推理以获得实例感知能力。其核心贡献在于识别了两个对鲁棒实例定位至关重要的属性，并将其制定为互补的正则化策略：偏移分布正则化和空间聚类正则化。该方法显著提升了室内外场景理解任务的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25165v2/fig-001.webp\", \"caption\": \"Figure 1. We achieve superior results over state-of-the-art selfsupervised approaches (DOS [2] and Sonata [36]) on both indoor instance (left) and outdoor panoptic segmentation (right).\", \"page\": 1, \"index\": 1, \"width\": 485, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-002.webp\", \"caption\": \"Table 1. Comparison of self-supervised methods on various indoor scene datasets: ScanNet [12], ScanNet200 [30] and S3DIS [21]. For fair comparison, all models deploy the same backbone of Point Transformer v3 (PTv3) [35] and no additional data is used during pre-training. The best and second best results in each setting are highlighted in bold and underline, respectively.\", \"page\": 6, \"index\": 2, \"width\": 872, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-003.webp\", \"caption\": \"Table 2. Comparison of self-supervised methods on the outdoor scene datasets: nuScenes [14] and SemanticKITTI [4].\", \"page\": 6, \"index\": 3, \"width\": 489, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-004.webp\", \"caption\": \"Figure 6. Visualization of predicted centroids ci = xi+Oi (no explicit regularization) from models pre-trained under four configurations. (a) Without regularization, the centroids are scattered irregularly across the scenes. (b) With ODR only, the centroids are aligned more closely with the scene structure but they are not grouped (lack of local coherence). (c) With SCR only, the centroids are partially grouped but spatially scattered. (d) With ODR and SCR, the centroids become tightly concentrated around instances.\", \"page\": 7, \"index\": 4, \"width\": 1006, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-005.webp\", \"caption\": \"Figure 8. Qualitative results of unsupervised instance segmentation. Left: ground-truth instance labels. Right: predictions from PointINS obtained directly from offset clustering without any downstream supervision.\", \"page\": 14, \"index\": 5, \"width\": 496, \"height\": 896}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-006.webp\", \"caption\": \"Figure 5. We monitor the linear probing (LP) performance of a model pre-trained with a recent SSL method [36]. Remarkably, the model reaches 85% of its final LP performance within just 10% of the total training.\", \"page\": 4, \"index\": 6, \"width\": 443, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-007.webp\", \"caption\": \"Figure 3. Overview of PointINS: A point cloud is augmented to two independent views and they are randomly masked. The teacher processes the full input and the student receives only visible points. Both networks share the same architecture. In the semantic branch, features are computed similarity with prototypes Q. A KL-divergence loss Lsem is then applied for distillation. In the offset branch, an offset head maps features into 3D offset vectors. Teacher offsets are first regularized by ODR to align with empirically observed geometric priors. Next, segments obtained from K-means-clustering are used to extract pseudo-instance masks. Those masks help to enhance instance awareness by regularizing local coherence of points. Finally, an offset loss Loff is computed as the second distillation.\", \"page\": 4, \"index\": 7, \"width\": 1031, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.25165v2/fig-008.webp\", \"caption\": \"Table 9. Pretraining settings for indoor and outdoor point clouds.\", \"page\": 12, \"index\": 8, \"width\": 489, \"height\": 375}]"
motivation: 现有自监督学习方法在点云上的表示对实例定位任务迁移效果不佳，而实例感知是3D感知的基础组件，因此需要弥补这一差距以推进真正的3D基础模型。
method: 提出PointINS框架，通过正交偏移分支联合学习高级语义理解和几何推理，并引入偏移分布正则化和空间聚类正则化两种互补策略。
result: "在五个数据集上的广泛实验表明，PointINS在室内实例分割上平均mAP提升+3.5%，在室外全景分割上平均PQ增益+4.1%。"
conclusion: PointINS通过几何感知学习，为构建支持所有下游任务的可扩展3D基础模型铺平了道路。
---

## 摘要
点云自监督学习的最新进展显著提升了无需人工标注的三维场景理解能力。现有方法通过增强视图间的特征一致性或掩码场景建模来强调语义感知。然而，由此获得的表征在迁移到实例定位任务时效果不佳，且通常需要完整的微调才能获得强劲性能。实例感知是三维感知的基本组成部分，因此弥合这一差距对于推进能够支持所有三维数据下游任务的真正三维基础模型至关重要。本文提出PointINS，一个面向实例的自监督框架，通过几何感知学习来丰富点云表征。PointINS采用正交偏移分支联合学习高层语义理解和几何推理，从而获得实例感知能力。我们识别出对鲁棒实例定位至关重要的两个一致性属性，并将其构建为互补的正则化策略：偏移分布正则化（ODR）将预测偏移与经验观察到的几何先验对齐；空间聚类正则化（SCR）通过使用伪实例掩码对偏移进行正则化来增强局部一致性。通过在五个数据集上的大量实验，PointINS在室内实例分割任务上平均提升3.5% mAP，在室外全景分割任务上平均提升4.1% PQ，为可扩展的三维基础模型铺平了道路。

## Abstract
Recent advances in self-supervised learning (SSL) for point clouds have substantially improved 3D scene understanding without human annotations. Existing approaches emphasize semantic awareness by enforcing feature consistency across augmented views or by masked scene modeling. However, the resulting representations transfer poorly to instance localization, and often require full finetuning for strong performance. Instance awareness is a fundamental component of 3D perception, thus bridging this gap is crucial for progressing toward true 3D foundation models that support all downstream tasks on 3D data. In this work, we introduce PointINS, an instance-oriented self-supervised framework that enriches point cloud representations through geometry-aware learning. PointINS employs an orthogonal offset branch to jointly learn high-level semantic understanding and geometric reasoning, yielding instance awareness. We identify two consistent properties essential for robust instance localization and formulate them as complementary regularization strategies, Offset Distribution Regularization (ODR), which aligns predicted offsets with empirically observed geometric priors, and Spatial Clustering Regularization (SCR), which enforces local coherence by regularizing offsets with pseudo-instance masks. Through extensive experiments across five datasets, PointINS achieves on average +3.5% mAP improvement for indoor instance segmentation and +4.1% PQ gain for outdoor panoptic segmentation, paving the way for scalable 3D foundation models.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将为您提供对这篇论文的深入、客观的结构化总结。

---

### **论文总结：Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds**

#### **1. 核心问题与整体含义**
*   **研究动机**：当前点云自监督学习（SSL）方法（如对比学习、掩码建模）主要关注**语义一致性**，学到的特征在语义分割任务上表现良好，但在需要精细几何推理的**实例级任务**（如实例分割、全景分割）上迁移效果不佳。这阻碍了构建能够统一支持所有下游任务的“3D基础模型”的进程。
*   **核心观点**：真正的3D场景理解需要同时具备**语义不变性**和**实例感知能力**。后者依赖于对点之间几何关系（例如，朝向物体中心）的高层推理，而非低级的几何线索。
*   **整体目标**：提出一个自监督学习框架，在不使用任何人工标注的情况下，联合学习点云的语义表征和实例感知的几何表征。

#### **2. 方法论：PointINS框架**
*   **核心思想**：在经典的教师-学生自蒸馏SSL框架基础上，引入一个并行的**偏移分支**来预测每个点指向其所属实例中心的3D偏移向量。为了在没有真实标签的情况下有效学习这个分支，提出了两种互补的正则化策略。
*   **关键技术细节与流程**：
    1.  **基础架构**: 采用教师-学生蒸馏范式。输入点云生成两个增强视图并随机掩码部分点给学生网络，完整视图给教师网络。语义分支通过原型聚类进行自蒸馏。
    2.  **偏移分支**: 一个轻量级的MLP头，从骨干网络特征预测每个点的3D偏移量 `oi`。
    3.  **偏移分布正则化 (ODR)**:
        *   **问题**: 无监督回归偏移容易崩溃到平凡解。
        *   **解决方案**: 利用真实场景中观测到的几何先验（偏移大小服从长尾分布、方向在球面上均匀分布）来约束预测。
        *   **技术**: 使用概率积分变换（PIT），将预测偏移的大小和每个坐标轴方向的分量映射到目标先验分布上，同时保持其原始排序关系。这提供了全局的、基于统计的稳定性。
    4.  **空间聚类正则化 (SCR)**:
        *   **问题**: ODR缺乏局部一致性约束（同一实例内的点应指向共同中心）。
        *   **解决方案**: 利用教师网络已具备的强语义特征生成伪实例掩码来提供局部监督信号。
        *   **流程**: 
            a) 对教师特征进行K-means聚类得到粗粒度语义段。
            b) 在每个段内，根据ODR正则化后的预测中心构建k-NN图并进行广度优先搜索（BFS），将空间不连通的区域划分为不同的伪实例组件。
            c) 计算每个伪实例组件的几何中心 `c̄`，并以此为目标重新计算目标偏移 `O*i = c̄ - xi`。
    5.  **损失函数与训练**: 
        *   总损失为语义蒸馏损失 `L_sem` 和偏移蒸馏损失 `L_off`的加权和 (`L = L_sem + λ_off L_off`)。
        *   偏移损失包含L1范数项（约束大小）和余弦相似度项（约束方向）。
        *   关键设计是ODR和SCR仅应用于教师网络的输出端，为学生提供一个稳定、结构化的监督信号，而不直接干扰学生骨干网络的梯度更新。

#### **3. 实验设计**
*   **评估任务与协议**: 
    *   **任务**: 室内/室外场景下的语义分割、实例分割、全景分割、物体检测。
    *   **协议**: “线性探测”（冻结骨干）、 “解码器探测”（训练标准解码器）、 “全微调”。
*   **数据集 (5个)**:
    *   **室内**: ScanNet, ScanNet200, S3DIS (Area-5)。
    *   **室外**: nuScenes, SemanticKITTI, Waymo Open Dataset (用于多数据集预训练)。
*   **基准模型与对比方法**:
    *   ***SOTA SSL方法***: DOS, Sonata, SegContrast, PSA, NOMAE等。
    *   ***有监督基线***: Point Transformer v3 (PTv3)。
    *   所有对比均使用相同的PTv3骨干网络以确保公平性。

#### **4. 资源与算力**
论文在补充材料中提供了相关信息：
*   ***硬件***: NVIDIA H200 GPU。
*   ***单数据集预训练***: 使用1块H200 GPU。PointINS相比基线DOS增加了约25%的训练时间（例如ScanNet从20小时增至25小时）。
*   ***多数据集预训练***: 使用2块H200 GPU以分布式数据并行模式运行。

#### **5. 实验数量与充分性**
实验非常充分且设计严谨：
1.  ***主实验***: 
    - Tabl1 & Tabl2展示了在5个主流数据集上、针对多个任务、在三种评估协议下与多个SOTA方法的全面对比。PointINS在几乎所有实例/全景分割指标上取得显著领先优势 (+2.5–4.8%) ，同时保持了顶级的语义分割性能。
2. ***消融研究***:
    - Tabl4 & Fig6系统地验证了ODR和SCR各自及组合的有效性及其互补性。可视化结果直观展示了正则化策略如何使预测中心从散乱变得聚集于实例周围。
    - Tabl8研究了正则化应用顺序的影响（先ODR后SCR最优），以及为何将正则化置于教师侧更优的理论解释（避免梯度冲突）。
3. ***敏感性分析***:
    - Tabl5 & Fig7深入分析了ODR对先验分布的鲁棒性（即使使用不同场景或无监督聚类得到的先验也有效）、关键超参数λ_off和K的选择影响以及预热策略的重要性(Tabl10)。
4. ***泛化性与扩展实验***:
    - Tabl6验证了PointINS可无缝集成到其他SSL框架(PSA, Sonata)中并带来一致提升。
    - Tabl7展示了其在极低标注比例(0.1%,1%)下的卓越数据效率。
    - Tabl11 & Tab12证明了其表征可迁移到物体检测任务及跨数据集设置中。

#### ****6.**主要结论与发现**
- PointINS是首个明确为点云设计的同时学习语义一致性和实例感知几何推理的自监督框架。
- ODR和SCR两种互补的正则化策略对于在没有真实标签的情况下稳定且有效地学习几何偏移至关重要。前者提供全局统计约束防止崩溃；后者提供局部结构约束促进分组一致性；二者结合效果最佳且相互增强(Fig6)。
- PointINS学到的表征在下游任务上表现出色：
    1)显著提升了室内外场景下的实例/全景分割性能；
    2)保持了强大的语义分割能力；
    3)具有良好的数据效率和跨任务/跨数据集的泛化能力；
    4)甚至在无任何下游微调的情况下也能产生有意义的无监督实例分割结果(Tabl13)。

#### ****7.**优点**
- ***问题定义清晰且有价值***：精准地指出了当前SSL方法在“实例感知”上的短板，并将其定位为迈向“3D基础模型”的关键一步。
- ***方法新颖且优雅***：将复杂的无监督实例感知问题巧妙地转化为一个受双重正则化的自蒸馏问题。ODR利用统计先验的想法非常巧妙；SCR利用模型自身特征生成伪标签的做法高效且合理(“Bootstrapping”) 。
- ***实验极其全面扎实***：覆盖了室内外主流数据集、多种下游任务、多种评估协议以及大量的消融分析和敏感性测试。结论可信度高。

#### ****8.**不足与局限**
- ***性能差距依然存在***：尽管在线性探测设置下取得了巨大进步，但与完全有监督的性能相比仍有明显差距(见Tabl1)。这表明纯粹的自监督学习要达到全监督的性能水平仍需努力或更多数据/算力投入。(作者也承认此为未来工作方向)
- ***对先验分布的潜在依赖***：虽然论文证明ODR对先验选择具有鲁棒性(Tabl5)，但该方法仍隐含地假设了场景中对象尺度和分布的某种规律性。对于具有极端或前所未见物体尺度的全新领域(如微观或天文尺度)，其有效性可能需要重新验证或调整先验分布参数(`a`, `θ`, `μ`, `σ`) 。
- ***计算开销增加***：虽然增加的25%训练时间可以接受，但额外的分支和复杂的后处理步骤(K-means+BFS)确实带来了额外的计算成本和时间开销。

---

**(完)**

好的，我将继续为您提供完整的总结。

---

#### **9. 潜在影响与未来方向**
*   **对领域的影响**：
    1.  **迈向3D基础模型**：该工作为构建能够统一处理多种3D场景理解任务的“基础模型”提供了关键思路。它表明，通过精心设计的自监督目标，可以同时学习语义和实例级表征，这是实现通用3D智能的重要一步。
    2.  **重新审视SSL目标**：论文挑战了“语义一致性是唯一目标”的普遍观点，强调了高层几何推理（如实例中心预测）在自监督学习中的重要性。这可能会启发后续研究探索更多样化的、面向特定下游任务需求的预训练任务。
    3.  **无监督实例分割的新范式**：PointINS框架本身即可产生有意义的无监督实例分割结果（Tabl13），为完全无标注的3D场景解析开辟了一条新路径。
*   **未来研究方向**：
    1.  **缩小与全监督的差距**：如何进一步改进自监督学习方法，使其在下游任务（尤其是实例分割）上的性能更接近甚至超越全监督方法，是核心挑战。
    2.  **探索更复杂的几何关系**：除了指向中心的偏移外，还可以考虑预测物体朝向、部件关系、对称性等更丰富的几何属性作为自监督信号。
    3.  **动态与开放世界场景**：将方法扩展到包含动态物体或未知类别的开放世界场景中。这需要更强的泛化能力和对新物体的归纳偏置。
    4.  **多模态融合**：结合图像、文本等多模态信息进行联合预训练，以增强模型的语义理解和开放词汇能力。

#### **10. 总结评价**
本文是一篇高质量、具有前瞻性的研究工作。它敏锐地识别了当前点云自监督学习领域的一个关键瓶颈——缺乏对实例级几何关系的有效建模。作者提出的PointINS框架构思巧妙，通过引入偏移分支并辅以ODR和SCR两种互补的正则化策略，在无需人工标注的情况下成功实现了对语义和实例感知表征的联合学习。

该工作的主要贡献在于其清晰的**问题定义**、新颖且有效的**方法论设计**以及极其全面和令人信服的**实验验证**。它不仅显著提升了多个基准数据集上实例/全景分割任务的性能，更重要的是为构建更具通用性的3D视觉基础模型指明了方向。尽管存在一定的计算开销以及与全监督方法的性能差距等局限性，但其核心思想和对领域的推动作用无疑是显著的。

总而言之，《Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds》是一篇扎实且富有洞察力的论文，为点云表示学习和3D场景理解领域做出了重要贡献。

---

（完）
