---
title: Bootstrap Perception Under Hardware Depth Failure for Indoor Robot Navigation
title_zh: 面向室内机器人导航的硬件深度失效下的自举感知
authors: "Nishant Pushparaju, Vivek Mattam, Aliasghar Arab"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28890v1"
tags: ["query:3d"]
score: 9.0
evidence: 将学习到的单目深度校准为度量尺度
tldr: "本文针对室内机器人导航中硬件深度传感器（如ToF相机）在反光表面大量失效的问题（数据丢失高达78%），提出了一种自引导的感知系统。该系统利用传感器幸存的有效像素来校准学习的单目深度到度量尺度，从而无需外部数据即可填补自身感知缺口。其核心是构建一个故障感知的层次化感知架构：以激光雷达为几何锚点，保留有效的硬件深度数据，仅在需要时引入学习深度进行补充。主要贡献在于显著提升了障碍物覆盖率，并实现了高效、低成本的实时运行性能。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.28890v1/fig-001.webp\", \"caption\": \"Fig. 5. Live Nav2 costmap during corridor replay. Left: hardware depth (top, mostly invalid) and RGB camera (bottom). Right: costmap overlay showing DA3-Small depth contributions (green) and V9 student depth (blue). Learned depth detects chairs and tables visible through the glass wall where the ToF sensor returns invalid depth.\", \"page\": 6, \"index\": 1, \"width\": 529, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2603.28890v1/fig-002.webp\", \"caption\": \"Fig. 1. Depth fusion under worst-case ToF failure (20% valid pixels). The hardware sensor retains only a sparse stippled pattern; DA3-Small supplies dense depth estimates in the regions where the hardware sensor fails. Even at 80% pixel failure, the fused output combines surviving hardware depth with learned depth to produce a dense depth estimate for costmap construction.\", \"page\": 2, \"index\": 2, \"width\": 529, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2603.28890v1/fig-003.webp\", \"caption\": \"TABLE IV STUDENT DISTILLATION ABLATION. PHASE 1 BUILDS THE GENERAL-PURPOSE STUDENT CUMULATIVELY; PHASE 2 TESTS DOMAIN-ADAPTATION STRATEGIES FROM THE BEST PHASE 1 CHECKPOINTS. LILOC RMSE IS EVALUATED ON LILOCBENCH DYNAMICS_0.\", \"page\": 7, \"index\": 3, \"width\": 506, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2603.28890v1/fig-004.webp\", \"caption\": \"Fig. 2. System architecture. LiDAR remains the primary geometric reference, hardware depth is used where valid, learned monocular depth replaces unreliable active-depth regions, and optional semantics refine obstacle footprint only when needed. Heavy teachers remain off-board. Dashed orange paths are optional.\", \"page\": 4, \"index\": 4, \"width\": 1032, \"height\": 768}, {\"url\": \"assets/figures/arxiv/2603.28890v1/fig-005.webp\", \"caption\": \"Fig. 4. False-positive source decomposition (DA3-Small, n=459 corridor frames). Sensor-invalid fill (34.6%) represents structure added in ToF deadpixel regions; hallucination (49.3%) is free-space false occupancy; inflation artifacts (18.1%) arise from Nav2 inflation of false cells.\", \"page\": 5, \"index\": 5, \"width\": 529, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2603.28890v1/fig-006.webp\", \"caption\": \"Fig. 3. Depth fusion on a representative corridor frame (43% valid ToF pixels). Top: RGB input showing polished floor and glass doors, sparse sensor depth with large invalid regions, and DA3-Small depth prediction. Bottom: V9 corridor-specialist student, and both fused outputs (sensor+DA3, sensor+V9). Learned depth fills the floor and glass regions where the ToF sensor returns zero depth.\", \"page\": 5, \"index\": 6, \"width\": 529, \"height\": 302}]"
motivation: 针对室内机器人导航中硬件深度传感器（如ToF相机）在反光表面大量失效的问题，仅靠2D激光雷达无法感知扫描平面上方障碍物。
method: 提出一种故障感知的层次化感知架构，结合激光雷达、硬件深度传感器和学习的单目深度估计，仅在需要时用学习深度填补缺失区域。
result: "在走廊和动态行人场景评估中，选择性融合使代价地图的障碍物覆盖率比仅用激光雷达提高55-110%；蒸馏后的轻量模型在Jetson Orin Nano上达到218 FPS，闭环仿真中导航成功率达9/10且零碰撞。"
conclusion: 该系统通过选择性融合策略，在硬件深度失效时有效补充感知信息，实现了与真实深度基准相当的导航性能，同时大幅降低了计算成本。
---

## 摘要
我们提出了一种用于硬件深度失效下室内机器人导航的自举感知系统。在我们的走廊数据中，飞行时间相机在反射表面上会丢失高达78%的深度像素，而仅靠2D激光雷达无法感知其扫描平面上方的障碍物。我们的系统利用了这种故障的一种自指特性：传感器存留的有效像素将学习到的单目深度校准到度量尺度，因此系统无需外部数据即可填补自身的空白。该架构形成了一个故障感知的传感层次结构：在传感器正常工作时保持保守，在失效时进行填补——激光雷达作为几何锚点保持不变，硬件深度在有效区域予以保留，而学习到的深度仅在需要时介入。在走廊和动态行人评估中，选择性融合相比仅使用激光雷达将代价地图的障碍物覆盖率提高了55-110%。一个经过蒸馏的紧凑学生模型在Jetson Orin Nano上以218 FPS运行，并在闭环仿真中实现了9/10的导航成功率且零碰撞，以远低于基础模型的成本达到了与真实深度基线相当的性能。

## Abstract
We present a bootstrap perception system for indoor robot navigation under hardware depth failure. In our corridor data, the time-of-flight camera loses up to 78% of its depth pixels on reflective surfaces, yet a 2D LiDAR alone cannot sense obstacles above its scan plane. Our system exploits a self-referential property of this failure: the sensor's surviving valid pixels calibrate learned monocular depth to metric scale, so the system fills its own gaps without external data. The architecture forms a failure-aware sensing hierarchy, conservative when sensors work and filling in when they fail: LiDAR remains the geometric anchor, hardware depth is kept where valid, and learned depth enters only where needed. In corridor and dynamic pedestrian evaluations, selective fusion increases costmap obstacle coverage by 55-110% over LiDAR alone. A compact distilled student runs at 218\,FPS on a Jetson Orin Nano and achieves 9/10 navigation success with zero collisions in closed-loop simulation, matching the ground-truth depth baseline at a fraction of the foundation model's cost.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Bootstrap Perception Under Hardware Depth Failure for Indoor Robot Navigation》的论文进行结构化、深入且客观的总结。

---

### **论文总结：硬件深度失效下的室内机器人导航自举感知系统**

#### **1. 核心问题与整体含义**
*   **研究动机**：针对室内机器人（如仓储、物流机器人）中广泛使用的“2D激光雷达+ToF相机”配置存在的固有缺陷。ToF相机在抛光地板、玻璃门等反光表面会严重失效（文中数据：特定走廊场景下高达78%的深度像素无效），导致几何信息不完整；而2D激光雷达仅能感知单一水平面的障碍物，无法探测扫描平面上方的物体（如行人躯干）。
*   **整体含义**：提出一种**故障感知**和**自引导**的感知系统。其核心创新在于利用传感器自身故障中的“幸存”有效像素来实时校准学习到的单目深度，从而在无需外部数据或离线标定的情况下，填补因硬件失效造成的感知空白。该系统旨在构建一个层次化、保守且互补的传感架构，而非简单地用学习模型替换硬件传感器。

#### **2. 方法论**
*   **核心思想**：构建一个分层的、故障感知的传感融合架构。
    1.  **几何锚点**：2D激光雷达作为稳定可靠的几何参考基准。
    2.  **选择性融合**：
        *   硬件（ToF）深度在置信度足够时优先使用。
        *   学习的单目深度仅在硬件深度无效的区域被激活并填充。
    3.  **自举校准**：利用同一帧中ToF传感器存留的有效像素（即使只有22%），通过计算中位数比例因子 `s`，将学习到的相对深度 `ˆd` 实时校准到度量尺度 `d = s·ˆd`。这是系统的关键创新点。
    4.  **可选语义细化**：在需要时（如动态行人场景），引入语义信息来调整不同类别障碍物的膨胀半径。

*   **关键技术细节与流程**：
    1.  **有效性过滤与校准**：基于ToF传感器的置信度 `c(u,v)` 和有效距离范围过滤无效像素。使用公式(1)计算每帧的尺度因子 `s`。
    2.  **融合规则 (公式2)**：
        ```
        dfused(u,v) = dtof(u,v), if c(u,v) ≥ τ and dmin < dtof < dmax
                       d(u,v),    otherwise
        ```
        即“硬件有效则用硬件，否则用学习深度”。
    3.  **代价地图构建**：将融合后的深度点云反投影到3D空间，转换到机器人坐标系，并与激光雷达点云一起输入到局部代价地图中。
    4.  **紧凑学生模型训练**：
        *   **架构**：基于EfficientViT-B1编码器，共享解码器同时预测深度和语义分割。
        *   **监督信号 (公式3)**：训练目标由教师模型（DA3-Large）和有效的硬件深度共同构成。在硬件失效区域使用教师监督，在有效区域直接使用硬件真值。
        *   **损失函数 (公式4)**：结合berHu深度损失、分割交叉熵损失和边缘感知平滑正则项，并使用Kendall不确定性加权进行多任务平衡。

#### **3. 实验设计**
*   **数据集/场景**：
    1.  **Corridor Dataset (自制)**：410秒大学走廊录制的数据（12,384帧），包含大量反光表面。其中459帧为高失效片段（平均78%像素无效），用于核心消融实验。
    2.  **LILocBench (公开)**：`dynamics_0`序列，包含动态行人的室内大厅场景，用于测试系统对激光雷达平面外障碍物的感知能力。
    3.  **NYU Depth V2 (公开)**：用于学生模型的通用预训练和评估基准。
*   **Benchmark/对比方法**：
    *   主要对比对象是导航系统中常用的传感器配置基线：
        1.  `Base/A1`: 仅使用LiDAR + （可选）固定/走廊宽度膨胀。
        2.  不同组合的消融实验 (`A2-A6`) ：比较 LiDAR + Sensor Depth (`L+S`)、LiDAR + Learned Depth (`L+D`)、仅Learned Depth (`D`)等配置的性能差异。
        3.  基础模型 (`DA3-Small`) vs. 蒸馏学生模型 (`V9`) 。
    *   评估指标包括代价地图占用单元数、检测率、IoU、假阳性率(FPR)、RMSE、δ<1.25精度以及闭环仿真中的导航成功率、碰撞次数等。

#### **4. 资源与算力**
*   论文未明确说明教师模型（DA3-Large, SAM2-Large）的训练所使用的具体GPU型号、数量和时长。这些重型模型仅在服务器端离线运行以生成监督标签。
*   对于学生模型的训练，提到了使用AdamW优化器进行200个epochs（NYU）或50个epochs（走廊微调），但未指定具体GPU型号和单次训练时间。
*   ***部署平台***有明确说明：
    *   ***机器人端***: Jetson Orin Nano (8GB)。
    *   报告了不同配置下的运行时性能（FPS）和内存占用（见表I）。

#### **5. 实验数量与充分性**
*   ***实验数量充足且设计合理***：
    1.  ***离线回放消融实验***: Table II展示了7种不同配置在走廊高失效片段上的详细对比，从多个维度评估了性能增益与代价。
    2.  ***动态场景评估***: Table III对比了不同传感器组合在两个数据集上的障碍物覆盖率提升效果(`+55%`, `+110%`) 。
    3.  ***学生模型蒸馏消融***: Table IV系统地进行了两阶段共6个变体的消融研究(V3-V9)，清晰地展示了从通用到专用模型的性能权衡曲线。
    4.  ***近距安全边界分析***: Table V专门评估了对导航安全至关重要的近距(0-1m)深度精度差距。
    5.  ***闭环仿真验证***: Table VI在Gazebo仿真环境中验证了专用学生模型在实际导航任务中的可行性(9/10成功率)。
*   ***客观性与公平性***:
    *   实验不仅报告了正面结果（覆盖率提升），也深入分析了负面结果。例如对假阳性来源进行了分解(Fig4)，明确指出约49%是学习模型的自由空间幻觉；承认了学生模型在近距精度上与基础模型的巨大差距；指出了专用化带来的灾难性遗忘问题。这种对局限性的坦诚讨论增强了研究的客观性和可信度。

#### **6.主要结论与发现**
1.**选择性融合策略有效**:提出的层次化融合策略(`L+D`)显著提升了代价地图障碍物覆盖率(走廊+55%，动态场景+110%) ，并且意外地通过提供更稳定的密集预测减少了因ToF间歇性失效导致的代价地图闪烁。

2.**自举校准机制可行**:即使在高比例像素失效的情况下(如78%) ，剩余的有效像素仍能提供足够稳定(变异系数<8%)的中位数尺度估计，实现无外部数据的实时度量校准。

3.**紧凑学生模型的可行性边界**:通过知识蒸馏可以获得极高效的专用模型(218 FPS, ~10倍于基础模型) ，并在已知环境的闭环仿真中达到与真实深度基线相当的导航性能。然而其近距精度(`δ<1,25=8,83%`)远低于基础模型(`70,7%`) ，这划定了其应用边界——适用于固定路线且有安全监控的场景。

####7.**优点**
- ***问题定义清晰且实用***:精准定位了室内机器人实际部署中的痛点问题(ToF反光失效+LiDAR垂直盲区)。
- ***方法新颖且优雅***:“利用故障本身来修复故障”的自举思想巧妙而高效地解决了在线标定难题.
- ***系统设计层次分明且保守***:以LiDAR为锚点保证了系统的鲁棒性基础;仅在必要时引入学习组件降低了风险.
- ***评估全面且深入***:不仅关注宏观指标(覆盖率) ，还深入到假阳性分解近距安全仿真验证等细节;同时兼顾了离线回放和闭环仿真两种验证方式.
- ***对工程部署有实际指导意义***:明确比较了不同配置的计算开销内存占用为嵌入式部署提供了具体参考.

####8.**不足与局限**
- ***泛化能力限制***
 -系统高度依赖LiDAR作为几何锚点若LiDAR本身发生故障或退化系统性能未知.
 -紧凑学生模型的优异性能仅限于其微调过的特定环境(走廊)在其他环境或通用场景下表现不佳存在灾难性遗忘.
- ***安全风险***
 -学习深度的幻觉问题始终存在尽管本工作将其部分转化为“有益的假阳性”(填补空白)但仍有约一半的假阳性是纯粹的自由空间误报这可能影响规划效率.
 -学生模型的近距精度严重不足在没有额外安全层的情况下不适合用于完全自主的一般性导航.
- ***实验条件理想化***
 -闭环仿真是在Gazebo中进行的缺乏真实的反光表面光照变化等挑战因此其结果更多是可行性证明而非性能保证.
 -所有真实世界数据均通过遥操作收集未进行端到端的全自主硬件部署测试.
- ***技术假设***
 -依赖于ToF传感器能提供有效的置信度估计(`c(u,v)`).若置信度估计不准融合规则可能失效.
 -当有效像素覆盖率极低(<5%)时文中提到尺度估计会变得不可靠但未给出此时的处理策略.

---
**(完)**

好的，我将从您提供的总结末尾开始，继续补全对这篇论文的分析。

---

#### **9. 潜在改进方向与未来工作**
*   **提升学生模型的通用性与安全性**：
    *   探索持续学习或领域自适应方法，以缓解专用学生模型的灾难性遗忘问题，使其能在多个环境或新环境中保持可靠性能。
    *   研究更先进的损失函数或架构，专门针对近距（0-1米）深度精度进行优化，这是导航安全的关键区域。
    *   集成更强大的不确定性估计模块，以区分“有益的填补性幻觉”和“有害的自由空间误报”，从而在代价地图构建中做出更智能的决策。
*   **增强系统鲁棒性**：
    *   设计LiDAR故障检测与处理机制，避免在几何锚点失效时系统崩溃。
    *   研究在有效像素覆盖率极低（如低于5%）情况下的备用策略，例如切换到纯学习模式并辅以更保守的安全策略。
*   **推进真实世界验证**：
    *   在包含更多样化、更具挑战性的反光表面和动态障碍物的真实环境中进行长期、端到端的自主导航测试。
    *   将系统集成到完整的机器人软硬件栈中，评估其在真实任务（如物料搬运、人员跟随）中的综合表现。

#### **10. 总结与最终评价**
本研究针对室内机器人导航中深度传感器硬件失效这一具体且实际的工程问题，提出了一种新颖、务实且高效的解决方案。其核心贡献在于**“自举校准”思想**——巧妙地利用传感器自身故障中残存的有效信息来实时校准学习模型，从而实现了无需外部标定的在线深度修复。通过层次化的融合架构（LiDAR为锚、硬件深度优先、学习深度填补）和专用的紧凑学生模型蒸馏，该系统在特定环境下取得了感知覆盖率显著提升、计算效率满足嵌入式部署要求的良好平衡。

然而，该工作也清晰地揭示了其局限性：专用学生模型的泛化能力弱、近距精度不足带来的安全风险，以及尚未在复杂真实世界中完成全面验证。这些不足并非方法的致命缺陷，而是为未来研究指明了方向。总体而言，这是一项**问题驱动明确、方法创新性强、实验分析深入细致**的优秀工程研究。它没有追求不切实际的通用全能感知，而是聚焦于解决一个痛点问题并坦诚地讨论了方案的边界，这种务实的态度对机器人领域的实际应用具有重要的参考价值。

（完）
