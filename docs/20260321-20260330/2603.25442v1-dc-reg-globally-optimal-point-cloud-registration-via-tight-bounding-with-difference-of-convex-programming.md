---
title: "DC-Reg: Globally Optimal Point Cloud Registration via Tight Bounding with Difference of Convex Programming"
title_zh: DC-Reg：通过凸差规划紧致定界实现全局最优点云配准
authors: "Wei Lian, Fei Ma, Hang Pan, Zhesen Cui, Wangmeng Zuo"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25442v1"
tags: ["query:3d"]
score: 9.0
evidence: 用于3D重建的全局最优点云配准
tldr: 本研究针对点云配准在部分重叠和大错位下的全局最优挑战，提出DC-Reg框架。该方法基于差异凸编程推导整体凹下界估计器，通过分支定界搜索和线性分配问题计算紧致下界，显著加速收敛。实验表明其在2D相似性和3D刚性配准中优于现有技术，实现高效且鲁棒的全局最优解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25442v1/fig-001.webp\", \"caption\": \"Figure 4: Average registration errors (top 2 rows) and run times (bottom row) by various methods under various np values (ranging from 1/2 to 1/1 of the ground truth value) over 100 random trials for 2D deformation, positional noise, mixed outliers and inliers, separate outliers and inliers, and occlusion+outlier tests.\", \"page\": 11, \"index\": 1, \"width\": 964, \"height\": 643}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-002.webp\", \"caption\": \"Figure 5: Example of registration results from different methods in the occlusion+outlier test, with np chosen as ground truth for all methods.\", \"page\": 11, \"index\": 2, \"width\": 950, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-003.webp\", \"caption\": \"Figure 11: Examples of registration results generated by different methods on the five RGB-D reconstruction datasets, arranged from top to bottom: sun3d, 7-scenes, rgbd-scenes-v2 (repeated), and analysis-by-synthesis.\", \"page\": 14, \"index\": 3, \"width\": 976, \"height\": 654}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-004.webp\", \"caption\": \"Figure 10: Average registration errors from various methods across five RGB-D reconstruction datasets.\", \"page\": 14, \"index\": 4, \"width\": 949, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-005.webp\", \"caption\": \"Figure 1: obain concave underestimator mi j of Ei j via DC decomposition and linearization of the convex term.\", \"page\": 3, \"index\": 5, \"width\": 444, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-006.webp\", \"caption\": \"Figure 8: Average registration errors (top 2 rows) and run times (bottom row) of various methods under varying np values (1/2 or 1/1 of the ground truth value) across 100 random trials for 3D deformation, positional noise, mixed outliers and inliers, separate outliers and inliers, and occlusion+outlier tests.\", \"page\": 13, \"index\": 6, \"width\": 924, \"height\": 644}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-007.webp\", \"caption\": \"Figure 9: Examples of registration results from different methods in the separate outliers and inliers test, where the np values of RPM-HTB and Go-ICP are both chosen as the ground truth.\", \"page\": 13, \"index\": 7, \"width\": 974, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2603.25442v1/fig-008.webp\", \"caption\": \"Figure 6: (a) Source images with source point sets superimposed. (b) For each category: target images with target point sets superimposed, registration results by DC-Reg, RPM-HTB [10], RPM-PA [42] and RPM-CAV [9] using similarity transformation. The np value for each method is chosen as 0.9 the minimum of the cardinalities of two point sets. 12\", \"page\": 12, \"index\": 8, \"width\": 858, \"height\": 1125}]"
motivation: 解决点云配准中因非凸耦合目标导致的局部最优和收敛缓慢问题，实现全局最优配准。
method: 提出DC-Reg框架，基于DC编程推导整体凹下界估计器，通过线性分配问题计算紧致下界以加速分支定界搜索。
result: 在合成数据和3DMatch基准测试中，DC-Reg相比现有方法收敛更快，对噪声和异常值具有更强鲁棒性。
conclusion: DC-Reg框架通过DC编程和整体凹下界估计，实现了点云配准的全局最优解，显著提升了收敛速度和鲁棒性。
---

## 摘要
在部分重叠和大错位情况下实现全局最优的点云配准仍然是一个根本性挑战。虽然同时进行变换（$\boldsymbolθ$）和对应关系（$\mathbf{P}$）估计具有对非刚性形变鲁棒的优点，但其非凸耦合目标函数常常导致启发式方法陷入局部极小值，而现有全局求解器则因松弛下界不紧而收敛时间过长。为解决此问题，我们提出了DC-Reg，一个通过紧致定界显著加强分支定界搜索的鲁棒全局最优框架。我们的核心创新在于基于凸差规划范式，为耦合的变换-分配目标函数推导了一个整体的凹下估计器。与先前依赖逐项松弛（例如McCormick包络）而忽略变量间相互作用的工作不同，我们的整体凸差分解捕捉了$\boldsymbolθ$与$\mathbf{P}$之间的联合结构交互。这种表述使得通过在搜索框顶点处求解高效的线性分配问题来计算极其紧致的下界成为可能。我们在2D相似变换和3D刚性配准上验证了我们的框架，后者利用旋转不变特征以实现高效率而不牺牲最优性。在合成数据和3DMatch基准测试上的实验结果表明，与最先进的全局技术相比，DC-Reg实现了显著更快的收敛速度以及对极端噪声和异常值的卓越鲁棒性。

## Abstract
Achieving globally optimal point cloud registration under partial overlaps and large misalignments remains a fundamental challenge. While simultaneous transformation ($\boldsymbolθ$) and correspondence ($\mathbf{P}$) estimation has the advantage of being robust to nonrigid deformation, its non-convex coupled objective often leads to local minima for heuristic methods and prohibitive convergence times for existing global solvers due to loose lower bounds. To address this, we propose DC-Reg, a robust globally optimal framework that significantly tightens the Branch-and-Bound (BnB) search. Our core innovation is the derivation of a holistic concave underestimator for the coupled transformation-assignment objective, grounded in the Difference of Convex (DC) programming paradigm. Unlike prior works that rely on term-wise relaxations (e.g., McCormick envelopes) which neglect variable interplay, our holistic DC decomposition captures the joint structural interaction between $\boldsymbolθ$ and $\mathbf{P}$. This formulation enables the computation of remarkably tight lower bounds via efficient Linear Assignment Problems (LAP) evaluated at the vertices of the search boxes. We validate our framework on 2D similarity and 3D rigid registration, utilizing rotation-invariant features for the latter to achieve high efficiency without sacrificing optimality. Experimental results on synthetic data and the 3DMatch benchmark demonstrate that DC-Reg achieves significantly faster convergence and superior robustness to extreme noise and outliers compared to state-of-the-art global techniques.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将为您提供对论文《DC-Reg: Globally Optimal Point Cloud Registration via Tight Bounding with Difference of Convex Programming》的结构化、深入且客观的总结。

---

## **DC-Reg 论文深度分析总结**

### **1. 核心问题与整体含义**
*   **研究背景**：点云配准是计算机视觉中的基础任务。在存在部分重叠、大错位、噪声和异常值的情况下，实现**全局最优**配准是一个长期挑战。
*   **现有方法的局限**：
    *   **局部方法（如ICP）**：依赖初始化，易陷入局部最优。
    *   **全局方法（如分支定界 BnB）**：能保证全局最优，但面临效率瓶颈。现有基于BnB的全局求解器通常采用“逐项松弛”策略来估计目标函数的下界，这种方法忽略了变换参数（$\boldsymbolθ$）与对应关系矩阵（$\mathbf{P}$）之间的耦合关系，导致下界过于宽松，收敛速度极慢。
*   **本文动机**：针对上述“逐项松弛”导致下界不紧的问题，提出一种新的框架（DC-Reg），旨在通过一种更紧致的下界估计方法，显著加速全局最优配准的收敛过程。

### **2. 方法论**
*   **核心思想**：将耦合的变换-对应关系联合优化问题重新表述为凸差规划问题，并推导出一个**整体的凹下估计器**来替代传统的逐项松弛。
    *   **关键创新点**：“整体”而非“逐项”。该方法捕捉了$\boldsymbolθ$和$\mathbf{P}$之间的联合结构交互，从而得到更紧致的下界。
*   **关键技术细节与流程**：
    1.  **问题建模**：最小化耦合目标函数 $E(\mathbf{P}, \boldsymbolθ)$。通过边缘化对应关系矩阵 $\mathbf{P}$，得到关于 $\boldsymbolθ$ 的函数 $E(\boldsymbolθ)$。
    2.  **DC分解与凹下估计器构建**：
        *   假设成对代价 $E_{ij}(\boldsymbolθ)$ 可以分解为一个凸函数和一个凹函数之和：$E_{ij}(\boldsymbolθ) = E_{ij}^{cvx}(\boldsymbolθ) + E_{ij}^{cav}(\boldsymbolθ)$。
        *   在搜索框 $M$ 内任取一点 $\boldsymbolθ_0$（如几何中心），将凸部分 $E_{ij}^{cvx}(\boldsymbolθ)$ 在该点线性化（一阶泰勒展开），得到一个线性下估计器。
        *   将此线性下估计器与原有的凹部分 $E_{ij}^{cav}(\boldsymbolθ)$ 相加，构造出原函数 $E_{ij}(\boldsymbolθ)$ 在整个搜索框 $M$ 上的一个凹下估计器 $m_{ij}(\boldsymbolθ)$。
    3.  **紧致下界的计算**：
        *   定义辅助函数 $z(\boldsymbolθ) = \min_{\mathbf{P} \in \mathcal{P}} \sum_{i,j} p_{ij} m_{ij}(\boldsymboltheta)$。由于 $m_{ij}$是凹的且 $\mathcal{P}$是凸多面体集合，该最小值必然在搜索框 $M$ 的顶点处取得。
        *   因此，只需计算搜索框所有顶点处的线性分配问题解的最小值，即可得到当前搜索区域的一个非常紧致的全局下界 `LB(M)`。这避免了在高维空间进行分支定界。
    4.  **算法流程 (BnB)**：
        1.  初始化包含所有可能变换参数的搜索空间（一个超矩形）。
        2.  **(定界)**：对于当前待处理的子区域（子框），使用上述方法计算其紧致的下界 `LB`和一个候选解 $\hat{\theta}$。
        3.  **(剪枝)**：若某个子区域的下界大于当前已知的最优解上界减去容忍误差 $\epsilon$，则将该区域从搜索树中剪除。
        4.  **(分支)**：选择具有最小下界的子区域进行分割（通常沿最长边二分）。
        5.  重复步骤2-4直到满足终止条件（如所有区域被剪枝或达到最大迭代次数）。
*   **具体应用实例**：
    *   **2D相似变换配准 (Case I)**：直接应用上述框架。目标函数关于变换参数是凸的 ($E^{cav}=0$)，因此其线性化就是其自身的凹下估计器。需要评估4维参数空间的16个顶点LAP问题。
    *   **高效3D刚性配准 (Case II)**：
        *   为了降低维度、避免在12维仿射空间搜索的高昂代价 ($2^{12}=4096$个顶点)，作者采用了一种两阶段策略：
            1.  **平移估计 (t ∈ R³)**：利用旋转不变特征——点的范数 $\|x_i + t\|$——构造新的目标函数来独立求解最优平移。该目标函数天然具有DC结构。此时只需评估8个顶点的LAP问题即可获得平移的下界和最优解。
            2.  **旋转恢复 (R)**：在获得最优平移后，使用现有的鲁棒旋转估计算法求解旋转。

### **3. 实验设计**
*   **数据集/场景与Benchmark对比对象**：
    1.  **合成数据实验 (2D & 3D)**:
        *   `fish`, `character` (2D); `horse`, `dino` (3D)。
        *   测试了五种扰动场景：(1)非刚性形变；(2)位置噪声；(3)混合异常值；(4)分离异常值；(5)遮挡+异常值。每种场景均包含随机旋转和缩放。
        *   ***对比方法***:
            - ***2D***: RPM-HTB, RPM-PA, RPM-CAV
            - ***3D***: RPM-HTB, Go-ICP, FRS, GORE, TEASER++
    2.  ***真实图像数据实验 (2D)***:
        *   从Caltech-256和VOC2007数据集中提取Canny边缘点集进行配准测试。源点集预先旋转180度以测试旋转不变性。
    3.  ***真实世界3D数据实验***:
        *   在标准的`3DMatch`基准数据集上进行评估。该数据集包含来自5个不同RGB-D重建数据集 (`sun3d`, `7-scenes`, `rgbd-scenes-v2`, `bundlefusion`, `analysis-by-synthesis`)的62个场景。

### **4.资源与算力**
论文中未明确说明具体的GPU型号、数量或训练时长等算力细节。文中仅提到：“We implement the proposed DC-Reg framework in Matlab R2023b. All experiments are conducted on a workstation equipped with a _6-core_ _3_. _2 GHz CPU_.” （我们在Matlab R2023b中实现了所提出的DC-Reg框架。所有实验均在一台配备6核_ _3_. _2 GHz CPU_的工作站上进行。）这表明其实验主要依赖CPU计算资源。

### **5.**实验数量与充分性
*   ****实验数量充足****: 
    1. ****维度覆盖****:涵盖了二维相似变换和三维刚性配准两大主流任务类型.
    2. ****扰动类型全面****:针对每种任务都系统性地测试了形变、噪声、不同类型异常值和遮挡等多种极具挑战性的扰动场景.
    3. ****数据来源多样****:使用了合成形状(控制变量)、真实图像边缘点(复杂背景)、以及大规模标准RGB-D扫描数据集(`3DMatch`)进行验证.
    4. ****参数敏感性分析****:不仅使用真实内点数(`np=1/1`)进行测试还使用了低估的内点数(`np=1/2`)来检验算法对先验信息不准确的鲁棒性.
    5 . ****统计可靠性**** :每个合成数据实验都进行了100次随机试验并报告平均误差和运行时间.

* ****客观性与公平性评估**** :
      - ****对比对象选择合理**** :选择了同类型的基于BnB的全局方法(RPM系列)、经典的Go-ICP以及近年来表现优异的鲁棒求解器(TEASER++ , GORE等).这确保了比较是在相同或相近的目标(全局最优或高鲁棒性)上进行的.
      - ****评价指标标准**** :使用均方根误差(RMSE)作为主要精度指标同时报告运行时间衡量效率这是该领域的通用做法.
      - ****潜在偏差风险较低但存在局限**** :
          - DC-Reg本身是一个确定性算法其结果不依赖于随机种子因此结果可复现性强.
          -然而其实验主要集中在部分重叠和非刚性形变的挑战上对于完全刚体且重叠率高的理想情况其相对于TEASER++等高度优化的专用方法的效率优势可能不明显这一点在文中也有提及(速度上的权衡).
          -未与其他前沿的非确定性深度学习方法(如Hunter , DGR等)进行直接比较这可能是因为后者追求的目标(高召回率快速推理)与本工作的核心贡献(确定性全局最优保证)有所不同.

###6 .论文的主要结论与发现
      - DC-Reg成功地通过整体DC分解构建了比传统逐项松弛更紧致的凹下估计器显著加速了分支定界的收敛过程实现了高效的全局最优点云配准.
      -该方法在二维相似性和三维刚性配准任务上均表现出色尤其在处理非刚性形变和高水平位置噪声时优于严格为刚体设计的先进方法(如TEASER++ , FRS).
      -即使在先验内点数(`np`)设置不准确的情况下(`np=1/2`) DC-Reg仍能保持稳定的性能证明了其整体边界策略的有效性和鲁棒性.
      -在真实世界的`3DMatch`基准测试中 DC-Reg达到了与顶尖鲁棒求解器(TEASER++ , GORE )相当的性能并在低重叠率(<0 .4 )场景中表现稳定有效避免了局部极小值.

###7 .优点
      - ****理论创新性强**** :首次将整体DC规划范式引入到联合变换-对应关系的BnB框架中解决了传统"逐项松弛"导致边界过松的核心问题这是方法论上的重要贡献.
      - ****性能优越且鲁棒**** :在各种极端扰动(形变、噪声、异常值、遮挡 )下的合成数据和真实基准测试中都展示了卓越的精度和稳定性.
      - ****算法设计巧妙灵活**** :
          1 .通过利用旋转不变特征(RIFs )将高维三维仿射配准问题巧妙地分解为先求平移再求旋转的两步策略极大地降低了计算复杂度使三维全局最优成为可能.
          2 .边界计算最终归结为在每个搜索框顶点求解线性分配问题(LAP )这是一个有高效成熟解法的问题保证了每步迭代的效率.

###8 .不足与局限
      - ****计算复杂度依然较高**** :尽管相比同类BnB方法收敛更快但其本质仍是穷举式搜索在最坏情况下时间复杂度依然是指数级的对于大规模点云或更高维度的变形模型实时应用仍有困难文中也承认这是未来需要GPU并行化加速的方向之一 .
      - ****应用范围限制于特定模型形式**** :目前的方法依赖于目标函数能够被表示为成对代价之和并且能够找到合适的DC分解这对于某些复杂的非刚性变形模型可能具有挑战性限制了其直接推广 .
      - ****对聚类异常值的处理相对较弱**** :实验结果显示在处理"分离异常值"(即异常值聚集成簇 )时 DC-Reg的性能偶尔会被GO-ICP或GORE超越这表明基于RIF的特征描述子在面对结构化离群点时可能不如其他专门设计的鲁棒机制 .
      - ****未与现代深度学习方法进行全面对比**** :虽然与传统及几何优化方法进行了广泛比较但未纳入近年来表现突出的基于深度学习的配准方法这使得读者难以全面评估其在当前整个技术谱系中的定位 .

**(完)**

### **9. 总结与展望**
*   **核心贡献总结**：本文提出了一种名为DC-Reg的新型全局最优点云配准框架。其核心创新在于，通过将耦合的变换-对应关系联合优化问题重新表述为凸差（DC）规划问题，并构建一个**整体的凹下估计器**，替代了传统分支定界（BnB）方法中“逐项松弛”的策略。这种整体边界策略能够更紧密地逼近真实目标函数的下界，从而显著加速了BnB的收敛过程。
*   **性能验证**：该方法在二维相似变换和三维刚性配准任务上得到了系统验证。实验表明，DC-Reg在处理非刚性形变、高噪声、异常值和遮挡等挑战性场景时，其精度和鲁棒性优于或媲美现有的全局最优方法（如Go-ICP）及先进的鲁棒求解器（如TEASER++），同时保持了全局最优的理论保证。
*   **未来工作方向**：
    1.  **计算效率提升**：当前方法虽然比同类BnB更快，但计算成本仍然较高。未来的工作可以探索利用GPU并行化来加速分支定界过程，或研究更高效的启发式剪枝策略。
    2.  **模型扩展**：将DC分解和整体边界估计的思想扩展到更复杂的变换模型（如非刚性变形、仿射变换）是值得研究的方向。
    3.  **与深度学习的结合**：探索如何将本方法的全局最优保证与深度学习的强大特征提取能力相结合，例如使用学习到的描述子来初始化或约束对应关系搜索，可能催生更强大、高效的混合配准方案。
    4.  **理论深化**：进一步分析所提出凹下估计器的紧致性理论边界，以及在不同问题结构和噪声模型下的收敛行为。

综上所述，《DC-Reg: Globally Optimal Point Cloud Registration via Tight Bounding with Difference of Convex Programming》一文在点云配准的全局优化领域做出了重要的理论和方法学贡献。它通过巧妙的数学重构解决了传统方法下界松驰的关键瓶颈，为实现高效、鲁棒的全局最优配准提供了一条新颖且有效的路径。

（完）
