---
title: "ViewSplat: View-Adaptive Dynamic Gaussian Splatting for Feed-Forward Synthesis"
title_zh: ViewSplat：用于前馈式合成的视角自适应动态高斯泼溅
authors: "Moonyeon Jeong, Seunggi Min, Suhyeon Lee, Hongje Seong"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25265v1"
tags: ["query:3d"]
score: 10.0
evidence: 用于新视角合成的3D高斯泼溅技术
tldr: 本文提出ViewSplat，一种用于未标定图像新视角合成的视图自适应3D高斯溅射网络。针对现有前馈式方法因回归静态高斯基元导致的保真度瓶颈，该方法将范式从静态基元回归转向视图自适应动态溅射：首先预测基础高斯基元和动态MLP权重，在渲染时通过MLP根据目标视角坐标预测各高斯属性的残差更新。这种机制使每个基元能修正初始估计误差，从而捕获高保真外观。实验证明ViewSplat在保持快速推理和实时渲染的同时实现了最先进的保真度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25265v1/fig-001.webp\", \"caption\": \"Fig. 3: Qualitative comparison on RE10K. Compared to baseline methods, our approach accurately reconstructs sharp reflections.\", \"page\": 11, \"index\": 1, \"width\": 719, \"height\": 502}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-002.webp\", \"caption\": \"Fig. 1: Given unposed images as input, the feed-forward network reconstructs 3D Gaussians. While (a) rendering from static 3D Gaussians often suffers from blurred details, (b) our method introduces view-adaptive Gaussian updates conditioned on the target pose to refine the Gaussians dynamically. As shown in the bottom panels, our approach improves the reconstruction of fine-grained details (e.g ., sharp edges and specular reflections) compared to SPFSplat [11].\", \"page\": 2, \"index\": 2, \"width\": 723, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-003.webp\", \"caption\": \"Table 1: Performance comparison of novel view synthesis on the RE10K dataset [42]. Results using the ViewSplat framework are highlighted in bold, demonstrating consistent performance gains across all SPFSplat variants. * indicates the use of evaluation-time pose alignment (EPA) strategy.\", \"page\": 10, \"index\": 3, \"width\": 716, \"height\": 322}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-004.webp\", \"caption\": \"Table 2: Performance comparison of novel view synthesis on the ACID dataset [20].\", \"page\": 10, \"index\": 4, \"width\": 719, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-005.webp\", \"caption\": \"Fig. 4: Qualitative results of cross-dataset generalization.\", \"page\": 13, \"index\": 5, \"width\": 718, \"height\": 236}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-006.webp\", \"caption\": \"Table 5: Cross-dataset generalization. All methods are trained on RE10K and evaluated in a zero-shot setting on ACID and DTU. Results using the ViewSplat framework are highlighted in bold, demonstrating consistent performance gains across all SPFSplat variants.\", \"page\": 12, \"index\": 6, \"width\": 656, \"height\": 344}, {\"url\": \"assets/figures/arxiv/2603.25265v1/fig-007.webp\", \"caption\": \"Fig. 2: Architecture of ViewSplat. Built upon a shared geometry transformer backbone [18, 32], our framework simultaneously predicts canonical 3D Gaussians and camera poses from unposed images using Gaussian heads and a pose head. To accurately capture view-dependent effects, a view-dependent head dynamically generates per-pixel view MLPs, which takes the target pose as input to predict residual offsets for Gaussians. These offsets are then applied to refine the canonical Gaussians during rendering.\", \"page\": 5, \"index\": 7, \"width\": 721, \"height\": 220}]"
motivation: 现有前馈式3D高斯溅射方法因单步网络回归静态高斯基元的局限性，导致渲染保真度存在瓶颈。
method: 提出视图自适应动态高斯溅射网络，通过动态MLP根据目标视角坐标预测高斯属性的残差更新，实现从静态基元回归到动态视图适应性的转变。
result: 实验表明ViewSplat在保持快速推理（17 FPS）和实时渲染（154 FPS）的同时，达到了最先进的渲染保真度。
conclusion: ViewSplat通过引入视图自适应动态溅射机制，显著提升了前馈式3D高斯溅射的渲染保真度，实现了高质量实时新视角合成。
---

## 摘要
我们提出了ViewSplat，一种用于从无位姿图像进行新视角合成的视角自适应3D高斯泼溅网络。尽管近期的前馈式3D高斯泼溅技术通过绕过逐场景优化，显著加速了3D场景重建，但仍存在一个根本性的保真度差距。我们将此瓶颈归因于单步前馈网络在回归满足所有视角的静态高斯基元方面能力有限。为解决这一限制，我们将范式从静态基元回归转向视角自适应动态泼溅。我们的流程学习的不是刚性的高斯表示，而是一种视角自适应的潜在表示。具体而言，ViewSplat首先预测基础高斯基元以及动态多层感知机（MLP）的权重。在渲染过程中，这些MLP以目标视角坐标作为输入，并为每个高斯属性（即3D位置、尺度、旋转、不透明度和颜色）预测视角相关的残差更新。我们将此机制称为视角自适应动态泼溅，它允许每个基元修正初始估计误差，从而有效捕捉高保真外观。大量实验表明，ViewSplat在保持快速推理（17 FPS）和实时渲染（154 FPS）的同时，实现了最先进的保真度。

## Abstract
We present ViewSplat, a view-adaptive 3D Gaussian splatting network for novel view synthesis from unposed images. While recent feed-forward 3D Gaussian splatting has significantly accelerated 3D scene reconstruction by bypassing per-scene optimization, a fundamental fidelity gap remains. We attribute this bottleneck to the limited capacity of single-step feed-forward networks to regress static Gaussian primitives that satisfy all viewpoints. To address this limitation, we shift the paradigm from static primitive regression to view-adaptive dynamic splatting. Instead of a rigid Gaussian representation, our pipeline learns a view-adaptable latent representation. Specifically, ViewSplat initially predicts base Gaussian primitives alongside the weights of dynamic MLPs. During rendering, these MLPs take target view coordinates as input and predict view-dependent residual updates for each Gaussian attribute (i.e., 3D position, scale, rotation, opacity, and color). This mechanism, which we term view-adaptive dynamic splatting, allows each primitive to rectify initial estimation errors, effectively capturing high-fidelity appearances. Extensive experiments demonstrate that ViewSplat achieves state-of-the-art fidelity while maintaining fast inference (17 FPS) and real-time rendering (154 FPS).

---

## 论文详细总结（自动生成）

# ViewSplat 论文分析总结

## 1. 核心问题与整体含义
*   **研究背景**：3D高斯泼溅（3DGS）已成为一种强大的3D表示方法，能够实现高保真度的实时渲染。传统3DGS依赖于耗时的逐场景优化。近期，**前馈式通用架构**旨在直接从少量输入图像预测场景表示，实现即时重建和新视角合成（NVS），但存在一个显著的保真度瓶颈。
*   **研究动机**：作者认为，前馈式方法的瓶颈在于其单步网络难以回归出能满足所有潜在视角的**静态高斯基元**。对于复杂几何或非朗伯表面，一次性预测最优表示是一个不适定问题。优化方法可以通过数千次迭代来细化属性以捕捉锐利的镜面反射等视图相关效果，而前馈模型在推理时缺乏修正初始预测的能力。
*   **核心贡献**：提出 **ViewSplat**，将范式从静态基元回归转向**视角自适应动态泼溅**。其核心思想是：与其费力预测通用的3D高斯，不如让网络专注于生成针对特定视角的、更易学习的更新。

## 2. 方法论
*   **核心思想**：
    1.  **两阶段预测与渲染**：网络首先为给定场景预测一组基础高斯基元和一组动态MLP的权重。
    2.  **视图自适应动态泼溅机制**：在渲染阶段，这些动态MLP以目标视角坐标作为输入，为所有高斯属性（位置、尺度、旋转、不透明度、球谐系数）预测视图相关的残差更新。
    3.  **在线修正**：将残差更新应用于基础高斯上，从而得到针对当前目标视角优化的最终高斯参数。

*   **关键技术细节**：
    1.  **架构基础**: 基于自监督、无位姿的前馈框架（如SPFSplat），使用共享的几何Transformer骨干网络（如MASt3R, VGGT）从无位姿图像中联合重建规范空间下的3D高斯和估计相机位姿。
    2.  **视图相关头**: 引入一个基于DPT的“视图相关头”，它采用超网络架构。该头根据几何Transformer的特征令牌生成像素级的“视图MLP”参数。
    3.  **目标姿态重参数化**: 为避免直接将复杂的变换矩阵输入MLP导致优化困难，将目标外参矩阵重新参数化为一个紧凑的4维表示——包含一个从高斯基元指向相机中心的单位视线方向向量和一个对数尺度距离项。
    4.  **训练策略**: 采用冻结骨干网络的训练策略。使用预训练的SPFSplat权重初始化骨干网络和高斯/位姿头，仅对新增的“视图相关头”进行优化。

## 3. 实验设计
*   **数据集与场景**: RealEstate10K (RE10K) （大规模房地产视频）、ACID （无人机拍摄的自然场景）。遵循标准训练-测试划分。
*   **评估协议**: 
    *   根据图像重叠率将输入对分为小、中、大三组进行评估。
    *   在零样本设置下评估跨数据集泛化能力（在RE10K上训练，在ACID和DTU上测试）。
    *   使用标准指标: PSNR, SSIM, LPIPS。
*   **对比方法 (Baselines)**:
    *   ***需要位姿的方法***: pixelSplat, MVSplat.
    *   ***有监督的无位姿方法***: CoPoNeRF, Splatt3R, NoPoSplat.
    *   ***自监督的无位姿方法***: SelfSplat, PF3plat, SPFSplat (及其变体V2, V2-L).

## 4. 资源与算力
*   **训练硬件**: “multiple NVIDIA RTX 4090 GPUs”。
*   **推理硬件**: “a single GPU”（推断为RTX 4090）。
*   **分辨率**: SPFSplat/V2为256x256；V2-L变体为224x224（遵循其骨干网络的默认分辨率）。
*   **其他细节**: 
    *   使用Adam优化器，学习率为1e-4。
    *   批大小为12（每个批次包含单个场景及其对应的输入和目标视图）。
    *   采用课程学习策略——在训练过程中逐步增加输入视图之间的帧距。

## 5.实验数量与充分性
实验设计较为全面和客观：
1.  ***主实验***: 
    *   在两个主要数据集(RE10K和ACID)上进行了全面的新视角合成定量比较(表1-2)。
    *   提供了丰富的定性结果对比(图1,图3及附录中的大量示例)。
2. ***效率分析***: 
		*	评估了模型参数量、推理时间和渲染速度(FPS)，并与基线进行了对比(表3)。
		*	分析了不同隐藏层维度对性能/效率的影响(附录表S2)。
		*	公平地指出了引入动态机制带来的额外计算开销以及仍能保持实时性能的事实。
4. ***消融研究***:
		*	验证了各高斯属性残差更新的必要性及协同作用(表6)，特别是发现位置偏移必须与不透明度偏移同步以避免灾难性退化(图5)。
		*	比较了不同的训练策略(冻结vs微调)(表7)。
5. ***泛化能力验证***:
		在两个未见过的数据集(ACID和DTU)上进行零样本评估(表5和图4)，证明了方法的泛化性。

总体而言，实验覆盖了性能、效率、组件有效性和泛化性等多个维度，对比了当前主流方法（包括有监督和无监督），并进行了深入的消融分析。实验设置遵循领域内标准协议（如重叠率分组），结果具有可比性和说服力。

##6 .论文的主要结论与发现
1 .ViewSplat通过引入“视角自适应动态泼溅”机制 ，成功解决了前馈式3DGS中静态基元回归导致的保真度瓶颈 。
2 .该方法在所有SPFSplat变体上都带来了正交且一致的性能提升 ，证明了其鲁棒性和可扩展性 。特别是集成VGGT骨干(V2-L)的版本在所有基准测试中都达到了最先进的性能 。
3 .ViewSplat能够有效捕捉复杂的高频镜面反射等视图相关效果 ，这是静态前馈模型难以实现的 。
4 .尽管引入了额外的计算开销 ，但模型仍能保持实时渲染速度 (≥30 FPS) ，实现了质量与效率的良好平衡 。
5 .该方法具有良好的跨数据集泛化能力 。

##7 .优点
1 .创新性地提出了“视角自适应动态泼溅”范式 ，为解决前馈式重建中的保真度问题提供了一个新颖且有效的思路 。
2 .方法设计巧妙 ：通过超网络生成像素级MLP来预测残差 ，实现了对不同区域不同表面属性的灵活适应 ；对目标姿态进行重参数化处理 ，便于网络学习 。
3 .实验非常充分 ：不仅进行了全面的定量/定性比较和消融研究 ，还深入分析了效率 、泛化能力和不同组件的贡献 。
4 .保持了实用性 ：在显著提升保真度的同时 ，依然保证了快速推理和实时渲染的能力 。

##8 .不足与局限
1 .继承了基线模型的根本限制 ：作为基于重建的方法 ，缺乏生成先验 。因此无法合成未观测区域的内容 ，导致稀疏视角区域出现模糊或空洞伪影 (如图6所示) 。这是当前许多重建方法的共同挑战 。
2 .虽然保持了实时渲染速度 ，但相比纯静态基线 (如SPFSlat) , ViewSpatl的动态机制确实降低了渲染帧率 (例如从386 FPS降至154 FPS ,见表三)。这需要在更高保真度和极致速度之间做出权衡 。
论文明确指出了这些局限性并提供了可视化示例 。

**(完)**

## 9. 总结与展望

ViewSplat 提出了一种新颖的视角自适应动态泼溅范式，有效解决了前馈式3D高斯泼溅（3DGS）方法在复杂场景和视图相关效果（如镜面反射）上的保真度瓶颈。其核心贡献在于将一次性回归静态高斯基元的困难任务，转化为一个更易学习的、针对特定渲染视角的动态残差更新过程。

该方法通过一个两阶段流程实现：首先预测一组基础高斯基元，然后利用一个由超网络生成的、视图条件化的MLP网络，为目标视角下的每个高斯属性预测残差修正。这种设计允许模型在推理时根据具体视角“微调”其表示，从而捕捉到优化方法通过迭代才能获得的精细细节。

实验表明，ViewSplat 在多个标准数据集上实现了最先进的性能，显著提升了新视角合成的质量，同时保持了实时渲染的实用性。其模块化设计使其能够作为即插即用的增强模块，无缝集成到现有的无监督前馈框架中（如 SPFSplat），并带来一致的性能提升。

**未来工作方向**可能包括：
1.  **解决内容生成局限**：探索结合扩散模型或其他生成先验，以合成未被输入视图充分观测的区域内容，减少模糊和空洞伪影。
2.  **进一步优化效率**：研究更轻量级的动态头架构或自适应执行策略，以在保持质量优势的同时进一步逼近原始静态模型的渲染速度。
3.  **扩展应用场景**：将该动态机制应用于其他类型的神经场景表示（如 NeRF）或更复杂的任务中，如动态场景建模、编辑等。

总体而言，ViewSplat 是一项扎实且富有洞察力的工作。它精准地识别了前馈式3D重建中的一个关键问题，并提出了一个巧妙、有效且经过充分验证的解决方案。论文实验详实、分析深入、写作清晰，为即时、高质量的3D场景重建与渲染提供了重要的技术推进。

（完）
