---
title: "UniQueR: Unified Query-based Feedforward 3D Reconstruction"
title_zh: UniQueR：统一的基于查询的前馈三维重建
authors: "Chensheng Peng, Quentin Herau, Jiezhi Yang, Yichen Xie, Yihan Hu, Wenzhao Zheng, Matthew Strong, Masayoshi Tomizuka, Wei Zhan"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22851v1"
tags: ["query:3d"]
score: 10.0
evidence: 使用3D查询从无位姿图像进行3D重建
tldr: 本文提出UniQueR，一种基于统一查询的前馈框架，用于从无位姿图像进行高效准确的三维重建。针对现有前馈模型（如DUSt3R）本质仍是2.5D、局限于可见表面的问题，该方法将重建定义为稀疏三维查询推断问题：学习一组紧凑的三维锚点作为显式几何查询，使网络能在单次前向传播中推断包括遮挡区域在内的完整场景结构。每个查询在全局三维空间编码空间和外观先验，并生成一组三维高斯用于可微渲染。通过利用多视图特征间的统一查询交互和解耦交叉注意力设计，UniQueR在显著降低内存和计算成本的同时实现了强大的几何表达能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22851v1/fig-001.webp\", \"caption\": \"Fig. 2: UniQueR pipeline overview. Given multi-view images, a ViT encoder with alternating attention extracts per-frame tokens and decodes camera poses and point maps. A set of 3D queries is refined through cross-attention with image tokens and self-attention among queries. Each query then spawns K Gaussians, which are rendered via differentiable splatting for RGB and depth supervision.\", \"page\": 6, \"index\": 1, \"width\": 725, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-002.webp\", \"caption\": \"Table 4: Quantitative comparison for dense-view novel-view synthesis. We report PSNR, SSIM, and LPIPS. Best and second-best are in bold and underline. Top rows: feedforward only. Bottom rows: feedforward initialization followed by per-scene 3DGS/MipSplatting optimization.\", \"page\": 11, \"index\": 2, \"width\": 712, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-003.webp\", \"caption\": \"Table 7: Key hyperparameters used by UniQueR.\", \"page\": 21, \"index\": 3, \"width\": 508, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-004.webp\", \"caption\": \"Fig. 3: Comparison of attention designs. We contrast full self-attention over concatenated image and query tokens with our decoupled design using cross-attention followed by inter-query self-attention.\", \"page\": 7, \"index\": 4, \"width\": 725, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-005.webp\", \"caption\": \"Fig. 5: Ablation studies on (a) the number of queries, (b) the number of Gaussians per query, and (c) model capacity. Increasing any of these factors leads to consistent improvements in PSNR, demonstrating clear scaling behavior in UniQueR.\", \"page\": 14, \"index\": 5, \"width\": 728, \"height\": 203}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-006.webp\", \"caption\": \"Fig. 7: 3DGS and Mip-Splatting post-optimization with initializations from various methods.\", \"page\": 23, \"index\": 6, \"width\": 508, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2603.22851v1/fig-007.webp\", \"caption\": \"Fig. 4: Qualitative and geometric comparison. Top rows: rendered RGB on heldout novel views. Bottom rows: rendered depth maps. AnySplat produces blank regions in RGB and holes in depth where no input pixels provide coverage, due to its pixel-aligned representation. UniQueR fills in these occluded areas through 3D queries, yielding more complete geometry and fewer rendering artifacts.\", \"page\": 13, \"index\": 7, \"width\": 725, \"height\": 565}]"
motivation: 现有前馈模型（如DUSt3R）通常预测像素级点图或像素对齐高斯，本质仍是2.5D且局限于可见表面，无法有效推断遮挡区域。
method: 该方法将重建定义为稀疏三维查询推断问题，学习一组三维锚点作为几何查询，并通过解耦交叉注意力设计生成高斯表示进行可微渲染。
result: 在Mip-NeRF 360和VR-NeRF数据集上的实验表明，UniQueR在渲染质量和几何精度上均优于最先进的前馈方法，且所用基元数量比密集方法少一个数量级。
conclusion: UniQueR 通过统一的查询前馈框架，在高效性和准确性上超越了现有方法，为无位姿图像的三维重建提供了新思路。
---

## 摘要
我们提出了UniQueR，一个统一的基于查询的前馈框架，用于从无位姿图像中进行高效且精确的三维重建。现有的前馈模型（如DUSt3R、VGGT和AnySplat）通常预测逐像素点图或像素对齐的高斯分布，这些方法本质上仍是2.5D的，且仅限于可见表面。相比之下，UniQueR将重建问题表述为一个稀疏的三维查询推断问题。我们的模型学习一组紧凑的三维锚点作为显式的几何查询，使网络能够在前向传播中推断场景结构（包括遮挡区域的几何信息）。每个查询直接在全局三维空间（而非逐帧相机空间）中编码空间和外观先验，并生成一组用于可微分渲染的三维高斯分布。通过利用跨多视图特征的统一查询交互以及解耦的交叉注意力设计，UniQueR在显著降低内存和计算成本的同时实现了强大的几何表达能力。在Mip-NeRF 360和VR-NeRF数据集上的实验表明，UniQueR在渲染质量和几何精度上均超越了最先进的前馈方法，且使用的基元数量比密集方法少一个数量级。

## Abstract
We present UniQueR, a unified query-based feedforward framework for efficient and accurate 3D reconstruction from unposed images. Existing feedforward models such as DUSt3R, VGGT, and AnySplat typically predict per-pixel point maps or pixel-aligned Gaussians, which remain fundamentally 2.5D and limited to visible surfaces. In contrast, UniQueR formulates reconstruction as a sparse 3D query inference problem. Our model learns a compact set of 3D anchor points that act as explicit geometric queries, enabling the network to infer scene structure, including geometry in occluded regions--in a single forward pass. Each query encodes spatial and appearance priors directly in global 3D space (instead of per-frame camera space) and spawns a set of 3D Gaussians for differentiable rendering. By leveraging unified query interactions across multi-view features and a decoupled cross-attention design, UniQueR achieves strong geometric expressiveness while substantially reducing memory and computational cost. Experiments on Mip-NeRF 360 and VR-NeRF demonstrate that UniQueR surpasses state-of-the-art feedforward methods in both rendering quality and geometric accuracy, using an order of magnitude fewer primitives than dense alternatives.

---

## 论文详细总结（自动生成）

# UniQueR 论文结构化总结

## 1. 核心问题与整体含义
*   **研究动机**：传统的基于优化的三维重建方法（如NeRF、3DGS）虽然质量高，但需要逐场景优化，效率低下且难以泛化。现有的前馈式方法（如DUSt3R、AnySplat）通过单次前向传播进行重建，效率高且可泛化，但其核心表示（如像素对齐的点图或高斯）本质上是**2.5D的**。这意味着它们只能重建输入视角下可见的表面，无法有效推断和表示**被遮挡或未观察到的区域**，导致在新视角下出现空洞和伪影。
*   **整体含义**：本文提出了一种新的范式——将三维重建视为一个**稀疏的三维查询推断问题**。通过引入一组可学习的、与输入视角解耦的3D查询作为场景的核心表示，模型能够主动推断并填充遮挡区域的几何结构，从而实现更完整、更准确的三维重建。

## 2. 方法论
*   **核心思想**：使用一组稀疏的、可学习的3D查询作为场景的全局表示。每个查询代表一个空间锚点，负责编码其所在区域的几何和外观信息。这些查询通过与多视图图像特征交互来更新自身状态，然后每个查询“生成”一组局部的高斯分布用于最终的渲染。
*   **关键技术细节**：
    1.  **两阶段流程 (图2)**：
        *   **第一阶段 (几何先验提取)**：使用ViT（DINOv2）提取图像特征，并通过交替注意力Transformer聚合跨视图信息。解码器同时预测相机位姿、点云图和置信度图。
        *   **第二阶段 (查询式重建)**：基于预测的点云图初始化一部分查询（提供观测表面先验），另一部分随机初始化（用于探索未观测区域）。所有查询通过“Query Transformer”进行更新。
    2.  **解耦注意力设计 (图3)**：为了高效处理大量图像特征和查询，作者设计了“解耦交叉注意力”模块。它首先让所有查询对图像特征做交叉注意力以吸收信息，然后在查询之间做自注意力以整合全局信息。这比将所有token拼接后做全自注意力更高效。
    3.  **高斯生成与监督**：每个更新后的查询会解码出K个高斯分布的参数（中心位置、协方差、颜色、不透明度等）。这些高斯分布通过可微分的3D高斯泼溅进行渲染，得到RGB和深度图。
    4.  **关键训练策略 - 新视角监督**：训练时使用的监督视角是输入视角的超集。例如给定2个输入视图，模型会在4个新视图中渲染并接受监督。这迫使模型必须为未直接观察到的区域也分配合理的高斯分布。

## 3. 实验设计
*   **数据集/场景**：
    *   **训练集**: DL3DV-10K, ScanNet++, BlenderMVS, Co3D (混合了真实世界室内外场景和合成数据)。
    *   **评估基准**: Mip-NeRF 360 (无界室外场景) 和 VR-NeRF (高保真室内空间)。
    *   **位姿评估**: RealEstate10K (场景级) 和 Co3Dv2 (物体级)。
*   **Benchmark指标**：
    *   **新视角合成质量**: PSNR, SSIM, LPIPS。
    *   **几何精度**: Depth Absolute Relative Error。
    *   **相机位姿估计**: RRA@30°, RTA@30°, AUC@30°。
    *   **效率**: GPU内存占用、推理时间（秒）。
*   **对比方法**：
    *   前馈式NVS: NoPoSplat, AnySplat.
    *   相机位姿估计: Fast3R, CUT3R, FLARE, VGGT, Pi³.
    *   后优化性能: 将各方法预测的高斯/点云作为初始化，再用标准3DGS或Mip-Splatting进行逐场景优化。

## 4. 资源与算力
文中明确提到了算力配置：
*   `32 × A100 GPUs`用于训练。
*   初始在224×224分辨率下训练`100 epochs`，然后在448×448分辨率下微调`20 epochs`。
*   推理时间测量在单张`A100 80GB GPU`上进行。

## 5. 实验数量与充分性
实验设计较为全面和客观：
1.  **主实验对比**: 
    *   在稀疏视图(3/6视图)和密集视图(32/64视图)两种设定下评估新视角合成质量(表2,4)。
    *   在两个数据集上评估相机位姿估计精度(表3)。
2.  **效率分析**: 
    对比了GPU内存占用、推理时间和生成的高斯数量(表5)，证明了方法的效率优势。
3.  **消融研究 (Ablation Studies)**:
    系统地验证了关键组件的有效性(表6及图5)，包括：
        - `深度渲染监督`的重要性。
        - `混合初始化策略`相对于纯随机初始化的优势。
        - `查询数量`、`每个查询生成的高斯数量(K)`以及`模型容量`对性能的正向影响规律(清晰的缩放趋势)。
4.  `公平性`: 
    对于后优化比较(表4)，所有方法都使用相同的优化流程(如标准的Mip-Splatting)，仅初始化不同；对于稀疏视图评估(表2)，采用了多次采样取平均以减少因视角选择带来的方差。

## 6.主要结论与发现
1. UniQueR在稀疏视图设定下的新视角合成质量超越了现有前馈方法(AnySplat等)(表2)。
2. UniQueR生成的表示是更优的初始化源。当用作后续逐场景优化的起点时(`Ours + MipSplatting/3DGS`)能取得最佳性能(表4)，表明其预测的几何结构更完整准确。
3. UniQueR使用比密集像素对齐方法少一个数量级的高斯基元(`260K vs AnySplat的~4M`)实现了更高的几何精度(Depth Abs Rel更低)和更快的推理速度(`~40% faster`)及更低的内存占用(`~40% less memory`) (表5)。
4. UniQueR的相机位姿估计精度与当前最先进的方法(Pi³)相当或接近(表3)。

##7 .优点
1. `创新性`: 
    首次将“基于查询”的思想系统性地应用于场景级三维重建任务中，为解决遮挡区域重建问题提供了新颖且有效的思路。
2. `高效性`: 
    提出的解耦注意力机制显著降低了计算复杂度；稀疏的全局三维表示比密集的像素对齐表示更节省内存和计算资源。
3. `完整性`: 
    通过混合初始化和新视角监督策略有效鼓励了对未观测区域的建模能力定性结果显示了更好的遮挡处理效果。(图4)
4 .实验充分: 
    从定量到定性从主任务到消融从效率到质量进行了全面的验证展示了良好的缩放规律。

##8 .不足与局限
1 .动态场景: 
论文明确指出当前框架无法处理动态场景这是未来工作的方向之一。(第15页Limitations)
    
2 .密集视图下的纯前馈性能:
在输入视图非常丰富的情况下UniQueR的直接前馈输出可能略逊于某些密集像素对齐方法作者将其归因于其表示的固有稀疏性。(第12页)
    
应用限制:
该方法依赖于大规模多视图数据进行训练对于极端稀疏或严重遮挡的情况其泛化能力仍有待进一步验证。

---
**(完)**

## 9. 潜在影响与未来方向
1.  **范式影响**：
    *   **从“像素对齐”到“空间查询”**：UniQueR 成功地将基于查询（Query-based）的表示范式从目标检测、分割等感知任务，拓展到了场景级三维重建这一生成/推理任务中。这为后续研究开辟了一条新路径，即如何设计更智能的“查询”来理解和建模三维世界。
    *   **连接前馈与优化**：该方法生成的表示作为优化方法的优质初始化，证明了高效前馈模型与高质量优化方法可以协同工作，而非相互替代。这启发了构建“前馈粗建 + 优化精修”混合流水线的更多可能性。

2.  **未来研究方向**：
    *   **动态场景建模**：如论文所述，将静态查询扩展为时空查询，以处理动态物体和场景变化。
    *   **表示压缩与编辑**：稀疏的、语义化的查询表示天然适合于场景压缩、抽象和语义编辑。未来可探索基于查询的场景理解与交互。
    *   **更大规模与更高分辨率**：进一步探索模型容量（更多查询、更大Transformer）和训练数据规模对重建完整性和细节的缩放规律。
    *   **极端条件泛化**：研究在输入视图极少（如单视图）、存在严重运动模糊或光照变化等挑战性条件下的鲁棒性。

## 10. 总结评价
UniQueR 是一篇在三维重建领域具有显著创新性和实用价值的论文。它精准地指出了现有前馈方法受限于2.5D表示的固有缺陷，并提出了一个优雅且有效的解决方案——使用稀疏的3D查询作为场景的全局抽象表示。

*   **贡献的核心在于“主动推断”**：不同于被动地聚合观测到的像素信息，UniQueR 的查询机制使其能够像“智能体”一样，在三维空间中主动探索和假设被遮挡区域的几何结构。这是实现真正完整三维重建的关键一步。
*   **工程实现扎实**：论文不仅在思想上创新，在方法实现上也考虑周全。两阶段设计平衡了效率与精度；解耦注意力机制解决了计算瓶颈；混合初始化与新视角监督策略则直接针对核心目标进行优化。
*   **实验验证全面可信**：论文通过详尽的定量比较、消融实验和定性可视化，令人信服地证明了其方法在稀疏视图重建质量、几何完整性、计算效率以及作为优化初始化源方面的优势。同时，也客观分析了其在密集视图下和动态场景中的局限性。

总体而言，UniQueR 为迈向高效、完整且可泛化的三维重建系统提供了一个强有力的新基线，其提出的“查询式重建”范式很可能在未来几年内激发该领域的系列跟进工作。

---
（完）
