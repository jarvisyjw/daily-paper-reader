---
title: "MotionScale: Reconstructing Appearance, Geometry, and Motion of Dynamic Scenes with Scalable 4D Gaussian Splatting"
title_zh: MotionScale：利用可扩展的4D高斯泼溅重建动态场景的外观、几何与运动
authors: "Haoran Zhou, Gim Hee Lee"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29296v1"
tags: ["query:3d"]
score: 10.0
evidence: 用于动态场景神经渲染的4D高斯泼溅
tldr: 针对从单目视频重建动态4D场景时几何精度与运动一致性不足的问题，本文提出了MotionScale框架。该方法基于可扩展的4D高斯泼溅，其核心是通过聚类中心基变换参数化的可扩展运动场来捕捉多样演化的运动模式，并采用包含背景扩展与前景传播两阶段的渐进式优化策略以确保长时程鲁棒重建。实验表明其在复杂场景的重建质量和时间稳定性上显著优于现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29296v1/fig-001.webp\", \"caption\": \"Table 2. Comparison of point-based tracking performance on the DyCheck [10] dataset.\", \"page\": 7, \"index\": 1, \"width\": 496, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2603.29296v1/fig-002.webp\", \"caption\": \"Figure 2. Overview of MotionScale. Our method adopts a scalable motion field that progressively captures object motions through an adaptive control mechanism, enabling efficient splitting and refinement of motion components. For optimization, the background is updated through region sampling, camera refinement, and shadow handling, while the foreground propagation employs a three-stage refinement to propagate motion across long temporal windows for consistent 4D reconstruction.\", \"page\": 4, \"index\": 2, \"width\": 1022, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2603.29296v1/fig-003.webp\", \"caption\": \"Table 1. Comparison of novel view synthesis results on DyCheck [10] and NVIDIA [55] datasets.\", \"page\": 6, \"index\": 3, \"width\": 491, \"height\": 252}]"
motivation: 从单目视频中真实重建动态4D场景对于理解物理世界至关重要，但现有方法在复杂环境中难以恢复准确的3D几何和时序一致的运动。
method: 该方法核心是采用基于聚类中心基变换的可扩展运动场，并引入包含背景扩展和前景传播两阶段的渐进式优化策略。
result: 在具有挑战性的真实世界基准测试上的大量实验证明了其优越性。
conclusion: MotionScale在重建质量和时间稳定性上显著优于现有方法，为动态场景的4D重建提供了高效且高质量的解决方案。
---

## 摘要
从单目视频中逼真地重建动态4D场景对于理解物理世界至关重要。尽管神经渲染领域近期取得了进展，但现有方法在复杂环境中往往难以恢复精确的3D几何结构和时间一致的运动。为了应对这些挑战，我们提出了MotionScale，这是一个4D高斯泼溅框架，能够高效地扩展到大规模场景和长序列，同时保持高保真的结构和运动一致性。我们方法的核心是一个可扩展的运动场，它通过以聚类为中心的基变换进行参数化，能够自适应地扩展以捕捉多样且演变的运动模式。为了确保长时间内的鲁棒重建，我们引入了一种渐进式优化策略，包含两个解耦的传播阶段：1）背景扩展阶段：适应新出现的可见区域、细化相机位姿并显式建模瞬态阴影；2）前景传播阶段：通过专门的三阶段细化过程强制执行运动一致性。在具有挑战性的真实世界基准测试上进行的大量实验表明，MotionScale在重建质量和时间稳定性方面均显著优于最先进的方法。项目页面：https://hrzhou2.github.io/motion-scale-web/。

## Abstract
Realistic reconstruction of dynamic 4D scenes from monocular videos is essential for understanding the physical world. Despite recent progress in neural rendering, existing methods often struggle to recover accurate 3D geometry and temporally consistent motion in complex environments. To address these challenges, we propose MotionScale, a 4D Gaussian Splatting framework that scales efficiently to large scenes and extended sequences while maintaining high-fidelity structural and motion coherence. At the core of our approach is a scalable motion field parameterized by cluster-centric basis transformations that adaptively expand to capture diverse and evolving motion patterns. To ensure robust reconstruction over long durations, we introduce a progressive optimization strategy comprising two decoupled propagation stages: 1) A background extension stage that adapts to newly visible regions, refines camera poses, and explicitly models transient shadows; 2) A foreground propagation stage that enforces motion consistency through a specialized three-stage refinement process. Extensive experiments on challenging real-world benchmarks demonstrate that MotionScale significantly outperforms state-of-the-art methods in both reconstruction quality and temporal stability. Project page: https://hrzhou2.github.io/motion-scale-web/.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《MotionScale: Reconstructing Appearance, Geometry, and Motion of Dynamic Scenes with Scalable 4D Gaussian Splatting》的论文进行结构化、深入且客观的总结。

---

## **MotionScale 论文深度分析总结**

### **1. 核心问题与整体含义**
*   **研究背景**：从单目视频中逼真地重建动态4D场景（3D几何+时间维度）是计算机视觉的核心挑战，对于自动驾驶、AR/VR和沉浸式远程呈现等应用至关重要。
*   **现有瓶颈**：尽管基于NeRF和3D高斯泼溅的动态场景重建方法取得了进展，但在处理复杂、无约束的真实世界视频时仍面临两大瓶颈：
    1.  **几何约束不足**：主要依赖视图相关的外观信号进行监督，难以保证动态物体严格的3D结构一致性。
    2.  **累积的时间漂移**：运动模型通常依赖缺乏3D感知的2D跟踪先验，在长序列中误差会累积，导致几何崩溃和运动轨迹不一致。
*   **整体含义**：本文旨在解决上述问题，提出一个能够从单目视频中高质量、高一致性重建大规模动态场景外观、几何与运动的可扩展框架。

### **2. 方法论**
*   **核心思想**：在3D高斯泼溅的基础上，构建一个**可扩展的运动场**来驱动一组静态的“规范”3D高斯粒子在时间上演化。通过一种**渐进式优化策略**来稳定地处理长序列视频。
*   **关键技术细节**：
    *   **可扩展的运动场 (Scalable Motion Field)**：
        *   将动态高斯粒子划分为多个空间聚类 (`C_k`)。
        *   每个聚类拥有一个全局刚性变换 (`G_tk`) 和一组局部非刚性基变换 (`B_tk`)。
        *   每个高斯粒子通过一组学习到的权重 (`w_i`) 混合其所属聚类的基变换，得到局部变换 (`L_ti`)，再与全局变换结合得到其在时刻 `t` 的状态（位置 `μ_ti` 和旋转 `R_ti`）。
        *   **自适应控制机制 (Adaptive Control)**：当某个聚类内的运动模式不一致时（如出现非刚性变形），该聚类会被自动分裂成两个新的聚类。新聚类的参数由原参数复制初始化，然后在优化中逐渐分化以捕捉更精细的运动。过小的聚类会被剔除。这使得模型容量能随场景复杂度自适应增长。
    *   **渐进式优化策略 (Progressive Optimization)**：
        1.  **背景扩展阶段 (Background Extension)**：
            *   针对新加入的视频帧，识别并填充之前未观察到的背景区域（利用单目深度图采样新高斯）。
            *   联合优化新增的背景几何和相机外参（通过端到端的光度损失进行轻量级细化）。
            *   引入专门的“阴影高斯”来建模前景物体移动产生的瞬态阴影效果。
        2.  **前景传播阶段 (Foreground Propagation)**：
            *   采用三阶段细化过程将运动场扩展到新帧：
                *   **(1)初始对齐 (Initial Alignment)**：使用单向跟踪损失（从旧帧到新帧），仅优化新帧的运动参数。
                *   **(2)短期一致性 (Short-term Consistency)**：在一个短传播窗口内使用双向跟踪损失，优化窗口内所有帧的运动参数以增强局部时间一致性。
                *   **(3)长期细化 (Long-term Refinement)**：在整个已优化的序列上采样帧对进行全局监督（结合跟踪损失、深度损失、光度损失和ARAP正则化），解决累积漂移并触发自适应控制机制。

### **3. 实验设计**
*   **数据集/场景**：
    1.  **DAVIS**: 包含复杂非刚性运动和自然相机运动的单视角随手拍摄视频。
    2.  **DyCheck**: 包含14个真实世界动态场景的数据集，提供额外的静态相机多视角图像和LiDAR深度数据用于评估。使用其稀疏关键点标注评估长期3D跟踪性能。
    3.  **NVIDIA Dynamic Scenes**: 提供标定的多视角序列，包含多样的人类和物体活动。
*   **对比方法 (Benchmark)**:
    *   ***4D重建质量***: Shape-of-Motion [43], GFlow [46] （在DAVIS上可视化对比）。
    *   ***新视角合成***: T-NeRF [10], HyperNeRF [30], DynIBaR [22], Deformable-GS [53], Dynamic GM [39], Shape of Motion [43]等 （在DyCheck和NVIDIA数据集上进行定量比较）。
    *   ***点跟踪性能***: HyperNeRF, DynIBaR, Deformable-GS, CoTracker+TAPIR+Depth Anything组合方法, DELTA [27], SpatialTracker [50]等 （在DyCheck上进行2D/3D点跟踪定量比较）。

### **4. 资源与算力**
论文中提到：“We implement MotionScale in PyTorch and conduct all experiments on a single NVIDIA RTX 4090 GPU.” （我们在PyTorch中实现MotionScale，并在单个NVIDIA RTX 4090 GPU上进行所有实验）。这表明该方法具有较高的计算效率。

### **5. 实验数量与充分性**
实验设计较为充分且客观公平：
*   ***主实验***: 
    1. DAVIS数据集上的定性4D重建对比（图3）。
    2. DyCheck和NVIDIA数据集上的定量新视角合成对比（表1）。
    3. DyCheck数据集上的定量点跟踪性能对比（表2）。
*   ***消融实验***: 
    在DyCheck上进行了全面的消融研究（表3），验证了各个核心组件的贡献：
        1. “Global Bases”：用全局共享基替换本文的聚类中心基运动场 → PSNR下降明显。
        2. “w/o Adaptive Control”：禁用自适应控制 → PSNR下降明显。
        3. “w/o Pose Ref.”：禁用相机位姿联合优化 → PSNR下降且出现模糊纹理（图4顶部）。
        4. “w/o Shadow”：禁用阴影高斯 → PSNR显著下降且出现前景物体膨胀伪影（图4底部）。
        5. “w/o FG Propagation”：用全局优化替换三阶段前景传播 → PSNR下降明显。

这些实验覆盖了不同任务类型的数据集，并与当前主流方法进行了广泛比较。消融实验系统地验证了每个模块的有效性。遵循了标准评估协议和使用相同输入先验以确保公平性。

### **6.主要结论与发现**
*   MotionScale能够从单目视频中高质量地重建大规模动态场景的外观、几何和时间一致的运动轨迹。
*   其提出的可扩展运动场和三阶段渐进式优化策略有效解决了现有方法在处理长序列时面临的几何失真和时间漂移问题。
*   在多个具有挑战性的真实世界基准测试上均取得了最先进的性能表现。

### **7.**优点
1.**创新性的运动表示**: “聚类中心基变换”的可扩展运动场设计巧妙地将全局刚性与局部非刚性解耦表达能力强且计算高效；自适应控制机制使模型能根据场景复杂度自动调整容量避免了固定架构的限制或冗余。

2.**系统化的渐进式训练策略**: “背景-前景解耦”以及“三阶段细化”的前景传播过程设计严谨有效地平衡了新信息引入与已有知识保持之间的关系显著提升了长序列优化的稳定性和最终结果的时序一致性。

3.**对现实因素的细致建模**: 
	-显式引入“阴影高斯”来处理移动物体的投影避免了阴影区域对前景几何的错误影响提升了视觉真实感
	-联合优化相机位姿弥补了预估计位姿的不精确性

4.**高效性与实用性**: 
	-整个框架可在单个消费级GPU上运行
	-基于高效的3DGS表示实现了高质量的实时渲染潜力

5.**全面且扎实的实验验证**: 
	-涵盖了定性可视化定量渲染质量和定量点跟踪等多个维度
	-消融实验完整有力地支撑了各个技术组件的必要性

###8.**不足与局限**

1.**对预训练先验模型的依赖**: 
	-整个流程严重依赖于外部预训练模型提供的单目深度图(Dt)前景掩码(Mt)密集二维点轨迹(Ut)以及初始相机位姿
	-这些先验的质量直接影响最终的重建效果如果先验存在系统性偏差或噪声(例如复杂遮挡下的错误追踪)可能会限制方法的鲁棒性

2.**潜在的应用范围限制**: 
	-方法主要针对具有明确前景物体的动态场景(如行人车辆)
	-对于高度非结构化或流体状的大规模变形(如烟雾火焰水波)其基于聚类的刚体+局部变形模型可能表达能力不足

3.**初始化阶段的假设**: 
	-使用K-means对三维轨迹点进行初始聚类假设同一聚类的点在规范空间内空间邻近且运动模式相似这在某些复杂交互场景下可能不成立影响后续优化的起点

4.**阈值敏感性风险**: 
	-自适应控制中的分裂决策依赖于距离阈值等超参数虽然文中未详细讨论但这些阈值的选择可能影响最终模型的粒度和收敛行为需要仔细调优

5.**未讨论的计算开销细节**: 
	-虽然总训练在一张GPU上完成但未报告具体每段视频的训练时长这对于评估方法的实际效率仍有欠缺
	-随着序列增长和高斯数量增加内存占用和处理速度的变化趋势也未详细分析

---

**(完)**

好的，我将从上一次总结的末尾继续，对论文的不足与局限、未来工作及整体评价进行补充分析。

---

### **8. 不足与局限（续）**

6. **评估指标的局限性**：
    *   定量评估主要依赖于PSNR、SSIM、LPIPS等图像质量指标和3D点跟踪误差。这些指标虽然标准，但可能无法完全捕捉动态场景重建中至关重要的**时间一致性**和**物理合理性**。例如，一个在视觉上清晰但在时间上“抖动”或运动轨迹不光滑的重建结果，可能仍会获得不错的帧级图像质量分数。
    *   对于几何重建的质量，缺乏直接的3D几何度量（如到真实表面网格的距离），主要依赖渲染图像的间接评估和新视角合成的保真度。

7. **对相机运动的假设**：
    *   方法假设相机运动是连续且平滑的，通过联合优化进行细化。对于存在剧烈、快速或非连续相机运动的视频（如快速切换镜头），该方法可能难以稳定地初始化或优化，导致场景结构崩溃。

### **9. 未来工作方向**
基于论文的讨论和局限性，潜在的未来研究方向包括：
1.  **更强的先验与弱化依赖**：探索更鲁棒或自监督的方式生成所需的先验（如深度、分割、轨迹），减少对特定预训练模型的依赖，提升方法在更广泛、更“野外”数据上的适用性。
2.  **更复杂的运动建模**：扩展当前模型以处理非刚体变形（如衣物褶皱）、流体动力学（如水、烟雾）以及物体间的相互作用力，这可能需要引入物理约束或更高级别的语义理解。
3.  **实时交互与编辑**：利用3D高斯泼溅的实时渲染潜力，开发基于重建的动态场景的实时交互编辑工具，例如改变物体运动路径、添加/删除动态元素等。
4.  **跨场景泛化与终身学习**：研究如何将在一个场景中学到的运动模式或对象知识迁移到新的场景中，或者设计能够持续从流式视频中学习而不遗忘先前内容的系统。

### **10. 整体评价**
本文《MotionScale》是一篇在动态场景重建领域贡献突出、技术扎实的研究工作。其核心贡献在于系统性地解决了现有方法在长序列单目视频重建中的两大关键瓶颈——几何约束不足和时间漂移累积。

*   **技术深度**：提出的“可扩展运动场”是一个优雅而强大的表示法，它通过聚类基变换和自适应控制机制，巧妙地在模型表达能力与优化稳定性之间取得了平衡。渐进式优化策略则体现了一种工程上的严谨性，确保了大规模序列处理的可行性。
*   **实验充分性**：论文在多个具有挑战性的公开数据集上进行了全面实验，涵盖了定性可视化、定量渲染质量和3D跟踪精度等多个维度。详尽的消融研究有力地证实了每个核心组件的必要性。
*   **实用价值**：该方法在单个消费级GPU上实现了高质量的重建，展示了其在实际应用中的潜力。它为从海量单目视频（如互联网视频、行车记录仪影像）中自动化重建动态4D世界迈出了坚实的一步。

尽管存在对预训练先验的依赖等局限性，但MotionScale框架本身具有良好的模块化和可扩展性。它为后续研究提供了一个强大的基线和一个清晰的技术演进方向——即如何在保持高效可扩展的同时，融入更强的物理约束和语义理解以进一步提升重建的逼真度与鲁棒性。

（完）
