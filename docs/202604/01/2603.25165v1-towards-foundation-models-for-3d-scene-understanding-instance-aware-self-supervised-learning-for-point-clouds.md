---
title: "Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds"
title_zh: 迈向三维场景理解的基础模型：面向点云的实例感知自监督学习
authors: "Bin Yang, Mohamed Abdelsamad, Miao Zhang, Alexandru Paul Condurache"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25165v1"
tags: ["query:3d"]
score: 9.0
evidence: 用于3D场景理解的实例感知自监督学习
tldr: 针对现有点云自监督学习方法缺乏实例感知能力、在下游任务（如实例定位）上迁移性能不佳的问题，本研究提出了一个面向实例的自监督框架PointINS。该方法通过一个正交偏移分支联合学习语义理解和几何推理，并设计了偏移分布正则化和空间聚类正则化两种策略来增强表征的鲁棒性。其主要贡献在于显著提升了点云表征的实例感知能力，在室内外多个分割任务上取得了显著性能提升，推动了可扩展3D基础模型的发展。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.25165v1/fig-001.webp\", \"caption\": \"Figure 1. We achieve superior results over state-of-the-art selfsupervised approaches (DOS [2] and Sonata [37]) on both indoor instance (left) and outdoor panoptic segmentation (right).\", \"page\": 1, \"index\": 1, \"width\": 485, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-002.webp\", \"caption\": \"Table 1. Comparison of self-supervised methods on various indoor scene datasets: ScanNet [13], ScanNet200 [31] and S3DIS [22]. For fair comparison, all models deploy the same backbone of Point Transformer v3 (PTv3) [36] and no additional data is used during pre-training. The best and second best results in each setting are highlighted in bold and underline, respectively.\", \"page\": 6, \"index\": 2, \"width\": 872, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-003.webp\", \"caption\": \"Table 2. Comparison of self-supervised methods on the outdoor scene datasets: nuScenes [15] and SemanticKITTI [4].\", \"page\": 6, \"index\": 3, \"width\": 489, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-004.webp\", \"caption\": \"Figure 6. Visualization of predicted centroids ci = xi+Oi (no explicit regularization) from models pre-trained under four configurations. (a) Without regularization, the centroids are scattered irregularly across the scenes. (b) With ODR only, the centroids are aligned more closely with the scene structure but they are not grouped (lack of local coherence). (c) With SCR only, the centroids are partially grouped but spatially scattered. (d) With ODR and SCR, the centroids become tightly concentrated around instances.\", \"page\": 7, \"index\": 4, \"width\": 1006, \"height\": 216}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-005.webp\", \"caption\": \"Figure 8. Qualitative results of unsupervised instance segmentation. Left: ground-truth instance labels. Right: predictions from PointINS obtained directly from offset clustering without any downstream supervision.\", \"page\": 14, \"index\": 5, \"width\": 496, \"height\": 896}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-006.webp\", \"caption\": \"Figure 5. We monitor the linear probing (LP) performance of a model pre-trained with a recent SSL method [37]. Remarkably, the model reaches 85% of its final LP performance within just 10% of the total training.\", \"page\": 4, \"index\": 6, \"width\": 443, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-007.webp\", \"caption\": \"Figure 3. Overview of PointINS: A point cloud is augmented to two independent views and they are randomly masked. The teacher processes the full input and the student receives only visible points. Both networks share the same architecture. In the semantic branch, features are computed similarity with prototypes Q. A KL-divergence loss Lsem is then applied for distillation. In the offset branch, an offset head maps features into 3D offset vectors. Teacher offsets are first regularized by ODR to align with empirically observed geometric priors. Next, segments obtained from K-means-clustering are used to extract pseudo-instance masks. Those masks help to enhance instance awareness by regularizing local coherence of points. Finally, an offset loss Loff is computed as the second distillation.\", \"page\": 4, \"index\": 7, \"width\": 1031, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.25165v1/fig-008.webp\", \"caption\": \"Table 9. Pretraining settings for indoor and outdoor point clouds.\", \"page\": 12, \"index\": 8, \"width\": 489, \"height\": 375}]"
motivation: 现有自监督学习方法在点云语义理解上取得进展，但表征迁移到实例定位任务时表现不佳，而实例感知是迈向真正3D基础模型的关键。
method: 提出PointINS框架，通过正交偏移分支联合学习高级语义理解和几何推理，并引入偏移分布正则化与空间聚类正则化两种互补策略。
result: "在五个数据集上的实验表明，PointINS在室内实例分割上平均提升3.5% mAP，在室外全景分割上提升4.1% PQ。"
conclusion: PointINS通过几何感知学习，为构建支持多种下游任务的可扩展3D基础模型铺平了道路。
---

## 摘要
点云自监督学习的最新进展显著提升了无需人工标注的三维场景理解能力。现有方法通过增强视图间的特征一致性或掩码场景建模来强调语义感知。然而，由此获得的表征在迁移至实例定位任务时效果不佳，且通常需要完全微调才能获得强劲性能。实例感知是三维感知的基本组成部分，因此弥合这一差距对于推进能够支持所有三维数据下游任务的真正三维基础模型至关重要。本文提出PointINS，一种面向实例的自监督框架，通过几何感知学习来丰富点云表征。PointINS采用正交偏移分支联合学习高层语义理解和几何推理，从而获得实例感知能力。我们识别出对鲁棒实例定位至关重要的两个一致性属性，并将其构建为互补的正则化策略：偏移分布正则化（ODR）将预测偏移与经验观察到的几何先验对齐；空间聚类正则化（SCR）通过伪实例掩码对偏移进行正则化以增强局部一致性。通过在五个数据集上的大量实验，PointINS在室内实例分割任务上平均提升3.5% mAP，在室外全景分割任务上平均提升4.1% PQ，为可扩展的三维基础模型开辟了道路。

## Abstract
Recent advances in self-supervised learning (SSL) for point clouds have substantially improved 3D scene understanding without human annotations. Existing approaches emphasize semantic awareness by enforcing feature consistency across augmented views or by masked scene modeling. However, the resulting representations transfer poorly to instance localization, and often require full finetuning for strong performance. Instance awareness is a fundamental component of 3D perception, thus bridging this gap is crucial for progressing toward true 3D foundation models that support all downstream tasks on 3D data. In this work, we introduce PointINS, an instance-oriented self-supervised framework that enriches point cloud representations through geometry-aware learning. PointINS employs an orthogonal offset branch to jointly learn high-level semantic understanding and geometric reasoning, yielding instance awareness. We identify two consistent properties essential for robust instance localization and formulate them as complementary regularization strategies, Offset Distribution Regularization (ODR), which aligns predicted offsets with empirically observed geometric priors, and Spatial Clustering Regularization (SCR), which enforces local coherence by regularizing offsets with pseudo-instance masks. Through extensive experiments across five datasets, PointINS achieves on average +3.5% mAP improvement for indoor instance segmentation and +4.1% PQ gain for outdoor panoptic segmentation, paving the way for scalable 3D foundation models.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds》的论文进行结构化、深入且客观的总结。

---

### **论文总结：PointINS - 面向点云的实例感知自监督学习**

#### **1. 核心问题与整体含义**
*   **研究背景**：自监督学习在点云语义分割上取得了显著进展，但现有方法（如基于对比学习或掩码建模）主要关注**语义一致性**，导致学习到的表征在需要精细几何推理的**实例级任务**（如实例分割、全景分割）上迁移性能不佳。
*   **核心问题**：如何在不依赖人工标注的情况下，让点云自监督学习框架同时具备强大的语义理解能力和**实例感知能力**？这是迈向能够泛化到多种下游任务的通用3D基础模型的关键一步。
*   **整体含义**：本文提出了一种新的自监督学习范式，旨在弥合语义级与实例级表征之间的鸿沟。其核心思想是引入“几何推理”作为与“语义一致性”并行的学习目标，通过精心设计的正则化策略引导模型从无标签数据中自发地学习物体实例的边界和结构。

#### **2. 方法论**
*   **核心思想**：在经典的教师-学生自蒸馏框架基础上，增加一个并行的**偏移分支**。该分支预测每个点指向其所属实例几何中心的3D偏移向量。通过联合优化语义分支和偏移分支，模型能同时学习“是什么”（语义）和“属于哪个个体”（实例）。
*   **关键技术细节**：
    *   **基础架构**：基于DOS框架和轻量化的Point Transformer V3 (PTv3) 主干网络。采用双视图增强、随机掩码、原型聚类进行语义自蒸馏。
    *   **偏移分布正则化 (ODR)**：
        *   **动机**：无监督下直接回归偏移易导致模型崩溃或预测无意义。
        *   **方法**：利用真实场景中观测到的几何先验（偏移大小服从长尾分布，方向近似均匀分布）来约束预测。使用概率积分变换将预测的偏移大小和方向分量映射到目标分布上，保持排序不变性，从而产生几何合理的预测。
    *   **空间聚类正则化 (SCR)**：
        *   **动机**：ODR提供了全局约束，但缺乏局部一致性（同一实例内的点应指向共同中心）。
        *   **方法**：
            1. 对教师网络提取的特征进行K-means聚类，得到粗粒度的语义片段。
            2. 在每个片段内，根据ODR正则化后的预测中心构建k近邻图。
            3. 在图上游走搜索连通分量，将其视为伪实例掩码。
            4. 计算每个伪实例内点的平均坐标作为新的目标中心，并据此生成更准确的目标偏移量来监督学生网络。
    *   **互补性**：ODR提供稳定的全局几何锚点，防止SCR因初始散乱预测而产生错误分组；SCR则利用局部空间一致性细化分组，弥补ODR缺乏局部约束的不足。两者均在教师侧进行，为学生提供稳定、结构化的监督信号。

#### **3. 实验设计**
*   **数据集与场景**：
    *   **室内场景**：ScanNet, ScanNet200, S3DIS (Area-5)。
    *   **室外驾驶场景（LiDAR）**: nuScenes, SemanticKITTI, Waymo Open Dataset（用于多数据集预训练）。
*   **评测基准与任务**：
    *   主要评测任务为需要实例感知能力的下游任务：
        *   室内：`Instance Segmentation` (mAP, AP50, AP25)。
        *   室外：`Panoptic Segmentation` (PQ, SQ, RQ)。
    *   次要评测任务为验证语义保持能力：`Semantic Segmentation` (mIoU)。
*   **对比方法**: 与当时最先进的自监督学习方法进行全面对比：
    *   早期/经典方法: SegContrast, PSA.
    *   近期强基线: NOMAE, Sonata, DOS.
    *   参考上限: PTv3 (全监督训练)。
*   **评估协议**: 严格遵循三种标准协议以评估表征质量：
    1.  `Linear Probing`: 冻结预训练主干网络权重。
    2.  `Decoder Probing`: 冻结编码器权重。
    3.  `Full Finetuning`: 微调全部参数。

#### **4. 资源与算力**
论文在附录中提供了相关信息：
*   `GPU型号`: NVIDIA H200 GPU(s)。
*   `单数据集预训练`: （例如ScanNet）使用单张H200 GPU耗时约25小时（相比基线DOS增加约25%）。
*   `多数据集预训练`: （例如混合多个室外数据集）使用两张H200 GPU以分布式数据并行模式进行训练。
*   总体而言算力需求处于领域内大型模型预训练的常规水平。

#### **5. 实验数量与充分性**
实验设计非常全面、客观且公平：
1.  `主实验对比`：在5个主流数据集上进行了广泛的横向比较（如表1、表2），涵盖了室内外不同场景和多种评估协议。所有对比均使用相同的主干网络(PTv3)以确保公平性。
2.  `消融实验`：（表4）系统地验证了各个组件（偏移损失L_off、ODR、SCR）的必要性和互补性。可视化结果（图6）直观地展示了不同配置下预测中心的差异。
3.  `敏感性分析`：（表5及图7）
    探究了ODR对不同先验分布的鲁棒性（包括完全无标注估计），证明了方法的稳定性；分析了关键超参数λ_off和K的影响；研究了损失预热比例的影响。
4.  `扩展性与兼容性实验`：（表6）
    验证了PointINS可无缝集成到其他SSL框架中；进行了多数据集预训练以探索可扩展性；评估了标签高效学习和跨数据集迁移能力；甚至测试了完全无下游训练的“无监督实例分割”。
5. `客观性与公平性`:
    所有结果均报告标准指标并与多个SOTA基线比较；明确说明了使用的骨干网络和数据规模；承认仍存在与全监督性能的差距。

#### **6.主要结论与发现**
1.PointINS成功地将实例感知能力注入到点云自监督学习中。其学习的表征在下游的室内外实例/全景分割任务上显著优于现有SOTA方法（平均提升约+3-4% mAP/PQ），同时在语义分割上保持竞争力。

2.**两种正则化策略是有效的且互补的**: ODR利用统计先验稳定全局优化; SCR利用特征的空间聚类增强局部一致性;二者结合才能达到最佳效果。

3.**该方法具有良好的泛化性和兼容性**:适用于不同的SSL框架;在多数据集预训练下展现出向基础模型发展的潜力;在极低标注数据(0.1%-1%)下仍能取得优异性能。

4.**证明了“几何推理”可以作为自监督学习的有效目标**,并且能与“语义一致性”目标协同促进整体表征能力的提升。

####7.**优点**
1.**问题定义清晰且有价值**:直指当前点云SSL研究在面向基础模型发展中的关键短板——缺乏对物体个体边界的感知能力。

2.**方法设计精巧且具启发性**:将复杂的无监督实例定位问题分解为可学习的偏移回归+两个具有明确物理/统计意义的正则化项(ODR & SCR)，思路清晰有效。

3.**实验极其充分且令人信服**:不仅在主任务上取得了显著提升还通过大量消融分析验证了各模块的作用机制以及方法的鲁棒性和泛化能力覆盖了从理论验证到实际应用的多个维度

4.**工程实现考虑周全**:将正则化施加于教师侧避免了对学生端梯度更新的直接干扰保证了训练的稳定性

####8.**不足与局限**
1.**性能差距依然存在**:尽管在线性探测设置下取得了巨大进步但与全监督模型的性能仍有明显差距尤其是在复杂的长尾类别或小物体上

2.**对先验分布的轻微依赖**:虽然证明了对不同先验具有鲁棒性但最优性能仍需一个大致合理的分布假设这在一定程度上引入了领域知识

3.**计算开销增加**:相比纯语义SSL方法增加了额外的分支和正则化计算使预训练时间增加了约25%

4.**未来方向限制提及较泛**:文末提到的局限性如扩展到时空数据和更大规模数据是领域共性问题未深入探讨本方法可能存在的特定瓶颈例如如何处理极度稀疏或不完整的点云中的实例关系

**(完)**

好的，我将从您提供的“不足与局限”部分之后继续，补充论文的**未来工作**、**整体评价**以及最终的总结性陈述。

---

#### **9. 未来工作**
论文在结论部分展望了未来的研究方向，主要围绕提升方法的通用性和扩展性：
1.  **扩展到时空数据**：将当前的静态点云实例感知学习框架推广到动态的4D点云序列（如来自连续LiDAR扫描或RGB-D视频的数据），以学习对运动物体和场景动态的理解。
2.  **迈向更大规模的基础模型**：利用更大规模、更多样化的无标签点云数据集进行预训练，进一步验证和提升模型作为3D视觉基础模型的泛化能力。
3.  **探索更复杂的下游任务**：将学习到的实例感知表征应用于更高级的任务，如3D物体检测、场景图生成、具身智能中的交互理解等。
4.  **与多模态学习的结合**：探索如何将点云的几何实例感知与图像、语言等多模态信息对齐与融合，构建更全面的场景理解模型。

#### **10. 整体评价**
本文是一篇在3D计算机视觉自监督学习领域具有重要贡献的高质量工作。
*   **学术价值**：它敏锐地识别并定义了一个关键的研究空白——现有自监督学习方法在实例级任务上的迁移瓶颈。通过提出“实例感知”这一新视角和一套行之有效的正则化方法（ODR+SCR），论文为构建面向多种下游任务的通用3D表征提供了新的思路和技术路径。其方法论设计巧妙，理论动机清晰，实验验证全面且扎实。
*   **实践意义**：PointINS显著提升了自监督预训练模型在实例分割和全景分割等实际应用任务上的性能，且在低标注数据 regime 下表现优异。这有助于降低对昂贵3D标注数据的依赖，推动3D场景理解技术在机器人、自动驾驶、增强现实等领域的落地。
*   **启发性**：论文的核心思想——利用几何先验和空间一致性作为自监督信号——具有普适的启发性。它不仅适用于点云，其理念也可能启发2D图像乃至其他模态的自监督学习研究，以更好地捕获结构化信息。

总而言之，《Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds》提出了一种新颖且强大的点云自监督学习方法PointINS。它通过引入并联合优化偏移预测分支，并辅以精心设计的偏移分布正则化和空间聚类正则化，成功地使模型从无标签数据中自发地学习了物体实例的几何结构。广泛的实验证明了该方法在多个基准测试和任务上均达到了新的 state-of-the-art 性能，同时保持了良好的语义理解能力。这项工作为推动能够泛化到多种任务的通用3D基础模型的发展迈出了坚实的一步。

（完）
