---
title: "GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories"
title_zh: GHOST：基于观测运动恢复结构轨迹的地面投影假设
authors: "Tomasz Frelek, Rohan Patil, Akshar Tumu, Henrik I. Christensen"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20583v1"
tags: ["query:3d"]
score: 10.0
evidence: 通过单目运动恢复结构（SfM）恢复相机轨迹
tldr: 本研究提出了一种名为GHOST的可扩展自监督方法，用于在复杂城市环境中从单目图像分割自动驾驶车辆的可行轨迹。该方法利用大规模行车记录视频，将自车运动作为隐式监督，通过单目运动恢复结构技术重建并投影相机轨迹以自动生成行驶区域标签。这些标签用于训练深度分割网络，使其能在运行时从单张RGB图像预测基于运动条件的路径建议，无需显式建模道路或车道标记。主要贡献在于：使用无约束的互联网数据训练模型，使其能隐式理解场景布局并泛化到不同摄像头配置；在NuScenes数据集上验证了可靠的轨迹预测能力；并通过轻量微调展示了向电动滑板车平台的迁移性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.20583v1/fig-001.webp\", \"caption\": \"Fig. 4. Examples of ground-truth masks generated via our pipeline, illustrating the ego vehicle: (Top Left) executing a sharp right turn, (Top Right) initiating a parallel parking maneuver, (Bottom Left) cruising along a straight highway, and (Bottom Right) performing a lane change.\", \"page\": 4, \"index\": 1, \"width\": 512, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2603.20583v1/fig-002.webp\", \"caption\": \"Fig. 5. Pre-trained model predicted outputs shown in red and ground truth annotations in green.\", \"page\": 6, \"index\": 2, \"width\": 1054, \"height\": 357}, {\"url\": \"assets/figures/arxiv/2603.20583v1/fig-003.webp\", \"caption\": \"Fig. 6. Scooter predictions: Finetuned (FT) and Trained from Scratch (TS)\", \"page\": 6, \"index\": 3, \"width\": 512, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2603.20583v1/fig-004.webp\", \"caption\": \"Fig. 1. Overview of the GHOST self-supervised trajectory estimation pipeline. The framework consists of three main stages: (1) SfM Pipeline: Raw uncalibrated video sequences are processed using COLMAP to extract sparse 3D point clouds and unscaled 6-DoF camera poses. (2) Post-Processing & Mask Generation: Ground points are recovered from the sequence’s sparse reconstruction; camera height is calculated; recovered height is used to project the trajectory onto the ground plane, generating spatially accurate Ground Truth (GT) masks. (3) Model Training: A segmentation model takes a batch of raw RGB images as input and predicts probabilistic spatial masks. The model is trained by calculating a weighted asymmetric loss L 2 between the predicted hypotheses and the auto-generated GT masks.\", \"page\": 2, \"index\": 4, \"width\": 727, \"height\": 375}]"
motivation: 研究旨在为复杂城市环境中的自动驾驶开发一种可扩展的自监督方法，从单目图像中分割出可行的车辆轨迹，无需手动标注。
method: 该方法利用大规模行车记录视频，将记录的自身车辆运动作为隐式监督，通过单目运动恢复结构技术重建相机轨迹，并将其投影到地平面以自动生成行驶区域的空间掩码标签。
result: 在NuScenes数据集上的评估表明该方法能可靠预测轨迹；通过少量微调可迁移至电动滑板车平台，模型能隐式捕捉场景布局、车道拓扑和交叉口结构。
conclusion: 研究表明，通过大规模自车运动蒸馏，能够生成超越已观测轨迹的结构化、可泛化的路径假设，从而通过图像分割实现轨迹假设估计。
---

## 摘要
我们提出了一种可扩展的自监督方法，用于从单目图像中分割自动驾驶在复杂城市环境中的可行车辆轨迹。该方法利用大规模行车记录仪视频，将记录的自身车辆运动作为隐式监督，并通过单目运动恢复结构技术重建相机轨迹，将其投影到地面平面上以生成行驶区域的空间掩码，无需人工标注。这些自动生成的标签用于训练一个深度分割网络，该网络可在运行时从单幅RGB图像预测基于运动条件的路径建议，而无需显式建模道路或车道标记。该模型在多样化的、无约束的互联网数据上训练，隐式捕获了场景布局、车道拓扑和交叉口结构，并能适应不同的相机配置。我们在NuScenes数据集上评估了该方法，证明了其可靠的轨迹预测能力，并进一步展示了通过轻量微调向电动滑板车平台的迁移效果。我们的结果表明，大规模自身运动蒸馏能够产生超越已演示轨迹的结构化且可泛化的路径建议，从而通过图像分割实现轨迹假设估计。

## Abstract
We present a scalable self-supervised approach for segmenting feasible vehicle trajectories from monocular images for autonomous driving in complex urban environments. Leveraging large-scale dashcam videos, we treat recorded ego-vehicle motion as implicit supervision and recover camera trajectories via monocular structure-from-motion, projecting them onto the ground plane to generate spatial masks of traversed regions without manual annotation. These automatically generated labels are used to train a deep segmentation network that predicts motion-conditioned path proposals from a single RGB image at run time, without explicit modeling of road or lane markings. Trained on diverse, unconstrained internet data, the model implicitly captures scene layout, lane topology, and intersection structure, and generalizes across varying camera configurations. We evaluate our approach on NuScenes, demonstrating reliable trajectory prediction, and further show transfer to an electric scooter platform through light fine-tuning. Our results indicate that large-scale ego-motion distillation yields structured and generalizable path proposals beyond the demonstrated trajectory, enabling trajectory hypothesis estimation via image segmentation.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对您提供的论文《GHOST: Ground-projected Hypotheses from Observed Structure-from-Motion Trajectories》进行结构化、深入且客观的总结。

---

### **论文总结：GHOST**

#### **1. 核心问题与整体含义**
*   **研究动机**：自动驾驶中的一个核心问题是理解车辆“可以”和“将会”如何移动。传统方法严重依赖昂贵的人工标注语义地图（如可行驶区域、车道线）或复杂的多传感器标定。然而，互联网上存在海量的行车记录仪视频，其中蕴含了人类驾驶员在真实环境中可行的运动轨迹，这是一种未被充分利用的**隐式监督信号**。
*   **整体含义**：本文提出了一种名为 **GHOST** 的可扩展自监督框架。其核心思想是：从海量、无标注的单目行车视频中，通过几何方法（运动恢复结构，SfM）自动提取自车轨迹作为监督信号，并将其转化为地面投影的掩码标签。利用这些自动生成的标签训练一个分割网络，使其能够仅从单张RGB图像中预测出未来可行的、多模态的轨迹假设区域。该方法旨在减少对人工标注和高精度地图的依赖，直接从视觉数据中学习驾驶行为先验。

#### **2. 方法论**
*   **核心思想**：将“可行轨迹估计”问题重新定义为“图像分割”问题。通过自监督方式生成训练标签（地面投影轨迹掩码），然后训练一个标准的分割网络来预测这些掩码。
*   **关键技术流程（三步流水线）**：
    1.  **SfM与轨迹提取**：
        *   输入：未经标定的单目行车视频序列。
        *   使用经典的SfM工具（COLMAP）处理视频，恢复出稀疏3D点云和相机的6自由度位姿（存在尺度不确定性）。
    2.  **后处理与掩码生成（关键创新）**：
        *   **相机高度估计**：为解决SfM的尺度模糊问题并实现地面投影，需要估计相机离地面的绝对高度。
            *   假设：相机光轴近似平行于路面；相机高度在单个重建序列中恒定。
            *   方法：从稀疏点云中筛选出可能属于地面的点（通过投影到图像下部的ROI区域）。利用这些点的局部3D坐标（深度`zc`和高度`yc`），采用鲁棒的Theil-Sen回归拟合地平面纵向剖面线，其y轴截距即为相机高度估计值 `Hcam`。
        *   **轨迹投影与掩码生成**：将未来一段时间窗口内（如5秒）的相机位姿沿重力方向平移 `Hcam` 距离至地面。为这条地面轨迹线赋予一个与车辆宽度成比例的横向宽度，从而生成一个二维空间掩码作为“真值”。
        *   **数据过滤**：自动过滤掉SfM重建失败或异常的帧（“空帧”）。
    3.  **模型训练**：
        *   **网络架构**：采用标准U-Net架构，编码器使用ResNet34或SegFormer等预训练骨干网络。
        *   **损失函数设计（关键创新）**：
            *   使用加权负对数似然损失 `L(x, y) = -(y - ε) log σ(x) + C`。
            *   该损失函数是**非对称的**：对假阴性（漏掉可行区域）惩罚重（乘以系数 `(1-ε)`），对假阳性惩罚轻。这鼓励模型覆盖所有可能的可行路径区域，而不是仅仅模仿数据集中出现的单一真实轨迹，从而能够预测多模态的轨迹假设。
            *   输出为概率图而非二值图，以保留不确定性信息。

#### **3. 实验设计**
*   **训练数据来源与规模**：
    *   从YouTube收集了约30小时的行车记录仪视频片段。
    *   经过SfM处理和过滤后，生成了约65万帧带有自动标注掩码的训练数据。数据涵盖多种天气、光照、城市环境以及不同的相机型号和安装位置。
*   **评估基准与方法对比**：
    *   **评估数据集/基准测试集**: NuScenes数据集中的OpenLaneV2子集B。利用其高清地图构建可达车道图作为真值进行评估。
    *   **评估指标**: Soft IoU (Ruzicka相似性)，适用于概率输出掩码的比较。
    - 对比方法: 
        1. 内部比较了不同骨干网络 (ResNet34 vs SegFormer)。
        2. 迁移学习实验: 将在汽车数据上预训练的模型在电动滑板车数据上进行微调 (Fine-tuning)，并与从头开始训练的模型 (Trained from Scratch) 进行对比。

#### **4. 资源与算力**
*   论文未明确说明具体的GPU型号、数量及总训练时长等硬件细节和算力消耗。

#### **5. 实验数量与充分性**
*   **实验组别相对有限但聚焦核心验证点**：
    1.  **(主要验证)** GHOST方法在NuScenes数据集上的定量性能评估 (Soft IoU)。
    2.  **(消融/变体)** ResNet34 vs SegFormer骨干网络的性能比较 (结果相近)。
    3.  **(泛化/迁移能力验证)** GHOST预训练模型向电动滑板车领域的迁移学习实验 (微调 vs 从头训练)。
    4.  **(定性分析)**提供了丰富的可视化结果展示模型在多场景下的预测能力 (如直行、转弯、多假设预测、避障等)。
*   ***充分性与客观性评价***:
    - ***优点***:
        1. NuScenes是自动驾驶领域广泛认可的权威数据集之一。
        2. OpenLaneV2提供的HD地图为评估提供了相对客观的结构化真值基础。
        3. “跨域迁移”实验直接有力地支撑了方法的泛化能力和实用性主张。
        4. Soft IoU指标适合概率输出评估且避免了阈值选择的偏差。
        5. NuScenes测试集的地理位置和驾驶规则均未出现在训练集中 (波士顿/新加坡 vs “左舵驾驶环境”)，体现了良好的泛化性。

#### - ***不足***:
    1.**缺乏与其他先进方法的直接定量对比**:没有将GHOST的性能与其他基于分割或多模态预测的方法进行数值比较(例如[18]等工作)。这使得难以判断其在现有技术中的绝对水平或优势程度。

#### - ***公平性***:
    作者承认并详细讨论了基于HD地图进行评估的内在局限性(如地图拓扑不完整无法覆盖所有合法路径静态地图忽略动态障碍物等)。这种讨论增强了分析的客观性和可信度。

#### - ***结论***:
    总体而言实验设计能够有效支撑论文的核心贡献即可扩展的自监督学习和多模态预测能力但若能增加更多横向对比将使论证更加全面有力。

###6.**主要结论与发现**

-大规模无约束行车视频中的自车运动可以作为有效的隐式监督信号用于学习可行的车辆轨迹先验而无需人工标注语义信息或依赖高精地图

-通过将可行轨迹估计建模为图像分割问题并配合非对称损失函数标准的分割网络能够从单一演示样本中学习并推断出多种可能的未来路径假设即具备多模态预测能力

-所提出的几何驱动标签生成管道具有鲁棒性能处理来自不同摄像头配置和环境条件的多样化真实世界视频

-经过大规模汽车数据预训练的模型可以通过少量微调快速适应到其他类型的移动平台如电动滑板车上证明了方法的可迁移性和实用性

###7.**优点**

-**方法论亮点**

-**可扩展性与低成本**:完全利用公开易得的互联网数据进行自监督学习避免了昂贵且耗时的数据标注过程

-**巧妙的问题转化**:将复杂的时空运动规划问题转化为更成熟的图像分割任务简化了建模过程

-**创新的损失函数设计**:非对称加权损失是促使模型产生超越演示样本的多模态预测的关键机制

-**鲁棒的几何后处理**:基于Theil-Sen回归的地面平面拟合和高度估计算法能够应对SfM重建中的噪声和不完整性

-**应用价值亮点**

-**强大的泛化能力**:模型能适应不同的摄像头参数天气条件和地理环境并在未见过的城市场景中表现良好

-**良好的可迁移性**:展示了向其他交通工具平台迁移的有效性对于缺乏结构化数据的非传统自动驾驶领域具有重要价值

###8.**不足与局限**

-**技术局限性**

-**依赖于SfM重建质量**:对于特征稀少动态物体过多或主要为纯平移运动的场景COLMAP可能重建失败导致无法生成有效标签限制了可用数据的比例约65万/100万帧)

-**局部平面假设的限制**:在坡度较大或有起伏的路面上相机高度恒定的假设可能被违反影响地面投影的准确性

-**静态世界假设的限制**:虽然模型在定性实验中表现出一定的动态避障意识但其基本监督信号来自静态场景重建并未显式建模动态物体这可能导致在某些情况下预测出不安全的路径

-**应用限制**

-**实时性未讨论**:整个流水线包括离线SfM处理和在线推理但论文未提及模型的推理速度是否满足实时自动驾驶决策的需求

-**仅限于短期规划**:当前工作聚焦于短时间窗口内的可达区域分割通常为5秒并未扩展到更长期的意图或行为规划

-无法提供精确路径:输出是像素级的概率热图而非矢量化的精确车道中心线或具体路径点这对于下游规划和控制模块可能需要额外的后处理步骤

好的，我将继续从您上次输出的中断处开始，补全对论文《GHOST》的总结。

---

-**仅限于短期规划**:当前工作聚焦于短时间窗口内的可达区域分割通常为5秒并未扩展到更长期的意图或行为规划

-无法提供精确路径:输出是像素级的概率热图而非矢量化的精确车道中心线或具体路径点这对于下游规划和控制模块可能需要额外的后处理步骤

-**评估局限性**

- **真值依赖HD地图**：定量评估依赖于OpenLaneV2提供的高清地图作为“可达区域”真值。然而，高清地图本身可能不完整、过时，且无法反映临时交通规则或动态障碍物造成的即时可达性变化。
- **缺乏行为多样性对比**：虽然模型能预测多模态假设，但评估主要基于静态场景的结构化真值（车道图），未能定量评估其在复杂交互路口（如无保护左转）中对多种合理驾驶行为的覆盖能力。
- **未与端到端规划方法对比**：论文未将GHOST预测的概率图作为输入，与基于规则的或学习型的下游规划器结合，并评估最终规划轨迹的质量（如舒适度、安全性、效率）。

#### **9. 未来展望**
*   **动态场景建模**：整合动态物体（其他车辆、行人）的感知信息，使预测的可行区域能够实时反映环境变化，避免碰撞。
*   **多模态与不确定性量化**：进一步探索如何从概率热图中解析出离散的、可解释的多条轨迹假设，并为每条假设提供置信度，以更好地服务于决策模块。
*   **长期规划集成**：将短期的几何可行先验与更高层次的语义目标（如导航终点）相结合，进行长期的轨迹预测和规划。
*   **传感器融合**：探索如何将纯视觉的GHOST输出与其他传感器模态（如LiDAR点云、雷达）进行融合，以提升在恶劣天气或光照条件下的鲁棒性。

#### **10. 总结**
本文提出的GHOST框架是一项具有创新性和实用价值的工作。它巧妙地利用海量无标注行车视频中的几何信息（SfM轨迹），通过自监督方式生成了“可行运动区域”的监督信号，从而将轨迹预测问题转化为图像分割任务。其核心贡献在于提供了一种可扩展的、不依赖于高精地图或人工标注的数据驱动方法，来学习车辆的运动先验。非对称损失函数的设计使其能够预测多模态的未来可能性，而非仅仅复制历史数据。实验证明了该方法在跨场景、跨平台上的有效泛化能力。尽管存在对SfM质量、静态世界假设以及评估方式的依赖等局限性，但GHOST为自动驾驶的运动理解提供了一个新的、数据高效的范式，并为后续结合动态感知与长期规划的研究奠定了坚实的基础。

（完）
