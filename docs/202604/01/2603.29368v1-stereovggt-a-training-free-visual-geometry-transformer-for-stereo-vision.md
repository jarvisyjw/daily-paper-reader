---
title: "StereoVGGT: A Training-Free Visual Geometry Transformer for Stereo Vision"
title_zh: StereoVGGT：一种用于立体视觉的无训练视觉几何Transformer
authors: "Ziyang Chen, Yansong Qu, You Shen, Xuan Cheng, Liujuan Cao"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29368v1"
tags: ["query:3d"]
score: 9.0
evidence: 立体视觉任务和3D设备的几何知识
tldr: 本文针对立体视觉任务中现有骨干网络因缺乏相机姿态监督而导致的几何知识不足问题，提出StereoVGGT。该方法以预训练了丰富3D先验（包括相机姿态）的Visual Geometry Grounded Transformer为基础，通过引入无需训练的特征调整流程来缓解其直接应用时出现的几何细节退化问题，从而充分利用模型内嵌的相机标定知识。实验表明，基于StereoVGGT的网络在KITTI基准上取得了领先性能，验证了其作为立体视觉高效骨干网络的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29368v1/fig-001.webp\", \"caption\": \"Table 4 Cross-dataset performance comparison of disparity estimation in a monocular setting on binocular stereo datasets. “Gen.”: Generation-based method, “MDE”: Monocular Depth Estimation method, “3R”: 3D Reconstruction method.\", \"page\": 15, \"index\": 1, \"width\": 729, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-002.webp\", \"caption\": \"Table 3 Quantitative comparison of our proposed method and Mono2Stereo on the Mono2Stereo dataset. Bold font indicates the better performance.\", \"page\": 15, \"index\": 2, \"width\": 467, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-003.webp\", \"caption\": \"Fig. 1 Camera focal length serves as a determinative factor in disparity estimation. While existing stereo vision backbones lack targeted learning of camera geometry, causing them to largely neglect this critical prior. Given that VGGT is explicitly trained on 3D geometric priors such as camera poses, we seek to its inherent capacity for encoding camera pose representations. However, we observe that VGGT tends to excessively degrade the structural contours within the feature maps. This smoothing property is architecturally incompatible with the pixel-accurate alignment demands of stereo vision, creating a bottleneck for downstream stereo applications. StereoVGGT integrates the camera geometry knowledge encoded in VGGT while preserving robust feature representation capabilities. It can serves as a highly effective backbone for stereo vision.\", \"page\": 2, \"index\": 3, \"width\": 773, \"height\": 235}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-004.webp\", \"caption\": \"Fig. 4 StereoVGGT architecture. StereoVGGT comprises three main stages. First, EMWM synthesizes a new set of optimized DINO weights by merging the weights of VGGT, DINOv2, and an MDE model, guided by an entropy-based criterion. Second, the patch tokens generated by the re-weighted DINO are concurrently fed into both frozen VGGT Frame Attention (FA) Blocks and an MDE neck. The VGGT FA features subsequently modulate modulate the MDE neck features, thereby achieving a balance between camera-geometry knowledge and fine-grained image-feature representation. Finally, the resulting latent features Xstereovggt are passed through a DPT head to generate the disparity prior dstereovggt. Both the latent features and the disparity prior can then be leveraged in downstream stereo-vision tasks, including stereo conversion and stereo matching.\", \"page\": 7, \"index\": 4, \"width\": 775, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-005.webp\", \"caption\": \"Table 2 Stereo conversion evaluation on Inria 3D Movie dataset.\", \"page\": 14, \"index\": 5, \"width\": 777, \"height\": 191}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-006.webp\", \"caption\": \"Fig. 2 Blue histograms visualize the median and mean camera FOV errors obtained when applying different frameworks. This analysis evaluates camera FOV on the ETH3D dataset.\", \"page\": 3, \"index\": 6, \"width\": 757, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-007.webp\", \"caption\": \"Table 1 Evaluation results on KITTI benchmark. The error threshold is 3 pixel. “Non-occ”: Evaluation on non-occluded pixels, “All”: Evaluation on occluded pixels and non-occluded pixels, “all”: Percentage of outliers averaged over all ground truth pixels, “fg”: Percentage of outliers averaged only over foreground regions, “bg”: Percentage of outliers averaged only over\", \"page\": 13, \"index\": 7, \"width\": 760, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2603.29368v1/fig-008.webp\", \"caption\": \"Fig. 3 VGGT suffers from spatial-structural degradation during its feature processing. (a) Histograms illustrate the SSIM values calculated between the extracted feature maps and the original input images across the ETH3D, KITTI, and Middlebury datasets. (b) Feature maps visualization. The red bounding boxes highlight the vehicle contours extracted by each respective method.\", \"page\": 4, \"index\": 8, \"width\": 777, \"height\": 221}]"
motivation: 现有立体视觉骨干网络（如MDE或VFMs）缺乏明确的相机姿态监督，导致几何知识不足，限制了性能；而预训练包含3D先验（包括相机姿态）的VGGT直接应用效果不佳，因其特征提取时几何细节退化严重。
method: 提出StereoVGGT，利用冻结的VGGT并引入无需训练的特征调整流程来缓解几何退化，同时挖掘模型内嵌的相机标定知识。
result: 基于StereoVGGT的立体匹配网络在KITTI基准测试中取得了所有已发表方法中的第一名。
conclusion: StereoVGGT通过引入无需训练的特征调整流程，有效缓解了几何细节退化问题，并充分利用了预训练模型中的相机标定知识，从而成为立体视觉任务的高效骨干网络。
---

## 摘要
随着三维设备的发展，包括立体匹配和立体转换在内的立体视觉任务已成为一个关键的研究前沿。当前的立体视觉骨干网络通常依赖于单目深度估计模型或视觉基础模型。关键在于，这些模型主要是在没有明确相机姿态监督的情况下进行预训练的。鉴于此类几何知识对于立体视觉不可或缺，缺乏明确的空间约束构成了现有架构的一个显著性能瓶颈。考虑到视觉几何基础Transformer是在包含相机姿态在内的大量三维先验知识上预训练的基础模型，我们研究了其作为立体视觉任务鲁棒骨干网络的潜力。然而，实证结果表明，将其直接应用于立体视觉效果欠佳。我们观察到VGGT在特征提取过程中存在更显著的几何细节退化问题。这种特性与双目立体视觉的要求相冲突，从而限制了其在相关任务中的效能。为了弥合这一差距，我们提出了StereoVGGT，一个专为立体视觉定制的特征骨干网络。通过利用冻结的VGGT并引入无需训练的特征调整流程，我们减轻了几何退化问题，并利用了模型中嵌入的潜在相机标定知识。基于StereoVGGT的立体匹配网络在KITTI基准测试中取得了所有已发表方法中的第一名成绩，验证了StereoVGGT可作为立体视觉的高效骨干网络。

## Abstract
Driven by the advancement of 3D devices, stereo vision tasks including stereo matching and stereo conversion have emerged as a critical research frontier. Contemporary stereo vision backbones typically rely on either monocular depth estimation (MDE) models or visual foundation models (VFMs). Crucially, these models are predominantly pretrained without explicit supervision of camera poses. Given that such geometric knowledge is indispensable for stereo vision, the absence of explicit spatial constraints constitutes a significant performance bottleneck for existing architectures. Recognizing that the Visual Geometry Grounded Transformer (VGGT) operates as a foundation model pretrained on extensive 3D priors, including camera poses, we investigate its potential as a robust backbone for stereo vision tasks. Nevertheless, empirical results indicate that its direct application to stereo vision yields suboptimal performance. We observe that VGGT suffers from a more significant degradation of geometric details during feature extraction. Such characteristics conflict with the requirements of binocular stereo vision, thereby constraining its efficacy for relative tasks. To bridge this gap, we propose StereoVGGT, a feature backbone specifically tailored for stereo vision. By leveraging the frozen VGGT and introducing a training-free feature adjustment pipeline, we mitigate geometric degradation and harness the latent camera calibration knowledge embedded within the model. StereoVGGT-based stereo matching network achieved the $1^{st}$ rank among all published methods on the KITTI benchmark, validating that StereoVGGT serves as a highly effective backbone for stereo vision.

---

## 论文详细总结（自动生成）

# StereoVGGT 论文结构化总结

## 1. 核心问题与整体含义
*   **研究背景**：立体视觉（如立体匹配、立体转换）是3D感知的基础。当前主流方法依赖单目深度估计模型或视觉基础模型作为特征骨干网络。
*   **核心问题**：这些骨干网络在预训练时缺乏对**相机姿态**等显式几何知识的监督。而根据几何关系（视差 d = f·B / z），相机焦距 `f` 是决定视差的关键因素。因此，现有骨干网络因缺乏相机几何先验知识，成为性能瓶颈。
*   **初步探索与发现**：作者发现，一个在大量3D先验（含相机姿态）上预训练的视觉几何基础Transformer模型虽然具备丰富的相机知识，但其直接应用于立体视觉任务时效果不佳。原因是VGGT在特征提取过程中会过度平滑图像结构，导致**几何细节严重退化**，这与立体视觉任务对像素级精确对齐的要求相悖。
*   **研究目标**：因此，本文旨在设计一个专为立体视觉定制的特征骨干网络，它需要既能利用VGGT内嵌的相机几何知识，又能避免其带来的特征退化问题。

## 2. 提出的方法论
*   **核心思想**：提出 **StereoVGGT**，一个无需训练的特征骨干网络。其核心是通过一种无需额外数据训练的方式，将VGGT的相机几何知识与MDE/VFM模型的精细特征保持能力进行融合。
*   **关键技术细节与流程**：
    1.  **熵最小化权重合并 (EMWM)**：
        *   动机：VGGT（强相机知识但特征退化）、MDE模型（弱相机知识但特征清晰）、DINOv2（基础模型）共享相同的DINO架构但权重不同。
        *   方法：将三个模型的DINO权重视为“任务向量”，通过线性组合合成新的权重 `θ_stereovggt`。组合系数 `λ_vggt` 和 `λ_mde` 的优化目标是最小化合成权重的信息熵（即最大化确定性），以此实现互补优势的最佳融合。整个过程是数据无关且无需梯度训练的迭代优化。
    2.  **双分支特征颈设计**：
        *   将EMWM得到的重加权DINO模块提取的特征令牌，同时输入两个分支：
            *   **VGGT帧注意力分支**：利用冻结的VGGT帧注意力块处理特征，并融入相机令牌 (`ct`)，生成富含相机几何知识的特征 `X_FA`。
            *   **MDE颈分支**：使用MDE模型的颈结构处理特征，保留精细的空间细节。
        *   通过特征减法进行调制：最终特征 `X_stereovggt = MDE_Neck_Features - α · X_FA`。这种设计基于观察：VGGT注意力激活值与距离正相关（类似视差图），而MDE深度值与距离负相关，减法操作能有效整合两者。
    3.  **下游任务适配**：
        *   **立体匹配**：将StereoVGGT提取的左右视图特征输入现有的IGEV-Stereo解码器（CGEV + ConvGRU）进行视差计算和迭代优化。仅训练解码器，StereoVGGT主干保持冻结。
        *   **立体转换**：将StereoVGGT生成的视差先验输入基于VAE的图像修复模型来生成右视图。整个流程同样无需训练。

## 3. 实验设计
*   **主要数据集与Benchmark**：
    *   **立体匹配评估**：
        *   KITTI Stereo Benchmark (在线排行榜)：主要评估指标为3像素误差阈值下的异常值百分比 (Non-occ, All)。
        *   Scene Flow Dataset: 评估终点误差 (EPE)和1像素阈值下的误差率 (1 px)。
    *   **立体转换评估**：
        *   Inria 3D Movie Dataset: RMSE, SSIM, SIoU, PSNR.
        *   Mono2Stereo Dataset: RMSE, SSIM, SIoU, PSNR.
    *   **消融与分析评估**: ETH3D, Middlebury, KITTI等数据集用于分析单目条件下的视差估计、相机FOV估计误差、结构相似性(SSIM)等。
*   **对比方法/基线/消融对象**:
    - SOTA立体匹配方法: PromptStereo, MonSter, IGEV-Stereo系列等。
    - SOTA立体转换方法: StereoDiffusion, StereoCrafter, Mono2Stereo等。
    - Backbone替换对比: DINOv2、Depth Anything V2 (DAv2)、Moge-2、原始 VGGT、MobileNet V2、EfficientNet V2等作为IGEV-Stereo或BridgeDepth框架的特征主干进行对比。

## 4. 资源与算力
论文中未明确说明训练所使用的具体GPU型号、数量及总训练时长。仅在附录B.3中提及了推理阶段的硬件配置和耗时比较：
> “All experiments were conducted on a single NVIDIA RTX 3090 GPU.”
> “The time reported in the table represents the average duration over 50 inference runs.”

这表明实验是在单张RTX 3090 GPU上进行的推理速度测试。

## 5. 实验数量与充分性
论文进行了大量且全面的实验组别：

1.  **主实验 (两大任务)**:
    - KITTI和Scene Flow上的立体匹配性能测试 (SOTA对比 + Backbone替换)。
    - Inria Movie和Mono2Stereo数据集上的立体转换性能测试。

2.  **深入的消融研究 (四组关键问题)**:
    1) StereoVGGT自身特征的鲁棒性评估（单目输入+已知内参直接预测视差）。
    2) EMWM策略的有效性验证（对比不同权重初始化）。
    3) MDE Neck与 VGGT Neck双分支设计的贡献分析。
    4) Hyperparameter α的影响分析。

3.  **补充实验 (附录)**:
    - Camera Pose Estimation精度验证。
    - Generalization to Other Frameworks (BridgeDepth框架)。
    - Computational Cost分析 (参数量、FLOPs、推理时间)。
    - Ill-posed Conditions下的表现分析。

*结论*：实验设计非常充分且系统化。不仅在主任务上与SOTA方法进行了公平比较（使用相同解码器框架），还通过详尽的消融实验深入剖析了每个组件的贡献和作用机理。附录中的补充实验进一步增强了结论的说服力。

##6.主要结论与发现

- Stereovggt成功地解决了现有立體視覺骨幹網絡缺乏相機幾何知識以及vggt特徵退化的雙重問題
-通過無需訓練的emwm策略和雙分支特徵頸設計 stereovggt能夠在保持精細空間細節的同時融入強大的相機標定先驗知識
-基於stereovggt構建的立體匹配網絡在kitti基準測試的非遮擋像素評估中取得了所有已發表方法的第壹名證明了其作為立體視覺骨幹網絡的有效性
-基於stereovggt構建的立體轉換網絡也在inria movie和monostereo數據集上達到了sota性能展示了其通用性

##7優點

-創新性地識別並解決了壹個關鍵瓶頸現有骨幹網絡缺乏相機幾何知識而具備該知識的vggt又存在特徵退化問題洞察深刻
-提出的emwm策略巧妙且高效無需額外數據或訓練僅通過優化權重合並系數即可融合不同模型的優勢實現了真正的trainingfree adaptation
-整體架構設計清晰雙分支調制機制直觀地利用了不同模塊輸出特性的互補關系公式推導嚴謹實驗驗證全面
-不僅在主任務上取得sota還進行了大量深入的消融分析和補充實驗對方法的有效性提供了強有力的支撐結論可信度高

##8不足與局限

-計算成本較高盡管推理時間增加不多但stereovggt繼承了vggt龐大的參數量總參數約億flops也較高這可能限制其在資源受限環境中的部署論文也承認這是未來需要改進的方向模型壓縮與蒸餾
-雖然在kittiscene flow等多個數據集上進行了評估但這些數據集主要涵蓋駕駛場景對於其他領域如室內場景醫療影像等的泛化能力有待進壹步驗證
-超參數α的值通過實驗手動確定為未來可以探索基於學習的自適應確定方法以進壹步提升性能或適應不同場景
-盡管聲稱trainingfree但emwm過程本身需要迭代優化權重合並系數這雖然不涉及梯度下降和標註數據但仍需要計算開銷嚴格來說是種無監督的參數優化過程

完

## 9. 潜在影响与未来方向

*   **对立体视觉领域的影响**：
    *   **重新审视骨干网络的作用**：本文明确指出并验证了相机几何先验对于立体视觉任务的重要性，挑战了当前依赖通用视觉基础模型（VFM）或单目深度估计（MDE）模型作为骨干网络的范式。这可能会促使后续研究更多地关注如何将显式几何约束或标定知识高效地整合到特征提取阶段。
    *   **提供了一种“即插即用”的强先验模块**：StereoVGGT作为一个冻结的、无需训练的特征提取器，可以方便地集成到现有的立体匹配、深度估计乃至3D重建等任务的框架中，为这些任务提供一个强大的几何感知特征基础。
    *   **开辟了“权重合并”的新应用场景**：EMWM策略展示了如何通过信息论目标（熵最小化）来融合不同预训练模型的专长，为模型融合、知识迁移领域提供了一个新颖且高效的思路，可能启发更多无需训练的任务适配方法。

*   **未来研究方向**：
    1.  **效率优化**：如论文所述，降低模型参数量和计算复杂度是首要方向。未来的工作可以探索对StereoVGGT进行知识蒸馏、剪枝或设计更轻量化的双分支结构。
    2.  **泛化能力拓展**：在更广泛和更具挑战性的场景（如无纹理区域、透明物体、动态场景、室内环境）中验证其鲁棒性。研究如何使模型适应在线标定或自标定场景。
    3.  **自适应机制**：探索动态或可学习的超参数α，使其能根据输入图像内容或任务需求自动调整调制强度。
    4.  **扩展到其他任务**：将这种融合几何先验的思想应用于其他密集预测任务，如光流估计、场景流估计、3D目标检测等，检验其通用性。
    5.  **理论深化**：进一步从理论上分析EMWM为何能有效融合不同模型的优势，以及特征减法调制背后的数学原理。

## 10. 总结评价

总体而言，《StereoVGGT》是一篇高质量的研究论文。它从一个清晰且重要的洞察出发——现有立体视觉骨干网络缺乏关键的相机几何先验。作者没有停留在问题分析层面，而是创造性地提出了一套完整且精巧的解决方案。

该方法的核心创新点在于其“无需训练”的特性：通过熵最小化权重合并（EMWM）巧妙地“组装”了一个兼具VGGT相机知识和MDE细节保持能力的新骨干网络权重；再通过双分支特征颈的减法调制，直观而有效地整合了两种互补的特征表示。整个流程避免了繁琐的端到端再训练，实现了高效的知识迁移。

实验部分堪称典范，不仅在主任务上取得了具有竞争力的SOTA结果（尤其在KITTI基准上表现突出），还设计了多层次、多角度的消融与分析实验，有力地验证了每个组件的必要性和有效性。论文写作清晰，逻辑连贯。

当然，方法在计算效率上的代价和在某些极端条件下的泛化能力是其当前的局限性。然而，这些不足在文中已被明确指出，并作为未来的改进方向。这项工作为立体视觉乃至更广泛的几何感知任务提供了一个新的强大工具和富有启发性的研究思路。

（完）
