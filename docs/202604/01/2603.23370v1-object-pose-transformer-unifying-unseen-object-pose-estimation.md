---
title: "Object Pose Transformer: Unifying Unseen Object Pose Estimation"
title_zh: Object Pose Transformer：统一未见物体的姿态估计
authors: "Weihang Li, Lorenzo Garattoni, Fabien Despinoy, Nassir Navab, Benjamin Busam"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23370v1"
tags: ["query:3d"]
score: 9.0
evidence: 3D视觉中的物体姿态估计
tldr: 针对未见物体的模型自由三维姿态估计难题，现有方法分为依赖预定义分类的类别级绝对姿态估计和无法恢复单视图绝对姿势的相对姿势估计两个割裂的范式。本研究提出了物体姿势变换器（OPT），一个统一的前馈框架，通过联合预测深度、点图、相机参数和归一化物体坐标，将这两种范式桥接起来。它利用对比性物体中心潜在嵌入进行规范化而无需推理时的语义标签，并通过跨帧特征交互提升单视图预测精度。该模型与相机无关，支持纯RGB输入并可选深度输入以恢复度量尺度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.23370v1/fig-001.webp\", \"caption\": \"Figure 1. Unified unseen object pose estimation. OPT-Pose utilizes a feed-forward transformer to predict point map, depth, NOCS, and camera parameters. Existing category-level methods predict canonical absolute 9-DoF SA(3) poses (equivalent to Depth + NOCS), but require predefined category labels and calibrated cameras. Relative pose methods align unseen objects across views in 6-DoF SE(3) (equivalent to Pointmap + Depth), but do not support single-view absolute pose prediction. OPT-Pose enables the simultaneous recovery of both unseen-object relative and category-level absolute poses (right-most column) for flexible single or multi-view RGB or RGB-D input, without the need for CAD models or semantic labels.\", \"page\": 1, \"index\": 1, \"width\": 519, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2603.23370v1/fig-002.webp\", \"caption\": \"Figure 4. Qualitative result in absolute pose estimation with RGB-D input. We showcase some difficult instances in comparison with AG-Pose[32]. We zoom in on the difficult object categories, the shoe and the box, for better visualization.\", \"page\": 7, \"index\": 2, \"width\": 983, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2603.23370v1/fig-003.webp\", \"caption\": \"Figure 3. Qualitative result in relative pose estimation. We compare different object instances across different scenes with Oryon [7]. Visualization shows that our OPT-pose can estimate the relative object poses across different objects and scenes.\", \"page\": 7, \"index\": 3, \"width\": 961, \"height\": 425}, {\"url\": \"assets/figures/arxiv/2603.23370v1/fig-004.webp\", \"caption\": \"Figure 2. OPT-Pose overview. A multiview transformer aggregates image tokens and emits predictions from light heads: camera parameters, depth, and point maps for camera-space geometry; a multi-view-keypoint-centric module fuses RGB and 3D features to discover object keypoints, predict NOCS coordinates, and build an object latent embedding. Absolute pose (SA(3)) and relative pose (SE(3)) are recovered in a single forward pass. Optional sensor depth provides metric scale, while the system remains fully functional in RGB-only mode.\", \"page\": 3, \"index\": 4, \"width\": 1034, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2603.23370v1/fig-005.webp\", \"caption\": \"Table 2. Category-level absolute pose estimation (RGB-D) on HouseCat6D. Models are trained on respective training sets and evaluated on the test set. Prior columns indicate: Category (predefined category input), Shape (shape prior), Semantic (semantic prior), and Calibration (camera intrinsics). MV indicates whether the method supports multi-view pose reasoning. Bold and underlined denote the best and second-best results, respectively. We additionally report OPT-Pose with multi-view enhanced inference (S=2, 3, 4), which is only applicable to our method and demonstrates its ability to leverage cross-view geometric cues for absolute pose estimation.\", \"page\": 8, \"index\": 5, \"width\": 1026, \"height\": 315}]"
motivation: 现有方法在未见物体的模型自由姿态估计上存在局限：类别级方法依赖预定义分类且无法处理单视图绝对姿态，而相对姿态方法则无法恢复单视图绝对姿态。
method: 该方法提出了物体姿态变换器（OPT），这是一个前馈框架，通过联合预测深度、点图、相机参数和归一化物体坐标，统一了类别级绝对姿态和未见物体相对姿态的估计。
result: 在多个基准测试上的广泛实验表明，该方法在统一的架构内，于绝对和相对姿态估计任务上都达到了最先进的性能。
conclusion: 该研究通过一个统一的模型架构，在绝对和相对姿态估计任务上均实现了最先进的性能，为未见物体的姿态估计提供了一个强大而灵活的解决方案。
---

## 摘要
学习针对未见实例的无模型物体姿态估计，仍然是三维视觉中的一个基本挑战。现有方法通常分为两个互不相交的范式：类别级方法在规范空间中预测绝对姿态，但依赖于预定义的分类体系；而相对姿态方法估计跨视图的变换，但无法恢复单视图的绝对姿态。在本工作中，我们提出了物体姿态变换器（\ours{}），这是一个统一的前馈框架，通过单个模型内的任务分解来桥接这些范式。\ours{} 从 RGB 输入中联合预测深度、点图、相机参数和归一化物体坐标（NOCS），从而能够同时实现类别级的绝对 SA(3) 姿态和未见物体的相对 SE(3) 姿态。我们的方法利用基于对比学习的以物体为中心的潜在嵌入进行规范化，在推理时无需语义标签，并使用点图作为相机空间表示以实现多视图相对几何推理。通过跨帧特征交互和共享的物体嵌入，我们的模型利用跨视图的相对几何一致性来改进绝对姿态估计，减少单视图预测中的歧义。此外，\ours{} 与相机无关，能够在线学习相机内参并支持可选的深度输入以恢复度量尺度，同时在纯 RGB 设置下仍能完全正常工作。在多个基准数据集（NOCS、HouseCat6D、Omni6DPose、Toyota-Light）上的大量实验表明，该单一统一架构在绝对和相对姿态估计任务中均达到了最先进的性能。

## Abstract
Learning model-free object pose estimation for unseen instances remains a fundamental challenge in 3D vision. Existing methods typically fall into two disjoint paradigms: category-level approaches predict absolute poses in a canonical space but rely on predefined taxonomies, while relative pose methods estimate cross-view transformations but cannot recover single-view absolute pose. In this work, we propose Object Pose Transformer (\ours{}), a unified feed-forward framework that bridges these paradigms through task factorization within a single model. \ours{} jointly predicts depth, point maps, camera parameters, and normalized object coordinates (NOCS) from RGB inputs, enabling both category-level absolute SA(3) pose and unseen-object relative SE(3) pose. Our approach leverages contrastive object-centric latent embeddings for canonicalization without requiring semantic labels at inference time, and uses point maps as a camera-space representation to enable multi-view relative geometric reasoning. Through cross-frame feature interaction and shared object embeddings, our model leverages relative geometric consistency across views to improve absolute pose estimation, reducing ambiguity in single-view predictions. Furthermore, \ours{} is camera-agnostic, learning camera intrinsics on-the-fly and supporting optional depth input for metric-scale recovery, while remaining fully functional in RGB-only settings. Extensive experiments on diverse benchmarks (NOCS, HouseCat6D, Omni6DPose, Toyota-Light) demonstrate state-of-the-art performance in both absolute and relative pose estimation tasks within a single unified architecture.

---

## 论文详细总结（自动生成）

# Object Pose Transformer (OPT-Pose) 论文总结

## 1. 核心问题与整体含义
*   **研究背景**：在三维视觉中，针对**未见物体实例**（即没有CAD模型先验）的**无模型姿态估计**是一个基础且具有挑战性的任务。这对于机器人操作、增强现实和自动驾驶等应用至关重要。
*   **现有方法的局限**：现有方法形成了两个割裂的范式：
    1.  **类别级绝对姿态估计**：预测物体在规范空间（如NOCS）中的9自由度SA(3)姿态（旋转、平移、各向异性缩放）。但这类方法严重依赖预定义的物体类别标签和分类体系，限制了其在开放词汇场景下的泛化能力。
    2.  **相对姿态估计**：估计同一未见物体在不同视图间的6自由度SE(3)变换。这类方法无需类别标签，但缺乏对物体的“规范化”定义，因此无法从单张图像中恢复物体的绝对姿态。
*   **研究动机与目标**：本文旨在**统一上述两个互补的范式**，提出一个单一的、模型无关的框架，能够同时处理类别级绝对姿态和未见物体相对姿态估计，并利用多视图几何一致性来提升单视图预测的精度。

## 2. 方法论
*   **核心思想 - 任务分解与几何互补机制**：
    *   提出通过联合预测一组互补的几何信号来实现统一：深度图、点图（相机空间3D坐标）、相机参数和归一化物体坐标（NOCS）。
    *   **深度 + NOCS → 绝对SA(3)姿态**：将相机空间的观测（深度）与规范空间的对应关系（NOCS）对齐，可恢复旋转、平移和各向异性缩放。
    *   **深度 + 点图 → 相对SE(3)姿态**：两个相机空间的3D表示可以直接进行跨帧对齐，无需规范空间定义。
*   **关键技术细节与流程**：
    1.  **统一的前馈多视图Transformer架构**：一个主干网络处理多帧RGB图像，生成共享的图像token。多个轻量级任务头并行输出：
        *   相机头：预测每帧的内参和外参。
        *   深度头 & 点图头：分别预测密集深度图和点图。
        *   关键点头 & NOCS头：
            *   从采样的前景像素中提取视觉和几何特征。
            *   通过可学习的查询进行注意力计算，生成一组M个潜在关键点的软热图及其对应的相机空间3D位置 `X_obj`。
            *   一个视觉-几何融合块聚合关键点局部邻域信息，生成增强的关键点描述符 `F_obj`。
            *   NOCS头以关键点特征和全局物体潜在嵌入 `z_obj` （通过FiLM调制）为条件，预测每个关键点在规范空间中的坐标 `C`。
        *   尺度恢复头：（可选）当有传感器深度时，预测绝对平移和尺寸；纯RGB模式下回归一个对数尺度先验。
    2.  **对比学习的物体潜在嵌入 (`z_obj`)**：
        *   通过对所有输入视图的关键点特征进行池化得到 `z_obj`。
        *   使用监督InfoNCE损失进行训练，使同一实例或类别的样本在潜在空间中靠近。这使得模型能够在推理时实现“类别无关的规范化”，而无需输入语义标签。
    3.  **姿态计算流程**：
        *   **绝对姿态 (SA(3))**：使用加权最小二乘（如Umeyama算法）对齐预测的关键点NOCS坐标 `C` 和其对应的相机空间坐标 `X_obj`。
        *   **相对姿态 (SE(3))**：
            a.  首先计算一个Sim(3)变换 `Sa`，将锚帧的点图对齐到其深度衍生的相机点云上（解决单目尺度歧义）。
            b.  然后计算一个SE(3)变换 `T_a→q`，将经过 `Sa` 校准后的查询帧点图对齐到查询帧的相机点云上。

## 3. 实验设计
*   **评估任务与基准数据集**：
    1.  **类别级绝对姿态估计 (RGB)**:
        *   **数据集**: REAL275 (CAMERA/REAL)。
        *   **指标**: NIoU, (5°/10°,0.2d/0.5d)等尺度无关指标。
    2.  **类别级绝对姿态估计 (RGB-D)**:
        *   **数据集**: HouseCat6D, Omni6DPose (ICCV2025挑战赛)。
        *   **指标**: (5°/10°,2cm/5cm), IoU50/75, AUC, VUS等度量尺度指标。
    3.  **未见物体相对姿态估计 (RGB-D)**:
        *   **数据集**: NOCS-REAL275, Toyota-Light.
        *   **指标**: ADD(-S), AR, MSSD, MSPD, VSD.
*   **对比方法**:
    *   **绝对姿态**: MSOS, OLD-Net, DMSR, LaPose, GIVE-Pose, UniDet; DPDN, VI-Net, SecondPose, AG-Pose, Sphere-Pose, Spot-Pose, GCE-Pose; GenPose++, CPPF++.
    *   **相对姿态**: SIFT+RANSAC, ObjectMatch, Oryon [7], Any6D [23], One2Any.

## 4. 资源与算力
论文未明确说明训练所使用的具体GPU型号、数量及总训练时长。仅提及了优化器参数（AdamW）、学习率调度策略（warm-up + cosine decay）、混合精度训练等技术细节。

## 5.实验数量与充分性
实验设计较为全面和客观：
*   **主实验数量**:在至少4个不同的主流数据集上进行了评估，覆盖了两种主要的任务范式（绝对/相对）和两种输入模态（RGB/RGB-D）。
*   **消融实验**:系统地验证了核心组件的必要性，包括：
    1.  移除对比学习的物体潜在嵌入 (`z_obj`) 。
    2.  移除点图头（验证其对绝对姿态任务无负面影响）。
    3.  移除绝对度量头并用测试时Umeyama替代。
    4.  用密集预测Transformer替代关键点提取模块。
    5. （隐含在多视图推理结果中）验证多视图信息对提升单视图绝对姿态的作用。
*   ***公平性***:在与各类SOTA方法比较时，清晰标注了对方方法是否使用了额外的先验信息（如类别标签、形状先验、语义先验、标定参数），并在自身方法中强调了“不依赖这些先验”的特性。这使比较更具参考价值。

##6 .主要结论与发现
1.**统一框架的有效性**: OPT-Pose首次在一个单一的前馈模型中成功统一了类别级绝对姿势和未见物体相对姿势估计这两个任务 ，并通过任务分解实现了性能互补 。
2.**最先进的性能**:在多个基准测试上 ，OPT-Pose在两种任务上都达到或超越了当前最先进方法的性能 ，特别是在严格的评价阈值下表现突出 。
3.**多视图推理的优势**:即使在不重新训练的情况下 ，仅通过输入更多视图进行推理 ，也能显著提升单视角下的绝对姿势估计精度 ，证实了跨视角几何一致性约束的有效性 。
4.**灵活性与泛化能力**:该模型是类别无关 、相机无关的 ，支持从纯RGB到RGB-D 、从单视角到多视角的灵活输入 ，展现了强大的泛化能力 。

##7 .优点
1.**范式创新性**:创造性地通过“深度+NOCS”和“深度+点图”两对互补的几何信号分解并统一了两个长期割裂的研究方向 。
2.**设计精巧且高效**:采用基于关键点的表示而非密集回归 ，降低了计算开销和对噪声的敏感性 ；共享的多视图Transformer主干和轻量级任务头的设计实现了高效的多任务学习 。
3.**强泛化性与实用性**:摆脱了对CAD模型 、预定义类别标签 、固定相机内参等强先验的依赖 ，更贴近真实世界的开放场景应用需求 。支持多模态 、多视角输入增加了其实用价值 。
4.**详实全面的评估**:在多样化的数据集和任务上进行广泛测试 ，并进行了深入的消融分析 ，充分验证了方法的各个组成部分及其有效性 。

##8 .不足与局限
1.**输入要求限制**:如文中所述 ，模型需要以物体为中心的裁剪图像作为输入 。这在实际全场景应用中需要一个前置的目标检测或分割步骤 。
2.**对极端条件的敏感性:**性能在光照发生剧烈变化时会下降 。此外 ，对于对称物体或数据集中标注规范不一致的情况 ，模型的规范化过程可能产生歧义 。
3.**未探索的方向:**论文承认未来可以在大规模预训练 、场景中多物体联合推理以及与机器人操作任务的端到端集成等方面进行进一步探索 。当前的实现仍是针对单个物体的 。

---
**(完)**

## 9. 核心贡献与未来展望
*   **核心贡献**：
    1.  **范式统一**：首次提出一个统一的、模型无关的框架，能够同时处理类别级绝对姿态估计和未见物体相对姿态估计这两个长期割裂的任务。
    2.  **几何互补机制**：通过联合预测深度图、点图、NOCS坐标和相机参数这组互补的几何信号，实现了从单视图到多视图、从绝对到相对姿态的自然转换与相互增强。
    3.  **类别无关的规范化**：引入基于对比学习的物体潜在嵌入 (`z_obj`)，使模型能够在无需语义类别标签的情况下学习物体的规范表示，实现了真正的开放世界泛化。
    4.  **高效的多视图架构**：设计了一个基于Transformer的多视图编码器与轻量级任务头，支持灵活的单视图/多视图推理，并利用几何一致性提升精度。
*   **未来展望**：
    1.  **扩展到全场景**：当前方法需要以物体为中心的裁剪图像作为输入。未来的工作可以探索将物体检测或分割模块集成到端到端的流程中，直接处理完整的场景图像。
    2.  **应对极端条件**：进一步提升模型在剧烈光照变化、严重遮挡和对称物体等挑战性条件下的鲁棒性。
    3.  **利用大规模预训练**：探索在更大规模、更多样化的数据集上进行预训练，以增强模型的泛化能力和表征学习。
    4.  **多物体联合推理**：从处理单个物体扩展到同时估计场景中多个物体的姿态，并考虑它们之间的空间关系。
    5.  **机器人应用集成**：将姿态估计系统与机器人抓取、操作和导航任务进行更紧密的端到端结合。

## 10. 总结
OPT-Pose是一项具有里程碑意义的工作。它通过巧妙的几何分解和统一的深度学习框架，成功弥合了类别级绝对姿态估计与未见物体相对姿态估计之间的鸿沟。该方法不仅在各种基准测试上达到了最先进的性能，更重要的是，它摆脱了对CAD模型、固定类别体系和精确相机先验的依赖，向更具泛化性和实用性的“模型无关”三维视觉迈出了关键一步。其灵活的多模态、多视图支持架构为未来的研究和实际应用提供了强大的基础。

（完）
