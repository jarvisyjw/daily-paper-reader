---
title: Static Scene Reconstruction from Dynamic Egocentric Videos
title_zh: 基于动态第一人称视频的静态场景重建
authors: "Qifei Cui, Patrick Chen"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22450v1"
tags: ["query:3d"]
score: 10.0
evidence: 基于第一视角视频的3D重建与位姿图拼接
tldr: 针对自我中心视频中因相机快速运动和动态交互导致现有静态三维重建系统性能下降的问题，本研究提出了一种鲁棒的流水线方法。该方法通过掩码感知重建机制抑制动态前景干扰，并采用分块重建与位姿图拼接策略来保证全局一致性、消除长期漂移。实验证明，该方案显著提升了轨迹精度并生成了干净的静态几何结构，成功将基础模型的能力扩展至动态第一人称场景。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22450v1/fig-001.webp\", \"caption\": \"Figure 5. Module B3&4: chunk-wise reconstruction and overlap-based Sim(3) stitching. We split a long egocentric RGB video into overlapping temporal chunks (here K=192 frames with overlap O=96). Each chunk is reconstructed independently by MapAnything, producing chunk-local camera poses and dense point clouds. To obtain a globally aligned trajectory and static map, we estimate a similarity transform between consecutive chunks by Umeyama alignment on overlap camera centers, anchoring the global frame with the first chunk (s=1, R=I, t=0). We then apply the estimated Sim(3) transform to map each chunk into the global frame and fuse transformed point clouds (with optional subsampling for memory control), yielding a stitched global camera trajectory and a fused point cloud with reduced cross-chunk misalignment.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 376}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-002.webp\", \"caption\": \"Figure 8. EPIC-KITCHENS Sense point-cloud reconstructions comparing four masking strategies across three scenes (rows). Baseline (no masking) exhibits severe hand ghosting. Blackout removes dynamic regions at the image level, resulting in large black voids. Dynamin (ours) suppresses most ghosting while preserving sufficient static context to maintain spatial consistency. Overall-mask removes all moving objects from the beginning, often leading to unstable or incomplete structure due to missing spatial cues.\", \"page\": 11, \"index\": 2, \"width\": 1036, \"height\": 573}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-003.webp\", \"caption\": \"Figure 11. SAM3D single-frame reconstruction (exploratory). For each object category, we show the input image (with mask) and the generated textured mesh. While outside our main scope, these results suggest a future direction for object-level reconstruction on top of our static mapping pipeline.\", \"page\": 15, \"index\": 3, \"width\": 1036, \"height\": 548}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-004.webp\", \"caption\": \"Figure 1. Overall pipeline. Module A constructs a binary dynamic activity prior Dt from VLM (Gemini) prompts and SAM3 video segmentation/tracking, optionally gated by interaction time windows. Module B runs chunk-wise reconstruction and fusion (MapAnything) while ignoring dynamic pixels indicated by Dt, then stitches chunks using overlap-based Sim(3) alignment to obtain globally consistent poses and a fused static map.\", \"page\": 2, \"index\": 4, \"width\": 1036, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-005.webp\", \"caption\": \"Figure 9. Semantic grounding vs. ID fragmentation (Grounded-SAM2). Grounded-SAM2 provides semantic relevance but exhibits severe ID fragmentation in long egocentric videos; our post-hoc matching reduces the number of fragmented IDs but does not fully resolve failures under heavy occlusion and re-entry.\", \"page\": 14, \"index\": 5, \"width\": 1036, \"height\": 507}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-006.webp\", \"caption\": \"Figure 10. Temporal stability vs. over-segmentation (SAMTrack + DeAOT). While long-term memory improves ID retention, the method progressively segments and tracks many irrelevant regions in long videos, yielding an explosion in tracked objects without semantic filtering.\", \"page\": 14, \"index\": 6, \"width\": 496, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-007.webp\", \"caption\": \"Figure 2. Module A: interaction-aware dynamic prior pipeline. Given egocentric video frames (or reconstruction chunks), a VLM selects scene-relevant semantic categories and text prompts (e.g., hand, plate, cup). SAM3 then performs concept-driven video segmentation and tracking to produce per-category instance masks with consistent IDs. We aggregate these masks into a per-frame dynamic prior Dt (union over categories, optionally filtered by proximity to the hand to isolate manipulated objects). Finally, MapAnything consumes Dt to suppress dynamic regions and reconstruct the static scene with reduced ghosting artifacts.\", \"page\": 4, \"index\": 7, \"width\": 1036, \"height\": 567}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-008.webp\", \"caption\": \"Figure 4. Dynamic prior injection for robust 3D reconstruction. We augment the standard transformer attention between 3D query tokens (cameras / 3D points) and image tokens (pixel patches) by gating cross-attention with a dynamic mask Dt. Tokens marked as dynamic (red) are suppressed, so 3D queries attend primarily to static regions (green), reducing motion-induced contamination and ghosting artifacts in the reconstructed scene.\", \"page\": 5, \"index\": 8, \"width\": 496, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2603.22450v1/fig-009.webp\", \"caption\": \"Figure 3. Interaction-activated dynamic prior. We show a SAM3 tracking frame with instance IDs together with the corresponding interaction-activated binary prior Dt (hands always masked; objects masked cumulatively after onset), which is used to construct attention masks in the reconstruction module.\", \"page\": 5, \"index\": 9, \"width\": 496, \"height\": 249}]"
motivation: 解决因快速相机运动和频繁动态交互（如手部移动）导致的现有静态重建系统在自我中心视频中性能下降的问题。
method: 提出了一种掩码感知重建机制，在注意力层中显式抑制动态前景，并采用分块重建与位姿图拼接策略以确保全局一致性。
result: 在HD-EPIC和室内无人机数据集上的实验表明，该方法显著降低了绝对轨迹误差并生成了视觉上干净的静态几何结构。
conclusion: 该方法有效扩展了基础模型在动态第一人称场景中的静态重建能力。
---

## 摘要
由于相机快速运动和频繁的动态交互，第一人称视频为三维重建带来了独特的挑战。最先进的静态重建系统（如MapAnything）在此类场景中性能往往下降，存在灾难性的轨迹漂移以及由移动的手部引起的“鬼影”几何结构。我们通过提出一个鲁棒的流程来弥合这一差距，该流程使静态重建主干网络能够适应长序列的第一人称视频。我们的方法引入了一种掩码感知重建机制，在注意力层中显式抑制动态前景，防止手部伪影污染静态地图。此外，我们采用了一种分块重建策略并结合位姿图拼接，以确保全局一致性并消除长期漂移。在HD-EPIC和室内无人机数据集上的实验表明，与简单基线相比，我们的流程显著改善了绝对轨迹误差并生成了视觉上干净的静态几何结构，有效地将基础模型的能力扩展到了动态第一人称场景。

## Abstract
Egocentric videos present unique challenges for 3D reconstruction due to rapid camera motion and frequent dynamic interactions. State-of-the-art static reconstruction systems, such as MapAnything, often degrade in these settings, suffering from catastrophic trajectory drift and "ghost" geometry caused by moving hands. We bridge this gap by proposing a robust pipeline that adapts static reconstruction backbones to long-form egocentric video. Our approach introduces a mask-aware reconstruction mechanism that explicitly suppresses dynamic foreground in the attention layers, preventing hand artifacts from contaminating the static map. Furthermore, we employ a chunked reconstruction strategy with pose-graph stitching to ensure global consistency and eliminate long-term drift. Experiments on HD-EPIC and indoor drone datasets demonstrate that our pipeline significantly improves absolute trajectory error and yields visually clean static geometry compared to naive baselines, effectively extending the capability of foundation models to dynamic first-person scenes.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将对这篇题为《Static Scene Reconstruction from Dynamic Egocentric Videos》的论文进行结构化、深入且客观的总结。

---

## 1. 核心问题与整体含义
*   **研究动机**：第一人称（自我中心）视频因其剧烈的相机运动和频繁的动态交互（如手部操作物体），给三维静态场景重建带来了巨大挑战。现有的先进静态重建系统（如MapAnything）在此类动态场景中性能严重下降，主要表现为：
    1.  **长期漂移**：长序列导致轨迹累积误差，使整个建筑结构“扁平化”。
    2.  **初始化脆弱**：相机位姿初始化不稳定，可能出现180°翻转。
    3.  **鬼影几何**：移动的手部和物体被错误地融合进静态地图，形成物理上不可能的“鬼影”结构。
*   **核心目标**：在不重新训练或微调基础模型的前提下，如何将MapAnything这类静态重建骨干网络适配到动态的第一人称视频中，从而获得可靠的静态地图并显式处理动态前景。

## 2. 方法论
论文提出一个两阶段的鲁棒流程，核心思想是**通过掩码感知机制抑制动态内容对重建过程的干扰，并通过分块对齐策略保证全局一致性**。

*   **整体流程**：
    1.  **模块A：构建交互感知的动态先验掩码**
        *   输入长视频序列。
        *   使用技能分割器（SymSkill-style）和LLM识别出可能发生运动的物体及其交互起始时间。
        *   利用视觉语言模型（VLM）为这些物体提供粗略的空间定位提示（如边界框）。
        *   将提示输入SAM3进行视频分割与实例跟踪，获得具有持久ID的逐帧实例掩码。
        *   根据交互起始时间激活掩码：手部始终被抑制；物体在首次被操作后即被持续抑制（累积激活）。
        *   输出逐帧的二值动态先验掩码 `Dt`。

    2.  **模块B：分块重建与令牌级动态门控**
        *   **分块处理**：将长视频分割为重叠的时间块（如每块192帧，重叠96帧），以应对内存和计算限制。
        *   **掩码注入与注意力门控**：
            *   将像素级动态掩码 `Dt` 映射到MapAnything的2D图像令牌网格上。
            *   在MapAnything的推理过程中（无需训练），修改其注意力机制。具体做法是：在计算注意力权重时，对标记为动态的键/值向量施加一个极大的负偏置 `-∞`。这使得经过Softmax后，来自动态区域的注意力权重趋近于零。
            *   此操作应用于两个关键环节：(a) 时间上的2D自注意力层，防止动态证据跨帧传播；(b) 2D→3D交叉注意力层，阻止动态图像令牌更新3D几何表示。
        *   **全局对齐与拼接**：
            *   每个分块独立重建后，会得到各自局部坐标系下的相机位姿和点云。
            *   利用相邻分块重叠部分的相机中心轨迹，通过Umeyama算法估计一个Sim(3)变换（包含旋转、平移和尺度）。
            *   将所有分块的位姿和点云依次变换到以第一个分块为原点的全局坐标系下并融合。

## 3. 实验设计
*   **数据集/场景**：
    *   HD-EPIC (EPIC-KITCHENS HD)：大规模第一人称厨房活动数据集。实验主要在一个包含3565帧的长序列上进行评估。
*   **基准方法 (Benchmark)**：
    *   MapAnything (Baseline)：原始方法的分块+拼接版本作为主要对比基线。
*   **对比方法 (Ablations)**：
    1.  Baseline: MapAnything + Sim(3) stitching, no dynamic suppression.
    2.  Naive blackout: Pixel-level blackout of masked regions in input frames.
    3.  Attention gating (dynamic-only): Token-level attention masking using per-frame dynamic masks.
    4.  Attention gating (cumulative/full-time): **(本文方法)** Token-level attention masking using interaction-activated cumulative masks.

## 4. 资源与算力
论文在“Implementation details”部分明确提到：
*   **硬件平台**: RTX-5090 GPU (32 GB)。
*   **运行时间**: 
    - Chunk-wise reconstruction: ~49秒/块
    - Full sequence processing: ~6分钟13秒/序列 （基于6Hz下采样设置）

## 5. 实验数量与充分性
实验设计较为系统和客观：
*   **主实验对比**: 在HD-EPIC数据集的一个代表性长序列上完整评估了四种方法变体。
*   **消融实验**: 
    1.  对比了不同的掩码策略（无、像素级、仅瞬时动态、累积激活）。
    2.  验证了重叠对齐的必要性及效果（图7）。
    3.  探索了不同跟踪方案并分析了失败原因（补充材料）。
*   **评价指标全面且针对性设计**: 
    1.  **(A) Overlap geometry consistency (`B`)**:衡量跨分块的几何一致性误差。发现此指标易受深度完整性影响而混淆结果解释。
    2. **(C) Density-normalized dynamic contamination (`Cden ratio`, `Cocc ratio`, `Cod ratio`)**:本文提出的核心指标。通过归一化处理消除了点云密度差异的影响，更真实地反映对动态区域的抑制效果。结果显示本文方法显著降低了该比值。
    3.**(D) Depth coverage (`D`)**:评估深度预测的完整性。发现使用注意力门控会降低深度覆盖率。
    4.**(E) Overlap camera-center residual (`ecen`) & Scale stability (`¯s`)**:评估轨迹拼接精度和尺度稳定性。

总体而言，实验围绕核心目标展开验证了方法的有效性但也揭示了其内在权衡关系——即抑制鬼影与保持几何完整性之间的冲突。评价指标的改进体现了作者对问题本质的深入思考。

##6 .论文的主要结论与发现
1 .成功地将静态重建骨干网络(Map Anything )适配到了高度动态的第一人称视频场景中实现了鲁棒的静态地图构建 。
2 .提出的“交互感知的动态先验+令牌级注意力门控”机制能有效减少由手部和移动物体引起的“鬼影”伪影生成更干净的静态几何结构 。
3 .“分块重建+Sim(3 )拼接”策略能够有效缓解长序列带来的轨迹漂移问题保证全局一致性 。
4 .存在一个关键的权衡(Trade-off ):虽然注意力门控显著降低了动态污染但同时也减少了深度覆盖率和跨分块的几何一致性 。这表明过度抑制可能导致有用的空间线索丢失 。
5 .简单的像素级遮挡(Naive blackout )效果有限而基于注意力的抑制更为有效 。

##7 .优点
1 .问题定义清晰 :精准指出了现有SOTA方法在特定应用场景下的失败模式 。
2 .方法论创新 :巧妙地将大模型(SAM3 , VLM , LLM )的能力与传统三维重建流程结合形成了一套端到端的解决方案 。特别是利用LLM/VLM进行语义理解和SAM进行跟踪来生成高质量的时序掩码 。
3 .无需重训练 :所有改进均在推理阶段完成通过修改注意力机制实现保持了基础模型的通用性易于部署 。
4 .评价指标设计合理 :针对性地提出了密度归一化的污染度量避免了传统指标因覆盖率变化而产生的误导 。

##8 .不足与局限
1 .性能权衡 :如前所述该方法以牺牲一定的几何完整性和重叠一致性为代价来换取更干净的静态地图 。这在交互密集区域尤为明显 。
2 .依赖上游组件性能 :整个流程依赖于技能分割器 、LLM 、VLM和SAM等上游模块的错误会向下传播影响最终的重建质量 。例如错误的交互起始时间判断会导致不正确的掩码激活 。
3 .缺乏闭环优化 :当前的拼接是顺序进行的没有进行全局的姿态图优化或闭环检测因此在极长的序列中仍可能存在长期漂移 。
4 .掩码质量敏感 :SAM生成的掩码不完美可能导致漏检(残留污染 )或过检(丢失静态几何 )。如何处理对象重新进入视野等情况仍是未来工作 。

---

**(完)**

好的，我将继续从上次总结的末尾开始，补充关于论文的进一步分析、潜在影响以及最终结论。

---

## 9. 潜在影响与未来方向
*   **对领域的贡献**：
    1.  **问题范式**：该工作明确地将“从动态自我中心视频中重建静态场景”定义为一个独立且重要的任务，并提供了首个系统性的解决方案框架。这为后续研究设立了清晰的基准和问题边界。
    2.  **技术路径**：证明了通过“大模型生成先验 + 推理时注意力门控”来适配通用骨干网络的可行性。这种“即插即用”的思路为将其他静态重建或SLAM系统扩展到动态场景提供了一种低成本的参考路径。
    3.  **评估标准**：提出的密度归一化污染指标（`Cden ratio`等）为解决类似“抑制-完整性权衡”问题提供了更公平的评估工具。
*   **未来研究方向**：
    1.  **缓解权衡**：探索更精细的动态抑制策略（如软性门控、置信度加权），而非简单的二进制硬抑制，以在减少鬼影的同时保留更多有用的静态线索。
    2.  **提升鲁棒性**：改进动态掩码生成流程，例如引入时序一致性更强的跟踪器，或设计端到端可微的系统以减少错误传播。
    3.  **全局优化**：在分块拼接后引入全局捆绑调整（Bundle Adjustment）或闭环检测，以进一步提升长序列的全局一致性。
    4.  **应用拓展**：将该方法应用于机器人操作、增强现实（AR）导航等需要从第一人称交互视频中理解环境长期结构的实际任务中。

## 10. 总结与最终评价
本文《Static Scene Reconstruction from Dynamic Egocentric Videos》是一项针对性强、方法新颖且实验扎实的工作。它敏锐地捕捉到了现有先进静态重建系统在自我中心动态视频这一极具挑战性场景下的失效模式，并提出了一套完整的两阶段流程来应对。

其核心创新在于：
1.  利用大语言模型和视觉基础模型的组合能力，自动化地生成高质量的、具有交互感知的动态时序掩码作为先验知识。
2.  设计了一种无需重新训练的令牌级注意力门控机制，将上述先验无缝注入到MapAnything等骨干网络中，有效隔离了动态内容对重建过程的污染。
3.  采用分块处理与Sim(3)对齐的策略，在计算资源受限的情况下维持了长序列重建的可行性。

实验结果清晰地验证了该方法在显著降低“鬼影”污染方面的有效性，同时也坦诚地揭示了其内在的权衡——即清洁度与完整性之间的冲突。论文的优点在于其清晰的逻辑链条、巧妙的模块化设计以及对评估指标的深入思考。其主要局限则在于性能对上游模块的依赖以及尚未解决的权衡问题。

总体而言，这项工作为动态自我中心场景下的鲁棒静态三维重建开辟了一条有前景的技术路径，并为进一步的研究奠定了重要的基础。

（完）
