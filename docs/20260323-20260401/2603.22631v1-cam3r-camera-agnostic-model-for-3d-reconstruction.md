---
title: "CAM3R: Camera-Agnostic Model for 3D Reconstruction"
title_zh: CAM3R：一种相机无关的三维重建模型
authors: "Namitha Guruprasad, Abhay Yadav, Cheng Peng, Rama Chellappa"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22631v1"
tags: ["query:3d"]
score: 10.0
evidence: 相机无关的稠密3D重建模型
tldr: CAM3R是一种相机无关的三维重建模型，旨在解决现有方法因依赖针孔相机假设而在处理广角图像时出现的几何失真问题。该模型采用两阶段网络结构：射线模块估计像素级射线方向，跨视图模块推断径向距离并生成置信度图；同时引入射线感知全局对齐框架进行姿态优化与尺度统一。实验表明，该方法在多种相机模型上均实现了最先进的姿态估计与三维重建性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.22631v1/fig-001.webp\", \"caption\": \"Fig. 2: CAM3R Overview. Given an input image pair (I1, I2), the framework operates through two parallel streams. The Shared Ray Module recovers the internal camera geometry by regressing Spherical Harmonic coefficients to reconstruct continuous ray directional fields di. Simultaneously, the Cross-view Module extracts features and utilizes a dual-block transformer decoder to facilitate information exchange between the two views. Specialized DPT heads then regress radial distances ri with confidence maps σi, while a Relative Pose Network estimates the rigid transformation P2→1. The local pointmaps Xi,i are generated by fusing rays di with radial distances ri. Finally, the second view is transformed into the reference coordinate frame of the first view via P2→1 to produce the globally aligned 3D reconstruction.\", \"page\": 6, \"index\": 1, \"width\": 723, \"height\": 234}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-002.webp\", \"caption\": \"Fig. 3: Qualitative Two-View Reconstructions. Visualization of 3D point clouds for image pairs across diverse optical manifolds (panorama, fisheye, pinhole). Despite extreme radial distortions and camera geometries, relative poses are accurately recovered and structural consistency is maintained. Note this is the raw output of the network.\", \"page\": 11, \"index\": 2, \"width\": 723, \"height\": 530}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-003.webp\", \"caption\": \"Fig. 5: Qualitative Multi-View Reconstructions. Global camera trajectories and dense point clouds recovered from unstructured image pools across diverse datasets. Despite high radial distortion and lack of scenegraph information, globally consistent poses and structural geometry are maintained through the Ray-Aware Global Alignment, effectively mitigating trajectory drift and scale ambiguity.\", \"page\": 13, \"index\": 3, \"width\": 723, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2603.22631v1/fig-004.webp\", \"caption\": \"Table 1: Two-view relative pose estimation results. We report Accuracy at 15◦ (higher is better). Bold indicates best performance.\", \"page\": 12, \"index\": 4, \"width\": 718, \"height\": 181}]"
motivation: 现有三维重建模型主要基于针孔相机训练，在处理广角图像时存在几何失真问题，需要一种相机无关的解决方案。
method: 提出两阶段网络结构（射线模块与跨视图模块），结合射线感知全局对齐框架进行姿态优化与尺度统一。
result: 在包括全景、鱼眼和针孔相机的多种数据集上验证了方法的优越性。
conclusion: CAM3R通过相机无关的架构设计，在多种相机模型上实现了最先进的姿态估计与三维重建性能。
---

## 摘要
从无位姿图像中恢复稠密三维几何结构仍然是计算机视觉领域的一项基础性挑战。当前最先进的模型主要在透视数据集上进行训练，这隐式地将其约束于标准针孔相机几何。因此，当这些模型应用于通过非直线光学器件（如鱼眼或全景传感器）捕获的广角图像时，会出现显著的几何退化。为解决此问题，我们提出了CAM3R，一种相机无关的前馈三维重建模型，能够处理来自广角相机模型的图像而无需事先校准。我们的框架由一个双视图网络构成，该网络分为两个模块：用于估计逐像素光线方向的射线模块（RM），以及用于通过置信度图、点图和相对位姿推断径向距离的跨视图模块（CVM）。为了将这些成对预测统一到一个一致的三维场景中，我们引入了一个射线感知全局对齐框架，用于位姿优化和尺度优化，同时严格保持预测的局部几何结构。在包括全景、鱼眼和针孔图像在内的多种相机模型数据集上进行的大量实验表明，CAM3R在位姿估计和重建方面确立了新的最先进水平。

## Abstract
Recovering dense 3D geometry from unposed images remains a foundational challenge in computer vision. Current state-of-the-art models are predominantly trained on perspective datasets, which implicitly constrains them to a standard pinhole camera geometry. As a result, these models suffer from significant geometric degradation when applied to wide-angle imagery captured via non-rectilinear optics, such as fisheye or panoramic sensors. To address this, we present CAM3R, a Camera-Agnostic, feed-forward Model for 3D Reconstruction capable of processing images from wide-angle camera models without prior calibration. Our framework consists of a two-view network which is bifurcated into a Ray Module (RM) to estimate per-pixel ray directions and a Cross-view Module (CVM) to infer radial distance with confidence maps, pointmaps, and relative poses. To unify these pairwise predictions into a consistent 3D scene, we introduce a Ray-Aware Global Alignment framework for pose refinement and scale optimization while strictly preserving the predicted local geometry. Extensive experiments on various camera model datasets, including panorama, fisheye and pinhole imagery, demonstrate that CAM3R establishes a new state-of-the-art in pose estimation and reconstruction.

---

## 论文详细总结（自动生成）

# CAM3R: Camera-Agnostic Model for 3D Reconstruction 论文分析总结

## 1. 核心问题与整体含义
*   **研究动机**：当前最先进的3D重建基础模型（如DUSt3R、MASt3R等）主要基于针孔相机模型的数据集进行训练。这种隐含的假设导致它们在处理由非线性光学器件（如鱼眼、全景镜头）捕获的广角图像时，会产生严重的几何失真和性能退化。
*   **核心问题**：如何构建一个**相机无关**的、前馈式的三维重建模型，使其能够直接从无标定、无位姿的图像中恢复稠密几何结构，并适用于多种相机模型（针孔、鱼眼、全景），克服现有方法对特定相机几何的依赖。

## 2. 方法论
*   **核心思想**：将相机内部几何（光线方向）与场景几何（深度/距离）解耦学习，并通过一个统一的框架进行联合优化。
*   **关键技术细节**：
    *   **两视图网络架构**：
        1.  **射线模块 (Ray Module, RM)**：使用共享的几何编码器和Transformer射线编码器，通过回归球谐函数系数来重建连续的逐像素光线方向场 `di`。这本质上是学习相机的内部校准（内参），而不依赖于参数化模型。
        2.  **跨视图模块 (Cross-view Module, CVM)**：采用孪生编码器和双分支Transformer解码器进行跨视图信息交换。它专门预测每个视图的径向距离 `ri`、置信度图 `σi`，以及两视图之间的相对位姿 `P2→1`。
    *   **点图生成**：局部点图通过 `Xi,i(u) = di(u) · ri(u)` 生成。
    *   **损失函数**：
        *   **非对称角度损失 (LA)**：监督光线方向预测，防止向最常见的针孔模式坍缩。
        *   **局部回归损失 (Lregr)**：监督局部点图的准确性。
        *   **相对位姿损失 (Lpose)**：监督旋转和平移预测。
        *   总损失为加权和：`Ltotal = λA LA + λregr Lregr + λpose Lpose`。
    *   **射线感知全局对齐 (Ray-Aware Global Alignment)**：
        *   用于多视图重建。首先对 exhaustive scene graph 进行成对推理和两阶段剪枝（对称位姿一致性检查、基于3D互最近邻的几何重叠验证）。
        *   在优化过程中，将点约束在预测的光线方向上移动，从而在优化深度、位姿和尺度时保持相机的光线流形不变。目标函数最小化所有连接图像对之间的置信度加权3D对齐误差。

## 3. 实验设计
*   **数据集**：
    *   **训练与评估集**：使用了增强版的多模态数据集混合物，包括全景数据集（2D3DS, 360Loc）、鱼眼数据集（ADT）、透视数据集（MegaDepth）。通过对原始数据集进行合成投影（如为全景图生成对应的鱼眼/透视视图）来引入光学异构性。
    *   **零样本评估集**：CO3Dv2数据集及其合成的鱼眼对应版本。
*   **基准任务与指标**：
    *   **双视图相对位姿估计**：报告旋转精度 (RRA@15°) 和平移精度 (RTA@15°)。
    *   **多视图重建**：报告旋转/平移精度 (RRA/RTA@30°)、平均精度 (mAA@30°) 和绝对轨迹误差 (ATE RMSE)。
*   **对比方法**：
    *   与当前最先进的3D基础模型对比，包括 DUSt3R, MASt3R, Pow3R, VGGT, π³。

## 4. 资源与算力
*   论文中提到训练在4块 NVIDIA H200 GPU上进行。
*   使用了课程学习的两阶段训练策略。具体训练时长未明确说明。

## 5. 实验数量与充分性
*   **实验组别充分**：
    1.  **主实验**：在多个异构数据集上进行了全面的双视图和多视图重建实验，并与多个SOTA方法进行了定量比较。
    2.  **消融实验**：
        *   验证了异构训练阶段的重要性（CAM3R vs. CAM3R-homo）。
        *   验证了射线感知全局对齐的有效性（对比DUSt3R的全局对齐方法）。
    3.  **架构消融研究（补充材料）**：详细阐述了从直接微调DUSt3R到最终解耦设计的演进过程，并提供了定量和定性证据说明各阶段的效果。
    4.  **零样本泛化测试**：在未见过的CO3Dv2和Matterport等数据集上展示了泛化能力。
    5.  **丰富的定性分析**：提供了大量可视化结果，展示了对极端畸变图像的鲁棒性、跨模态匹配能力以及场景图剪枝效果。
*   **客观性与公平性**：
    *   遵循了各数据集的官方划分协议以确保公平比较。
    *   对于多视图评估，由于部分基线方法在异构光学器件上性能严重下降，作者选择了在该设定下仍能工作的VGGT和π³进行比较，这体现了评估的针对性而非选择性忽略。

## 6.主要结论与发现
1. CAM3R成功实现了相机无关的三维重建，在处理针孔、鱼眼和全景图像时均达到了最先进的性能水平。
2. “解耦”的设计思想至关重要——将光线估计与场景深度/位姿估计分离，有效解决了同时学习非线性投影和多视图形状的优化难题。简单的微调现有模型不足以解决此问题。
3. “射线感知全局对齐”框架显著优于基于针孔假设的传统捆绑调整或对齐方法，特别是在处理广角图像时能有效抑制轨迹漂移和尺度模糊性。

##7 .优点
1.	创新性地提出并验证了“相机-场景解耦”的学习范式是解决跨相机模型三维重建问题的有效途径 。
2	提出的“射线感知全局对齐”优化框架 ，将问题提升到纯粹的 、保持光线一致性的三维空间中进行求解 ，理论优雅且实践有效 。
4	实验设计非常全面且具有说服力 ，不仅涵盖了标准评测 、消融分析 ，还通过详尽的架构演进分析阐明了设计决策背后的根本原因 。
5	展现出强大的零样本泛化能力 ，表明该方法学到了通用的几何先验 。

##8 .不足与局限
1	架构开销 ：当前设计使用了两个独立的ViT骨干网络 （分别用于RM和CVM ） ，导致内存占用较高 。未来可通过参数共享等方式提高效率 。
2	可扩展性 ：多视图重建需要对所有图像对进行O(N²)次前向推理以构建场景图 。尽管剪枝策略可以缓解 ，但对于超大规模图像集合仍显昂贵 。
4	应用限制 ：虽然能处理多种相机类型 ，但论文未讨论其对更极端或非结构化光学系统 （如折反射镜头 、事件相机 ）的适用性 。此外 ，实时性应用可能受限于当前模型的复杂度 。

(完)

好的，我们继续从“不足与局限”部分开始补全。

## 8. 不足与局限（续）
5.  **理论边界**：论文的核心假设是相机模型可以通过一个连续的、逐像素的光线方向场来表征。虽然这涵盖了广泛的模型（针孔、鱼眼、全景），但对于那些光线方向无法被平滑函数有效建模的复杂或非结构化光学系统（例如，存在严重遮挡或由多个独立光学元件拼接而成的系统），方法的有效性尚未得到验证。
6.  **数据依赖**：模型的强大性能依赖于大规模、多样化的异构训练数据。尽管论文展示了零样本泛化能力，但其在真实世界极端、罕见或噪声极大的光学畸变下的鲁棒性仍需进一步在更广泛的野外数据上进行验证。
7.  **尺度模糊性**：与大多数单目/双视图方法一样，CAM3R恢复的是度量尺度未知的场景。虽然“射线感知全局对齐”在多视图设置中通过优化解决了尺度一致性问题，但这依赖于足够的视图重叠和成功的场景图构建。在仅有少数视图或纹理匮乏的区域，尺度模糊问题可能依然存在。

## 9. 未来展望
1.  **效率优化**：探索更轻量级的架构设计，如共享编码器参数、知识蒸馏或将RM和CVM进一步集成，以降低计算和内存开销，使其更适合移动端或实时应用。
2.  **扩展到动态场景**：当前工作专注于静态场景重建。一个自然的延伸是处理动态物体和场景，这需要将时间维度与相机几何解耦相结合。
3.  **更广泛的光学建模**：探索将方法应用于更奇特的成像模型，如折反射镜头、光场相机或非中心相机模型，进一步挑战“相机无关”的边界。
4.  **与生成式模型的结合**：利用学到的通用几何先验来辅助3D生成任务（如文本/图像到3D生成），特别是在处理非标准视角输入时。
5.  **自监督与数据效率**：研究如何通过自监督或弱监督方式减少对大规模精确标注数据（如深度、位姿）的依赖，提升方法的可访问性和实用性。

## 10. 总结评价
CAM3R是一项具有重要理论和实践价值的研究。它敏锐地指出了当前3D重建基础模型对针孔相机假设的隐性依赖这一根本局限，并提出了一个优雅而有效的解决方案——通过解耦学习光线方向场和场景几何来实现真正的相机无关性。该方法不仅在多个异构数据集上取得了最先进的性能，其“射线感知”的优化思想也为多视图几何处理提供了新视角。尽管在计算效率和理论边界上存在提升空间，但这项工作无疑为构建更具通用性和鲁棒性的三维视觉基础模型开辟了一条清晰且有前景的道路。

（完）
